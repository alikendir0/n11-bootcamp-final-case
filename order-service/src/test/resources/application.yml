# Disables config-server fail-fast import for ALL tests (including @DataJpaTest / @JdbcTest
# slice tests). Loaded at bootstrap time before profile-specific files.
spring:
  application:
    name: order-service
  config:
    import: "optional:configserver:"
  flyway:
    enabled: false
