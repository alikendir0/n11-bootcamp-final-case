# Leave empty for same-origin relative requests proxied by Vite to the gateway
# (see vite.config.ts). Works on localhost and behind the Cloudflare tunnel unchanged.
# Set an absolute URL only to point the browser at a gateway on a different origin.
VITE_API_BASE_URL=
VITE_FREE_SHIPPING_THRESHOLD=500
