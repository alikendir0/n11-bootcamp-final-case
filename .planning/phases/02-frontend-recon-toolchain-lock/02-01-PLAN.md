---
phase: 02-frontend-recon-toolchain-lock
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/recon/package.json
  - tools/recon/tsconfig.json
  - tools/recon/playwright.config.ts
  - tools/recon/.gitignore
  - tools/recon/README.md
  - tools/recon/lib/dismiss-banners.ts
  - tools/recon/lib/harvest-copy.ts
  - tools/recon/lib/harvest-colors.ts
  - tools/recon/assemble-recon.ts
  - tools/recon/check-recon.ts
  - scripts/check-phase-02-artifacts.sh
  - .gitignore
autonomous: true
requirements: [FE-01]
tags: [phase-2, frontend-recon, playwright, toolchain]

must_haves:
  truths:
    - "tools/recon/ npm project exists and `npx playwright --version` reports 1.59.x"
    - "tools/recon/playwright.config.ts has anti-bot posture (headless:false, real Chrome UA, locale tr-TR, --disable-blink-features=AutomationControlled, workers:1, fullyParallel:false, slowMo:250)"
    - "tools/recon/tsconfig.json exists with strict TS config (target ES2022, module ESNext, moduleResolution bundler, strict true, allowImportingTsExtensions true, noEmit true, types: [\"node\"]) so `npx tsc --noEmit` is a deterministic type-check signal"
    - "scripts/check-phase-02-artifacts.sh --bootstrap exits 0 against the empty Wave-0 state"
    - "tools/recon/ is NOT registered in settings.gradle.kts and has no build.gradle.kts"
    - "Root .gitignore has additive recon block; no existing lines reordered or removed"
    - ".planning/intel/screenshots/ directory is reserved (committed via .gitkeep or pre-created)"
  artifacts:
    - path: "tools/recon/package.json"
      provides: "npm manifest with @playwright/test 1.59.x pinned exact, tsx devDep, typescript devDep, Node engines >=22.12, three scripts: capture/assemble/check"
      contains: "\"@playwright/test\": \"1.59"
    - path: "tools/recon/tsconfig.json"
      provides: "Strict TypeScript config — target ES2022, module ESNext, moduleResolution bundler, strict true, allowImportingTsExtensions true, noEmit true, types: [\"node\"]; enables `npx tsc --noEmit` as the canonical type-check gate"
      contains: "\"noEmit\": true"
    - path: "tools/recon/playwright.config.ts"
      provides: "Playwright config with anti-bot launchOptions, tr-TR locale, Europe/Istanbul timezone, baseURL=https://www.n11.com, workers:1"
      contains: "disable-blink-features=AutomationControlled"
    - path: "tools/recon/.gitignore"
      provides: "Local recon ignore (node_modules, test-results, playwright-report, output, .playwright)"
      contains: "node_modules"
    - path: "tools/recon/lib/dismiss-banners.ts"
      provides: "Defensive cookie-banner dismisser (count() > 0 + isVisible().catch guards)"
      exports: ["dismissBanners"]
    - path: "tools/recon/lib/harvest-copy.ts"
      provides: "Turkish-charset phrase harvester (1-6 words, tag whitelist, deduplicated)"
      exports: ["harvestCopy"]
    - path: "tools/recon/lib/harvest-colors.ts"
      provides: "Computed-style harvester + rgbToHex helper"
      exports: ["harvestColors", "rgbToHex"]
    - path: "tools/recon/assemble-recon.ts"
      provides: "Markdown assembler that writes the 8-section recon report"
      contains: "## 1. Page Inventory"
    - path: "tools/recon/check-recon.ts"
      provides: "Sanity checker (≥30 phrase rows, ≥10 token rows, all 8 sections, all 7 screenshots, plus 9th assertion: Decision Matrix subsection appended by Plan 02-03)"
      contains: "Turkish Copy Catalog"
    - path: "tools/recon/README.md"
      provides: "Run instructions + anti-bot fallback ladder + 'what it does NOT do' guard"
      contains: "launchPersistentContext"
    - path: "scripts/check-phase-02-artifacts.sh"
      provides: "Dual-mode artifact lint (default + --bootstrap)"
      contains: "--bootstrap"
    - path: ".gitignore"
      provides: "Append-only recon ignore block"
      contains: "tools/recon/node_modules"
  key_links:
    - from: "tools/recon/playwright.config.ts"
      to: "Pattern 2 in 02-RESEARCH.md lines 386-425"
      via: "verbatim copy of anti-bot posture"
      pattern: "headless:\\s*false"
    - from: "tools/recon/lib/dismiss-banners.ts"
      to: "Pattern 3 in 02-RESEARCH.md lines 435-460"
      via: "verbatim copy of defensive locator idiom"
      pattern: "count\\(\\)\\s*>\\s*0"
    - from: "tools/recon/check-recon.ts"
      to: ".planning/intel/n11-recon.md ## Decision Matrix subsection (appended by Plan 02-03 Task 1)"
      via: "9th assertion verifies the Phase-10 carry-forward subsection isn't accidentally deleted"
      pattern: "Decision Matrix"
    - from: "scripts/check-phase-02-artifacts.sh"
      to: "02-VALIDATION.md Per-Task Verification Map automated commands"
      via: "every shape command listed in the table is implemented as a check function"
      pattern: "set -euo pipefail"
---

<objective>
Bootstrap the Phase 2 recon project: scaffold `tools/recon/` as a standalone npm project (NOT a Gradle subproject), pin Playwright 1.59.x, install the Chromium binary, and lay down all the helper / assembler / checker scripts plus the artifact-lint shell script. NO captures yet — that is Plan 02. After this plan completes, `bash scripts/check-phase-02-artifacts.sh --bootstrap` exits 0, `cd tools/recon && npx playwright --version` reports a 1.59.x version, and `cd tools/recon && npx tsc --noEmit` exits 0 (clean type-check).

Purpose: Front-load every Wave-0 artifact from 02-VALIDATION.md so Plan 02's capture run starts from a complete, verified scaffold. The recon project deliberately stays outside the Gradle build (S-6 in PATTERNS) — no `tools/recon/build.gradle.kts`, no `settings.gradle.kts` include, no Playwright dep in `gradle/libs.versions.toml`.

Output: Complete `tools/recon/` scaffold (11 files including tsconfig.json), `scripts/check-phase-02-artifacts.sh`, root `.gitignore` patch, reserved `.planning/intel/screenshots/` directory. Wave 1 (Plan 02) inherits a runnable, type-checked recon harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-VALIDATION.md
@CLAUDE.md
@.gitignore

<interfaces>
<!-- Shape contracts the executor needs. Patterns 1-4 in 02-RESEARCH.md are paste-ready; PATTERNS.md S-1..S-8 are the cross-cutting rules. -->

From 02-RESEARCH.md "Pattern 2: Playwright config (anti-bot posture)" (lines 386-425) — paste into tools/recon/playwright.config.ts:
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  fullyParallel: false,
  workers: 1,
  retries: 0,
  reporter: 'list',
  use: {
    baseURL: 'https://www.n11.com',
    headless: false,
    locale: 'tr-TR',
    timezoneId: 'Europe/Istanbul',
    userAgent:
      'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 ' +
      '(KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36',
    viewport: { width: 1440, height: 900 },
    launchOptions: {
      slowMo: 250,
      args: ['--disable-blink-features=AutomationControlled'],
    },
    navigationTimeout: 60_000,
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
});
```

From 02-RESEARCH.md "Pattern 3: Cookie-banner / login-wall handling" (lines 435-460) — paste into tools/recon/lib/dismiss-banners.ts:
```typescript
import { Page } from '@playwright/test';

export async function dismissBanners(page: Page): Promise<void> {
  const cookieAcceptors = [
    page.getByRole('button', { name: /Kabul Et|Tümünü Kabul Et|Tümüne İzin Ver/i }),
    page.getByRole('button', { name: /Çerez(ler)? Kabul/i }),
  ];
  for (const btn of cookieAcceptors) {
    if (await btn.count() > 0 && await btn.first().isVisible().catch(() => false)) {
      await btn.first().click().catch(() => {});
      await page.waitForTimeout(500);
      break;
    }
  }
  const closeBtn = page.getByRole('button', { name: /Kapat|Daha Sonra|×/i });
  if (await closeBtn.count() > 0 && await closeBtn.first().isVisible().catch(() => false)) {
    await closeBtn.first().click().catch(() => {});
  }
}
```

From 02-PATTERNS.md S-7 "Versions pinned exact, not ranged" + 02-RESEARCH.md Standard Stack — package.json shape:
```json
{
  "name": "n11-recon",
  "private": true,
  "version": "0.0.0",
  "description": "Phase-2 throwaway recon project against n11.com. Not part of the Gradle build.",
  "scripts": {
    "capture": "playwright test",
    "assemble": "tsx assemble-recon.ts",
    "check": "tsx check-recon.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": { "node": ">=22.12" }
}
```

Note: re-run `npm view @playwright/test version` at install time and use the latest 1.59.x patch published. Do NOT use `^1.59.0`. `typescript` and `@types/node` are pinned with caret ranges (acceptable for devDeps that don't impact runtime).

`tools/recon/tsconfig.json` shape (Issue-1 Option-A — canonical strict config so `npx tsc --noEmit` is the verify gate):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "test-results", "playwright-report", "output"]
}
```

From 02-PATTERNS.md S-1 "UTF-8 file I/O on every read/write" — every fs.readFileSync / fs.writeFileSync MUST pass `'utf-8'`.

From 02-PATTERNS.md S-5 "Screenshots write OUT of tools/recon/, into .planning/intel/screenshots/":
```typescript
const SCREENSHOT_DIR = path.resolve(__dirname, '../../../.planning/intel/screenshots');
// __dirname is tools/recon/tests/ — three .. segments to ascend tests → recon → tools, then descend.
```

From 02-PATTERNS.md S-6 "Keep recon outside Gradle":
- DO NOT add `tools/recon` to settings.gradle.kts include(...)
- DO NOT create tools/recon/build.gradle.kts
- DO NOT add Playwright/Node deps to gradle/libs.versions.toml
- DO NOT register a Gradle task that runs Playwright
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bootstrap tools/recon/ npm project + Playwright + tsconfig + helpers</name>
  <files>tools/recon/package.json, tools/recon/tsconfig.json, tools/recon/playwright.config.ts, tools/recon/.gitignore, tools/recon/README.md, tools/recon/lib/dismiss-banners.ts, tools/recon/lib/harvest-copy.ts, tools/recon/lib/harvest-colors.ts, .gitignore, .planning/intel/screenshots/.gitkeep</files>
  <read_first>
    - tools/recon/package.json (the file being modified — will not exist yet)
    - tools/recon/tsconfig.json (the file being modified — will not exist yet)
    - tools/recon/playwright.config.ts (the file being modified — will not exist yet)
    - tools/recon/lib/dismiss-banners.ts (the file being modified — will not exist yet)
    - tools/recon/lib/harvest-copy.ts (the file being modified — will not exist yet)
    - tools/recon/lib/harvest-colors.ts (the file being modified — will not exist yet)
    - tools/recon/README.md (the file being modified — will not exist yet)
    - tools/recon/.gitignore (the file being modified — will not exist yet)
    - .gitignore (root — already exists; will be APPENDED to)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md (Patterns 1-4 paste-ready snippets, lines 285-501; Standard Stack lines 96-189; Anti-Patterns lines 503-512; Common Pitfalls #1-#7 lines 533-624)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md (per-file Shape Contracts and Shared Patterns S-1..S-8)
    - CLAUDE.md (Rule #4 — verify SDK docs before writing code; Rule #5 — no secrets in source)
  </read_first>
  <action>
    Create the recon scaffold. Use the Write tool for every file — never `cat << EOF`.

    1. **`tools/recon/package.json`** — exact shape from `<interfaces>` block above. Re-verify the latest 1.59.x patch by running `npm view @playwright/test version` and use whatever patch number is current (likely 1.59.1; if a higher 1.59.x has shipped, use that — still pin EXACT, no `^`). Do NOT add `"type": "module"` (PATTERNS.md per-file rule for package.json point 3). Include `typescript`, `@types/node`, and `tsx` as devDependencies. Include a `typecheck` npm script that runs `tsc --noEmit` (this is the Task-2 verify gate).

    2. **`tools/recon/tsconfig.json`** — paste the canonical strict-config shape from `<interfaces>` block verbatim. Critical knobs (Issue-1 Option-A — required so `npx tsc --noEmit` is a real type-check signal): `target: "ES2022"`, `module: "ESNext"`, `moduleResolution: "bundler"`, `strict: true`, `allowImportingTsExtensions: true`, `noEmit: true`, `types: ["node"]`. The `"include": ["**/*.ts"]` glob covers tests, lib, assemble-recon, check-recon — every TS file in the project gets type-checked.

    3. **`tools/recon/playwright.config.ts`** — paste the Pattern 2 block from `<interfaces>` verbatim. Critical knobs that MUST be present (PATTERNS.md S-4, S-8): `headless: false`, `workers: 1`, `fullyParallel: false`, `slowMo: 250`, `--disable-blink-features=AutomationControlled`, `locale: 'tr-TR'`, `timezoneId: 'Europe/Istanbul'`, real Chrome UA string (not `HeadlessChrome`).

    4. **`tools/recon/.gitignore`** — local ignore matching the comment-grouped style of root `.gitignore` lines 1-29. Sections: `# Node / npm` (node_modules/), `# Playwright outputs` (test-results/, playwright-report/, output/, .playwright/), `# IDE` (.idea/, .vscode/). LF line endings only (root `.gitattributes` enforces).

    5. **`tools/recon/lib/dismiss-banners.ts`** — paste the Pattern 3 block from `<interfaces>` verbatim. The defensive `count() > 0 && isVisible().catch(() => false)` guard is mandatory (PATTERNS.md S-3) — do not omit `.catch(() => false)` on `isVisible` (race-condition swallow).

    6. **`tools/recon/lib/harvest-copy.ts`** — extract the in-line `page.evaluate` block from 02-RESEARCH.md Pattern 1 lines 322-335 into an exported function `harvestCopy(page: Page): Promise<string[]>`. KEEP exactly: tag whitelist `'a, button, span, h1, h2, h3, p, li'`, word count range 1-6, Turkish charset regex `/^[a-zA-Z0-9ÇĞİÖŞÜçğıöşü\s.,'!?₺%/-]+$/`, Set-based dedup. Do NOT loop over `<*>` (anti-pattern flag at line 378).

    7. **`tools/recon/lib/harvest-colors.ts`** — export `harvestColors(page: Page): Promise<TokenRow[]>` extracting the `page.evaluate` block from 02-RESEARCH.md Pattern 1 lines 343-366 (same selectors: `header / body / button[class*="add" i] / a / h1`). Type `TokenRow = { role: string; selector: string; color: string; backgroundColor: string; fontFamily: string; fontWeight: string; fontSize: string }`. ALSO export `rgbToHex(rgb: string): string` from PATTERNS.md per-file harvest-colors.ts section (matches `/^rgba?\((\d+),\s*(\d+),\s*(\d+)/`, hex padStart 2). Do NOT call `rgbToHex` inside `harvestColors` — keep colors as `rgb(...)` at harvest time; the assembler converts (PATTERNS.md S-1 separation rule + RESEARCH Pitfall #4).

    8. **`tools/recon/README.md`** — match the author voice of `service-template/skeleton/README.md` (terse, "Why X" rationale, table for token lookup, code blocks with shell-comment context). Required sections (PATTERNS.md per-file README contract):
       - **What this is** — throwaway Phase-2 recon project, NOT a Gradle module, runs once, writes to `.planning/intel/`.
       - **How to run from a fresh clone** — `cd tools/recon && npm install && npx playwright install chromium && npm run capture && npm run assemble && npm run check`.
       - **Anti-bot fallback ladder** — (1) configured posture; (2) if 403 persists, switch to `chromium.launchPersistentContext({ userDataDir: <real-profile> })` per 02-RESEARCH.md Code Examples lines 691-708.
       - **What it writes & where** — table: `screenshots/*.png → .planning/intel/screenshots/`, `output/*.json → tools/recon/output/ (gitignored)`, `n11-recon.md → .planning/intel/n11-recon.md`.
       - **What it does NOT do** — no real login, no real cart adds (RESEARCH Anti-Patterns lines 505-506).
       - **Re-running individual specs** — `npx playwright test homepage.spec.ts`.
       - **Type-check gate** — `npm run typecheck` (alias for `tsc --noEmit`) is the canonical CI/code-review signal that all `*.ts` files compile cleanly.

    9. **Append to root `.gitignore`** — read existing file first, then add a NEW commented section at the end (do NOT rewrite or reorder existing lines). Append exactly:
       ```
       # Playwright recon (tools/recon/)
       tools/recon/node_modules/
       tools/recon/test-results/
       tools/recon/playwright-report/
       tools/recon/output/
       tools/recon/.playwright/
       ```

    10. **Reserve `.planning/intel/screenshots/`** — create `.planning/intel/screenshots/.gitkeep` (empty file) so the committed directory exists before Plan 02 writes PNGs.

    11. **Install Playwright + TypeScript + Chromium binary** — run from the project root:
        ```bash
        cd tools/recon && npm install
        cd tools/recon && npx playwright install chromium
        ```
        The first command writes `tools/recon/package-lock.json` (committed) and `tools/recon/node_modules/` (gitignored), and installs `typescript` + `@types/node` so `npx tsc --noEmit` is invokable. The second downloads the Chromium binary to `~/.cache/ms-playwright/` (NOT in repo). If `npx playwright install` requires sudo for system deps on Linux, log the error and continue — the binary itself installs to the user cache without sudo.

    Per CLAUDE.md Rule #4: re-verify `@playwright/test` latest 1.59.x via `npm view @playwright/test version` BEFORE writing package.json. Per Rule #5: no env vars / secrets needed for n11 recon — site is public.

    NEGATIVE constraints (PATTERNS.md S-6):
    - Do NOT add `include("tools:recon")` to `settings.gradle.kts`.
    - Do NOT create `tools/recon/build.gradle.kts`.
    - Do NOT add `playwright` or `tsx` entries to `gradle/libs.versions.toml`.
    - Do NOT register a Gradle task that runs Playwright (e.g., `tasks.register("recon")`).
  </action>
  <verify>
    <automated>cd tools/recon && npx playwright --version && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/recon/package.json` exits 0
    - `grep -q '"@playwright/test": "1.59' tools/recon/package.json` exits 0 (exact-pin to 1.59.x patch)
    - `grep -qE '"@playwright/test":\s*"\^|"@playwright/test":\s*"~' tools/recon/package.json` exits 1 (NO range prefix)
    - `grep -q '"private": true' tools/recon/package.json` exits 0
    - `grep -qE '"node":\s*">=22\.12"' tools/recon/package.json` exits 0
    - `grep -q '"typescript"' tools/recon/package.json` exits 0 (TS devDep present)
    - `grep -q '"@types/node"' tools/recon/package.json` exits 0
    - `grep -q '"typecheck"' tools/recon/package.json` exits 0 (npm-run alias for `tsc --noEmit`)
    - `test -f tools/recon/tsconfig.json` exits 0
    - `grep -q '"noEmit": true' tools/recon/tsconfig.json` exits 0
    - `grep -q '"strict": true' tools/recon/tsconfig.json` exits 0
    - `grep -q '"moduleResolution": "bundler"' tools/recon/tsconfig.json` exits 0
    - `grep -q '"allowImportingTsExtensions": true' tools/recon/tsconfig.json` exits 0
    - `test -f tools/recon/playwright.config.ts` exits 0
    - `grep -q "headless: false" tools/recon/playwright.config.ts` exits 0
    - `grep -q "disable-blink-features=AutomationControlled" tools/recon/playwright.config.ts` exits 0
    - `grep -q "workers: 1" tools/recon/playwright.config.ts` exits 0
    - `grep -q "fullyParallel: false" tools/recon/playwright.config.ts` exits 0
    - `grep -q "locale: 'tr-TR'" tools/recon/playwright.config.ts` exits 0
    - `grep -q "Europe/Istanbul" tools/recon/playwright.config.ts` exits 0
    - `grep -qv "HeadlessChrome" tools/recon/playwright.config.ts` exits 0 (NO HeadlessChrome substring in UA)
    - `test -f tools/recon/.gitignore && grep -q 'node_modules' tools/recon/.gitignore` exits 0
    - `test -f tools/recon/lib/dismiss-banners.ts && grep -q 'export async function dismissBanners' tools/recon/lib/dismiss-banners.ts` exits 0
    - `grep -q 'count() > 0' tools/recon/lib/dismiss-banners.ts` exits 0
    - `test -f tools/recon/lib/harvest-copy.ts && grep -q 'export async function harvestCopy' tools/recon/lib/harvest-copy.ts` exits 0
    - `grep -q 'ÇĞİÖŞÜçğıöşü' tools/recon/lib/harvest-copy.ts` exits 0 (Turkish charset regex present)
    - `test -f tools/recon/lib/harvest-colors.ts && grep -q 'export async function harvestColors' tools/recon/lib/harvest-colors.ts` exits 0
    - `grep -q 'export function rgbToHex' tools/recon/lib/harvest-colors.ts` exits 0
    - `test -f tools/recon/README.md` exits 0
    - `grep -q 'launchPersistentContext' tools/recon/README.md` exits 0 (anti-bot fallback documented)
    - `grep -q 'no real login' tools/recon/README.md || grep -q 'NOT do' tools/recon/README.md` exits 0
    - `grep -q 'typecheck' tools/recon/README.md` exits 0 (Task-2 verify gate documented)
    - `test -f .planning/intel/screenshots/.gitkeep` exits 0
    - `grep -q 'tools/recon/node_modules/' .gitignore` exits 0
    - `grep -q 'tools/recon/output/' .gitignore` exits 0
    - `grep -c '^# Build outputs$' .gitignore` returns exactly 1 (existing section preserved, not duplicated)
    - `grep -F 'tools/recon' settings.gradle.kts` exits 1 (S-6: NOT in Gradle build)
    - `test ! -f tools/recon/build.gradle.kts` exits 0 (S-6: no Gradle build file)
    - `grep -F 'playwright' gradle/libs.versions.toml` exits 1 (S-6: not in Java version catalog)
    - `cd tools/recon && npx playwright --version` prints `Version 1.59.x` and exits 0
    - `test -d tools/recon/node_modules/@playwright/test` exits 0 (npm install completed)
    - `test -d tools/recon/node_modules/typescript` exits 0 (TS install completed)
    - `cd tools/recon && npx tsc --noEmit` exits 0 (canonical type-check signal — no errors against the helper modules)
  </acceptance_criteria>
  <done>
    Standalone npm project at `tools/recon/` is bootstrapped with Playwright 1.59.x exact-pinned, TypeScript installed and configured (strict tsconfig.json with noEmit, allowImportingTsExtensions, bundler moduleResolution), anti-bot config posture installed (Pattern 2), defensive banner-dismiss helper installed (Pattern 3), TR-text + computed-style harvesters installed (Pattern 1 extracted), README documents the recon contract (run steps + anti-bot fallback + "what it does NOT do" + typecheck gate). Root `.gitignore` has the additive recon block. `.planning/intel/screenshots/` is reserved. `tools/recon/` is NOT integrated into the Gradle build (S-6). `npx playwright --version` reports 1.59.x AND `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the recon assembler + check-recon scripts (with Decision Matrix assertion)</name>
  <files>tools/recon/assemble-recon.ts, tools/recon/check-recon.ts</files>
  <read_first>
    - tools/recon/assemble-recon.ts (the file being modified — will not exist yet)
    - tools/recon/check-recon.ts (the file being modified — will not exist yet)
    - tools/recon/tsconfig.json (created in Task 1; both scripts must satisfy `tsc --noEmit` against this config)
    - tools/recon/lib/harvest-colors.ts (created in Task 1; assemble-recon.ts imports `rgbToHex` from here)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md (Pattern 4 lines 470-501; Recon Report Schema lines 856-1003 — the BINDING contract for the 8 sections, table column orders, and pre-seeded rows; Pitfall #4 lines 572-589 for rgbToHex usage)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md (per-file Shape Contract for assemble-recon.ts and check-recon.ts; Shared Patterns S-1 UTF-8 + S-5 path resolution)
  </read_first>
  <action>
    Create two TypeScript scripts that get invoked by Plan 02 (assembler) and the validation gate (checker). Both run via `tsx` (already a devDep from Task 1) and BOTH must compile cleanly under `tsc --noEmit` against the Task-1 tsconfig.

    **`tools/recon/assemble-recon.ts`** — Markdown assembler. Skeleton lives in 02-RESEARCH.md Pattern 4 (lines 470-501); the §Recon Report Schema (lines 856-1003) is the BINDING output schema.

    Behavior:
    1. Read `tools/recon/output/*-phrases.json` and `tools/recon/output/*-tokens.json` (paths resolved with `path.resolve(__dirname, 'output')`).
    2. Always pass `'utf-8'` to `fs.readFileSync` and `fs.writeFileSync` (PATTERNS.md S-1).
    3. Write a SINGLE file: `.planning/intel/n11-recon.md` (resolved as `path.resolve(__dirname, '../../.planning/intel/n11-recon.md')`).
    4. The 8 sections must appear IN ORDER with EXACT heading text (verbatim from 02-RESEARCH.md schema):
       - `## 1. Page Inventory`
       - `## 2. Turkish Copy Catalog`
       - `## 3. Category Taxonomy`
       - `## 4. Color Token Table`
       - `## 5. Typography Notes`
       - `## 6. Layout Patterns`
       - `## 7. Anti-pattern flags (what we will NOT copy)`
       - `## 8. Open n11 questions for Phase 10 / 11`
    5. Top metadata block (verbatim shape from RESEARCH lines 861-867):
       ```
       # n11.com Recon Report

       **Captured:** YYYY-MM-DD  (today's date in ISO format)
       **Source:** https://www.n11.com — public-page surface (no login, no real cart)
       **Tool:** Playwright @playwright/test 1.59.x
       **Recon project:** `tools/recon/`
       **Consumer:** Phase 10 (Storefront), Phase 11 (Chat Bubble)
       ```
    6. **Page Inventory** table columns (verbatim): `| Page | URL Captured | Screenshot | Element Detail Shots | Capture Date |`. Pre-seed the 7 expected page rows from RESEARCH lines 871-879 (homepage / category / pdp / cart / checkout-step1 / account / login). For PDP, the URL is recon-derived — emit a `<URL captured during recon>` placeholder if the per-page tokens JSON has no `url` field, but if `output/pdp-phrases.json` has `data.url`, write that URL.
    7. **Turkish Copy Catalog** table columns (verbatim): `| # | Phrase (TR) | English Gloss | Source Page | Section / Component |`. Read all `output/<slug>-phrases.json` files; deduplicate phrases case-insensitively (Set keyed by `phrase.toLowerCase()`); enumerate rows starting at `1`. English Gloss column may be `—` for harvested (un-glossed) phrases — DO NOT translate via Claude (Manual-Only verification: gloss is a Phase-10 enrichment, not Phase-2). Source Page = the page slug (e.g., `homepage`, `pdp`). Section / Component column may be `—`. Pre-seed the 30 expected rows from RESEARCH lines 889-918 IF the harvest didn't pick them up; merge harvested + pre-seeded, dedup by lower-case phrase. Output MUST have ≥30 rows in the table body.
    8. **Category Taxonomy** — pre-seed the 8-row table verbatim from RESEARCH lines 928-937 (slug, Turkish label, sub-categories). The sub-categories column is `<recon will confirm>` if no recon JSON contributes (Plan 02 will improve this if homepage spec hovered the mega-menu).
    9. **Color Token Table** — `| Token | Hex | Source page | Computed source (rgb) |` (4 columns; PATTERNS.md per-file assemble-recon.ts point 4 mandates this exact column order). Read `output/<slug>-tokens.json` files. For each token, run the harvested color (e.g., `rgb(247, 130, 50)`) through `rgbToHex(...)` from `lib/harvest-colors.ts` (`import { rgbToHex } from './lib/harvest-colors';`) and write the hex into the `Hex` column; preserve the original `rgb(...)` in the `Computed source (rgb)` column. Token name comes from the `role` field of the harvested TokenRow, prefixed with `--color-` if not already (so `nav-bg` becomes `--color-nav-bg`). Output MUST have ≥10 token rows. If harvesting yielded fewer than 10 distinct tokens, append rows from RESEARCH §Color Token Table lines 946-957 with hex from there and `Computed source (rgb)` = `<placeholder — recon underfilled>`.
    10. **Typography Notes**, **Layout Patterns**, **Anti-pattern flags**, **Open n11 questions** — emit verbatim section bodies from RESEARCH lines 963-998. For Anti-pattern flags, the "No floating chat panel observed" callout is REQUIRED (Pitfall #19 carry-forward). For Typography Notes, populate from `output/homepage-tokens.json` if present (font-family, font-weight, font-size from the harvested `body` and `h1` selectors); else emit the `<recon will confirm>` placeholders.
    11. After write, log to stdout: `Wrote .planning/intel/n11-recon.md with N unique phrases and M color tokens` (substitute counts).
    12. Idempotent — re-running with the same `output/*.json` produces byte-identical Markdown (no timestamps in the body except the frontmatter `**Captured:**` line, which uses `new Date().toISOString().slice(0,10)`).

    **`tools/recon/check-recon.ts`** — sanity checker. Behavior (PATTERNS.md per-file check-recon.ts contract). The intent docstring at the top of the file MUST mention all 9 sections checked (the original 8 + the appended Decision Matrix subsection). Suggested docstring opening: `// check-recon.ts — verifies .planning/intel/n11-recon.md shape across 9 sections (the 8 schema sections written by assemble-recon.ts + the ## Decision Matrix subsection appended by Plan 02-03 Task 1).`

    1. Verify `.planning/intel/n11-recon.md` exists; exit 1 if missing.
    2. Read with `'utf-8'`. Verify all 8 schema section headings present and in order: scan line-by-line, track index of each `## N.` heading, assert ascending order from 1 to 8.
    3. Count distinct phrase rows in §2 (the rows between `## 2.` and `## 3.` headings, where the row matches `^| <num> | ` — count rows excluding header + divider). Assert `≥ 30`. (Mirrors 02-VALIDATION.md line 47 awk check; the equivalent in node is `lines.filter(l => /^\|\s+\d+\s+\|/.test(l)).length`).
    4. Count rows in §4 Color Token Table where the line matches `^\|\s*--[a-z]`. Assert `≥ 10`. (Mirrors 02-VALIDATION.md line 49 awk check.)
    5. Verify all 7 screenshots exist under `.planning/intel/screenshots/`: `homepage-fullpage.png`, `category-elektronik-fullpage.png`, `pdp-fullpage.png`, `cart-fullpage.png`, `checkout-step1-fullpage.png`, `account-fullpage.png`, `login-fullpage.png`. Each must be `>= 50_000` bytes (50 KB — PATTERNS.md per-file screenshots contract Pitfall #1 guard against captured Cloudflare interstitials).
    6. Verify NO `rgb(` substring appears in §4 Color Token Table `Hex` column (column 2 between pipes): every row's hex column must match `^#[0-9A-F]{6}$` after trimming. (Pitfall #4 evidence — every cell got hex-converted.)
    7. **9th assertion — Decision Matrix subsection guard (Issue-3 fix; Phase-10 carry-forward):** verify the `## Decision Matrix` heading is present in the report AND the body of that section (slice from the heading to EOF) contains BOTH the literal string `Vite + React 19 SPA` AND the literal string `65` (the matrix winner score). Plan 02-03 Task 1 appends this subsection; if a future edit accidentally deletes it, the Phase-10 mechanical reader breaks silently. Implementation:
       ```typescript
       const dmIdx = lines.findIndex(l => /^## Decision Matrix/.test(l));
       if (dmIdx === -1) {
         fail('## Decision Matrix subsection missing — Plan 02-03 Task 1 must append it; Phase 10 reads this for the toolchain audit trail');
       } else {
         const dmBody = lines.slice(dmIdx).join('\n');
         if (!dmBody.includes('Vite + React 19 SPA')) {
           fail('## Decision Matrix subsection missing winner label "Vite + React 19 SPA"');
         }
         if (!/\b65\b/.test(dmBody)) {
           fail('## Decision Matrix subsection missing matrix winner score 65');
         }
       }
       ```
    8. On success, print exactly: `RECON OK — <N> phrases, <M> tokens, <K> screenshots, Decision Matrix present` and exit 0.
    9. On any failure, print `FAIL: <description>` for each problem, sum failures, exit with that count (capped at 1).

    Both scripts: read-only on the filesystem (assembler writes one file; checker writes nothing). NO `process.argv` parsing — both are zero-arg invocations. BOTH must satisfy `tsc --noEmit` (annotate types as needed; the strict tsconfig will reject `any` uses).

    Use the Write tool to create both files.
  </action>
  <verify>
    <automated>cd tools/recon && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/recon/assemble-recon.ts` exits 0
    - `test -f tools/recon/check-recon.ts` exits 0
    - `grep -q "## 1. Page Inventory" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 2. Turkish Copy Catalog" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 3. Category Taxonomy" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 4. Color Token Table" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 5. Typography Notes" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 6. Layout Patterns" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 7. Anti-pattern flags" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "## 8. Open n11 questions" tools/recon/assemble-recon.ts` exits 0
    - `grep -q "rgbToHex" tools/recon/assemble-recon.ts` exits 0 (rgb→hex conversion is applied in the assembler — Pitfall #4)
    - `grep -qE "from\\s+'\\./lib/harvest-colors'" tools/recon/assemble-recon.ts` exits 0 (rgbToHex is imported from the harvest-colors module — single source of truth)
    - `grep -q "'utf-8'" tools/recon/assemble-recon.ts` exits 0 (UTF-8 explicit on file I/O — S-1)
    - `grep -q "'utf-8'" tools/recon/check-recon.ts` exits 0
    - `grep -qE 'No floating chat panel|no.{0,3}chat panel' tools/recon/assemble-recon.ts` exits 0 (Pitfall #19 carry-forward — chat-panel anti-pattern flag is hard-coded)
    - `grep -qE '50[_]?000' tools/recon/check-recon.ts` exits 0 (50 KB minimum for screenshot — Pitfall #1 guard)
    - `grep -qE 'homepage-fullpage|category-elektronik-fullpage|pdp-fullpage|cart-fullpage|checkout-step1-fullpage|account-fullpage|login-fullpage' tools/recon/check-recon.ts` exits 0 (all 7 expected screenshots checked)
    - `grep -q "RECON OK" tools/recon/check-recon.ts` exits 0
    - **9th assertion / Issue-3 fix:** `grep -q 'Decision Matrix' tools/recon/check-recon.ts` exits 0
    - `grep -q 'Vite + React 19 SPA' tools/recon/check-recon.ts` exits 0 (winner-label literal present)
    - `grep -qE "['\"\\\\b]65['\"\\\\b]|/\\\\b65\\\\b/" tools/recon/check-recon.ts` exits 0 (matrix winner score 65 asserted)
    - `grep -qE '9 sections|all 9 sections' tools/recon/check-recon.ts` exits 0 (intent docstring acknowledges the 9th assertion — guards against silent deletion)
    - `cd tools/recon && npx tsc --noEmit` exits 0 (Issue-1 fix: real syntax-and-typecheck — both scripts compile cleanly under the strict Task-1 tsconfig)
  </acceptance_criteria>
  <done>
    `tools/recon/assemble-recon.ts` produces a deterministic 8-section Markdown report at `.planning/intel/n11-recon.md` from harvested JSON, applying `rgbToHex` to every color cell and hard-coding the "no chat panel" anti-pattern flag (Pitfall #19). `tools/recon/check-recon.ts` enforces ≥30 phrase rows, ≥10 token rows, all 7 screenshots ≥50KB, zero `rgb(` substrings in the Hex column, AND the new 9th assertion: `## Decision Matrix` heading present + body contains `Vite + React 19 SPA` + `65` (Phase-10 carry-forward guard against silent deletion). The intent docstring acknowledges all 9 sections checked. Both scripts pass UTF-8 explicitly to all fs calls and compile cleanly under `npx tsc --noEmit` against the strict Task-1 tsconfig.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author scripts/check-phase-02-artifacts.sh and confirm bootstrap state</name>
  <files>scripts/check-phase-02-artifacts.sh</files>
  <read_first>
    - scripts/check-phase-02-artifacts.sh (the file being modified — will not exist yet; the parent scripts/ directory does NOT exist either)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-VALIDATION.md (Per-Task Verification Map lines 42-49 — every "shape" automated command in the table is the spec for what this script checks)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md (per-file Shape Contract for scripts/check-phase-02-artifacts.sh — dual-mode, set -euo pipefail, LF endings)
    - .gitattributes (to confirm `*.sh text eol=lf` is enforced — line 7 of root .gitattributes)
  </read_first>
  <action>
    Create the dual-mode artifact lint script at `scripts/check-phase-02-artifacts.sh`. The `scripts/` directory does NOT exist yet — creating it is fine; future phases (per VALIDATION strategy) will reuse.

    Script header:
    ```bash
    #!/usr/bin/env bash
    # Phase 2 artifact lint. Verifies recon scaffold + (in default mode) capture outputs + recon report shape.
    # Usage:
    #   scripts/check-phase-02-artifacts.sh             # default — full Phase 2 verification
    #   scripts/check-phase-02-artifacts.sh --bootstrap # Wave 0 only — scaffold checks only, skip capture/report checks
    set -euo pipefail
    ```

    Mode parsing:
    - `MODE=full` by default; if `$1 == "--bootstrap"`, set `MODE=bootstrap`.
    - Initialize `FAIL_COUNT=0`.
    - Define helper functions:
      - `check_ok() { echo "OK: $1"; }`
      - `check_fail() { echo "FAIL: $1"; FAIL_COUNT=$((FAIL_COUNT + 1)); }`
      - `assert_file_exists() { local f="$1"; local desc="$2"; if [ -f "$f" ]; then check_ok "$desc ($f exists)"; else check_fail "$desc — missing: $f"; fi; }`
      - `assert_file_nonempty() { local f="$1"; local desc="$2"; if [ -s "$f" ]; then check_ok "$desc ($f is non-empty)"; else check_fail "$desc — empty/missing: $f"; fi; }`
      - `assert_grep() { local pattern="$1"; local f="$2"; local desc="$3"; if grep -qE "$pattern" "$f" 2>/dev/null; then check_ok "$desc"; else check_fail "$desc — pattern '$pattern' not found in $f"; fi; }`

    **Bootstrap-mode checks (run in BOTH modes):**
    1. `assert_file_exists tools/recon/package.json "recon package.json scaffolded"`
    2. `assert_grep '"@playwright/test": "1\.59' tools/recon/package.json "Playwright pinned to 1.59.x exact"`
    3. `assert_file_exists tools/recon/tsconfig.json "recon tsconfig.json scaffolded"`
    4. `assert_grep '"noEmit": true' tools/recon/tsconfig.json "tsconfig.json has noEmit:true (canonical type-check gate)"`
    5. `assert_file_exists tools/recon/playwright.config.ts "Playwright config scaffolded"`
    6. `assert_grep 'headless:\\s*false' tools/recon/playwright.config.ts "anti-bot posture: headless false"`
    7. `assert_grep 'disable-blink-features=AutomationControlled' tools/recon/playwright.config.ts "anti-bot posture: AutomationControlled flag"`
    8. `assert_grep 'workers:\\s*1' tools/recon/playwright.config.ts "rate-limit: workers 1"`
    9. `assert_file_exists tools/recon/lib/dismiss-banners.ts "banner-dismiss helper"`
    10. `assert_file_exists tools/recon/lib/harvest-copy.ts "copy-harvest helper"`
    11. `assert_file_exists tools/recon/lib/harvest-colors.ts "color-harvest helper"`
    12. `assert_file_exists tools/recon/assemble-recon.ts "Markdown assembler"`
    13. `assert_file_exists tools/recon/check-recon.ts "report sanity checker"`
    14. `assert_file_exists tools/recon/README.md "recon README"`
    15. `assert_file_exists tools/recon/.gitignore "recon-local .gitignore"`
    16. `assert_grep 'tools/recon/node_modules/' .gitignore "root .gitignore has recon ignore block"`
    17. `assert_file_exists .planning/intel/screenshots/.gitkeep "intel screenshots dir reserved"`
    18. Negative check: `if grep -F 'tools/recon' settings.gradle.kts >/dev/null 2>&1; then check_fail "S-6 violation: tools/recon should NOT appear in settings.gradle.kts"; else check_ok "S-6: tools/recon not registered as Gradle subproject"; fi`
    19. Negative check: `if [ -f tools/recon/build.gradle.kts ]; then check_fail "S-6 violation: tools/recon/build.gradle.kts should not exist"; else check_ok "S-6: no Gradle build file under tools/recon/"; fi`

    **Full-mode-only checks (skip in `--bootstrap`):**
    20. For each slug in `homepage category-elektronik pdp cart checkout-step1 account login`:
        - `if [ -f ".planning/intel/screenshots/${slug}-fullpage.png" ] && [ "$(stat -c%s ".planning/intel/screenshots/${slug}-fullpage.png" 2>/dev/null || echo 0)" -ge 50000 ]; then check_ok "screenshot ≥50KB: ${slug}"; else check_fail "missing or <50KB screenshot: ${slug}"; fi`
    21. `assert_file_exists .planning/intel/n11-recon.md "recon report"`
    22. `assert_grep '^## 1\\. Page Inventory$' .planning/intel/n11-recon.md "report §1 heading"`
    23. `assert_grep '^## 2\\. Turkish Copy Catalog$' .planning/intel/n11-recon.md "report §2 heading"`
    24. `assert_grep '^## 3\\. Category Taxonomy$' .planning/intel/n11-recon.md "report §3 heading"`
    25. `assert_grep '^## 4\\. Color Token Table$' .planning/intel/n11-recon.md "report §4 heading"`
    26. `assert_grep '^## 5\\. Typography Notes$' .planning/intel/n11-recon.md "report §5 heading"`
    27. `assert_grep '^## 6\\. Layout Patterns$' .planning/intel/n11-recon.md "report §6 heading"`
    28. `assert_grep '^## 7\\. Anti-pattern flags' .planning/intel/n11-recon.md "report §7 heading"`
    29. `assert_grep '^## 8\\. Open n11 questions' .planning/intel/n11-recon.md "report §8 heading"`
    30. Phrase count ≥30:
        ```bash
        PHRASE_COUNT=$(awk '/^## 2\. Turkish/,/^## 3\./' .planning/intel/n11-recon.md | grep -cE '^\|\s+[0-9]+\s+\|')
        if [ "$PHRASE_COUNT" -ge 30 ]; then check_ok "Turkish copy catalog ≥30 rows ($PHRASE_COUNT)"; else check_fail "Turkish copy catalog has only $PHRASE_COUNT rows (need ≥30)"; fi
        ```
    31. Token count ≥10:
        ```bash
        TOKEN_COUNT=$(awk '/^## 4\. Color/,/^## 5\./' .planning/intel/n11-recon.md | grep -cE '^\|\s+--[a-z]')
        if [ "$TOKEN_COUNT" -ge 10 ]; then check_ok "Color token table ≥10 rows ($TOKEN_COUNT)"; else check_fail "Color token table has only $TOKEN_COUNT rows (need ≥10)"; fi
        ```
    32. No `rgb(` in §4 Hex column:
        ```bash
        if awk '/^## 4\. Color/,/^## 5\./' .planning/intel/n11-recon.md | grep -qE 'rgb\('; then check_fail "Color Token Table contains rgb() — should be hex only"; else check_ok "Color Token Table is hex-only (Pitfall #4)"; fi
        ```
    33. PROJECT.md Key Decisions row: `assert_grep 'Frontend toolchain' .planning/PROJECT.md "PROJECT.md mentions Frontend toolchain decision"`. (Plan 03 lands the actual row; bootstrap mode skips this check.)
    34. `if grep -A 200 '^## Key Decisions$' .planning/PROJECT.md 2>/dev/null | grep -qE 'Vite|Next\.js'; then check_ok "PROJECT.md Key Decisions mentions Vite or Next.js"; else check_fail "PROJECT.md Key Decisions has no Vite/Next.js entry"; fi`

    Final block:
    ```bash
    if [ "$FAIL_COUNT" -eq 0 ]; then
      echo "ALL CHECKS PASSED (mode=$MODE)"
      exit 0
    else
      echo "FAILED: $FAIL_COUNT check(s) failed (mode=$MODE)"
      exit 1
    fi
    ```

    Make the script executable: `chmod +x scripts/check-phase-02-artifacts.sh`. LF line endings (root `.gitattributes` line 7 enforces `*.sh text eol=lf`).

    After writing, run the script in `--bootstrap` mode to confirm Wave 0 state:
    ```bash
    bash scripts/check-phase-02-artifacts.sh --bootstrap
    ```
    Expect exit 0 and a final line `ALL CHECKS PASSED (mode=bootstrap)`.
  </action>
  <verify>
    <automated>bash scripts/check-phase-02-artifacts.sh --bootstrap</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-phase-02-artifacts.sh` exits 0
    - `test -x scripts/check-phase-02-artifacts.sh` exits 0 (executable bit set)
    - `head -1 scripts/check-phase-02-artifacts.sh | grep -q '#!/usr/bin/env bash'` exits 0
    - `grep -q 'set -euo pipefail' scripts/check-phase-02-artifacts.sh` exits 0
    - `grep -q -- '--bootstrap' scripts/check-phase-02-artifacts.sh` exits 0 (dual-mode supported)
    - `grep -q 'tsconfig.json' scripts/check-phase-02-artifacts.sh` exits 0 (Issue-1 fix: tsconfig.json scaffold check is part of bootstrap)
    - `grep -qE 'homepage-fullpage|category-elektronik-fullpage|pdp-fullpage|cart-fullpage|checkout-step1-fullpage|account-fullpage|login-fullpage' scripts/check-phase-02-artifacts.sh` exits 0 (all 7 expected slugs)
    - `grep -qE '50000|50_000' scripts/check-phase-02-artifacts.sh` exits 0 (Pitfall #1 50KB guard)
    - `grep -q 'Turkish' scripts/check-phase-02-artifacts.sh` exits 0 (≥30 catalog row check)
    - `grep -qE 'rgb\\\\?\\(' scripts/check-phase-02-artifacts.sh || grep -q 'rgb(' scripts/check-phase-02-artifacts.sh` exits 0 (Pitfall #4 hex-only enforcement)
    - `bash scripts/check-phase-02-artifacts.sh --bootstrap` exits 0 and prints `ALL CHECKS PASSED (mode=bootstrap)`
    - `file scripts/check-phase-02-artifacts.sh | grep -qv CRLF` exits 0 (LF line endings — .gitattributes enforced)
  </acceptance_criteria>
  <done>
    `scripts/check-phase-02-artifacts.sh` is a dual-mode bash artifact-lint script. `--bootstrap` mode verifies the recon scaffold (Task 1 + 2 outputs, including the new tsconfig.json gate); default mode adds capture-output checks (7 screenshots ≥50KB, recon report 8 sections, ≥30 phrase rows, ≥10 token rows, no `rgb(` in §4) plus the PROJECT.md Key Decisions row check. The script uses `set -euo pipefail`, has LF line endings, is executable. Running with `--bootstrap` against the post-Plan-01 state exits 0.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `cd tools/recon && npx playwright --version` reports `Version 1.59.x`.
2. `cd tools/recon && npx tsc --noEmit` exits 0 (canonical type-check signal — Issue-1 fix).
3. `bash scripts/check-phase-02-artifacts.sh --bootstrap` exits 0 with `ALL CHECKS PASSED (mode=bootstrap)`.
4. `tools/recon/` is NOT in `settings.gradle.kts`, has no `build.gradle.kts`, has no Playwright entry in `gradle/libs.versions.toml`.
5. Root `.gitignore` has additive recon block (no existing line reordered).
6. `.planning/intel/screenshots/` directory exists (committed via `.gitkeep`).
</verification>

<success_criteria>
- All 11 Wave-0 artifacts from 02-VALIDATION.md "Wave 0 Requirements" exist and pass shape checks (10 originals + tools/recon/tsconfig.json from Issue-1 fix)
- `tools/recon/` is a runnable npm project with Playwright 1.59.x exact-pinned, TypeScript installed, and `tsc --noEmit` exits clean
- Anti-bot posture (Pattern 2) is verbatim in `playwright.config.ts`
- Helper modules (Patterns 1 & 3 extracted) export `harvestCopy`, `harvestColors`, `rgbToHex`, `dismissBanners`
- Assembler script writes the 8-section recon report schema with `rgbToHex` conversion (Pitfall #4 mitigation)
- Checker script enforces ≥30 phrases, ≥10 tokens, all 7 screenshots ≥50KB, no `rgb(` in Hex column, AND the 9th assertion (Decision Matrix subsection winner-label + score 65 — Issue-3 fix)
- Artifact-lint shell script (`scripts/check-phase-02-artifacts.sh`) is dual-mode (`--bootstrap` + default)
- S-6 honored: `tools/recon/` is OUTSIDE the Gradle build (no build.gradle.kts, no settings.gradle.kts include, no entry in libs.versions.toml)
- Pitfall #19 carry-forward: assembler hard-codes the "no floating chat panel observed" callout in §7
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-recon-toolchain-lock/02-01-SUMMARY.md` with:
- Files created (10 in tools/recon/ + 1 tsconfig.json, 1 in scripts/, 1 .gitkeep, 1 .gitignore patch)
- Playwright version actually pinned (1.59.x patch number resolved at install time)
- TypeScript version installed (devDep range from package.json plus actual resolved version from package-lock.json)
- Result of `cd tools/recon && npx tsc --noEmit` (must be exit 0; if any errors, document the fix)
- Any deviations from the planned shape (e.g., if `npx playwright install chromium` required sudo or a specific OS lib; document in deviations)
- Wave 1 hand-off note: `cd tools/recon && npm run capture` is the entry point Plan 02 will use; if anti-bot fallback is needed, the README documents the persistent-context recovery
</output>
