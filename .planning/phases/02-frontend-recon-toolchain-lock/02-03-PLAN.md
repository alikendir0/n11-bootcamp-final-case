---
phase: 02-frontend-recon-toolchain-lock
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - .planning/intel/n11-recon.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [FE-01]
tags: [phase-2, frontend-recon, toolchain-decision, project-md]

must_haves:
  truths:
    - "n11-recon.md §5 (Typography Notes), §6 (Layout Patterns), §7 (Anti-pattern flags), §8 (Open Questions) are filled in based on actual recon evidence — not just assembler placeholders"
    - "PROJECT.md Key Decisions table contains a new row for 'Frontend toolchain' that explicitly mentions Vite 8 + React 19 SPA + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5"
    - "The toolchain rationale paragraph in PROJECT.md references ≥2 specific recon findings (not generic platitudes)"
    - "The toolchain rationale paragraph mentions VITE_API_BASE_URL env-var convention (Pitfall #23 carry-forward to Phase 10)"
    - "The PROJECT.md Key Decisions row uses date-substituted Locked stamp matching `Locked YYYY-MM-DD (Phase 2)` — produced via shell `$(date +%Y-%m-%d)` so the row is correct on whatever day execution actually runs"
    - "PROJECT.md Open Questions: 'Frontend toolchain' bullet is moved from open list to ### Resolved section"
    - "STATE.md is updated: Phase 2 marked Ready for verify; current focus advances to Phase 2 verification or Phase 3"
    - "All 8 sections of n11-recon.md are well-formed Markdown with no broken tables or orphan placeholders"
  artifacts:
    - path: ".planning/intel/n11-recon.md"
      provides: "Final recon report — 8 sections fully populated from recon evidence, ready for Phase 10 mechanical consumption; ## Decision Matrix subsection appended (Phase-10 carry-forward, also gated by check-recon.ts 9th assertion)"
      contains: "VITE_API_BASE_URL"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table has a new 'Frontend toolchain' row; Open Questions list has 'Frontend toolchain' moved to Resolved"
      contains: "Vite 8"
    - path: ".planning/STATE.md"
      provides: "Phase 2 status updated to ready-for-verify; current focus advanced"
      contains: "Phase 02"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions row"
      to: ".planning/intel/n11-recon.md decision matrix"
      via: "rationale column references the recon evidence (Decision Matrix in §10 of n11-recon.md)"
      pattern: "n11-recon\\.md"
    - from: ".planning/PROJECT.md Key Decisions row"
      to: "VITE_API_BASE_URL env-var convention"
      via: "rationale paragraph mentions the env var name verbatim — Phase 10 reads this as a frontend-config requirement"
      pattern: "VITE_API_BASE_URL"
    - from: ".planning/intel/n11-recon.md §7"
      to: "Phase 11 floating chat bubble"
      via: "no-chat-panel-observed callout (Pitfall #19 prevention)"
      pattern: "no.{0,5}chat panel|chat panel.{0,5}observed"
---

<objective>
Finalize Phase 2 by (a) enriching the assembled `.planning/intel/n11-recon.md` with recon-evidence-grounded content for the qualitative sections (Typography Notes, Layout Patterns, Anti-pattern Flags, Open Questions, plus a paste-ready Decision Matrix subsection), (b) writing the toolchain decision into `.planning/PROJECT.md` Key Decisions and resolving the matching Open Question, and (c) marking Phase 2 ready-for-verify in STATE.md. This plan turns the recon evidence into a phase-closing decision record.

Purpose: The toolchain lock-in IS the deliverable for FE-01 (per ROADMAP success criterion 3 and CONTEXT note in the planning brief). The decision must be auditable — graders weighing code quality + SOLID will read PROJECT.md to understand why Vite over Next.js. The rationale must cite specific recon findings (not generic "Vite is faster") and must call out `VITE_API_BASE_URL` env-var convention so Phase 10 doesn't reinvent the wheel.

Output: 3 file edits — `.planning/intel/n11-recon.md` (enrichment + Decision Matrix subsection), `.planning/PROJECT.md` (Key Decisions append + Open Questions move), `.planning/STATE.md` (status touch). After this plan, Phase 2's success criteria from ROADMAP are all met and `bash scripts/check-phase-02-artifacts.sh` (full mode) exits 0.

**Date discipline (Issue-2 fix):** Every dated stamp written by this plan uses shell substitution at execution time — `$(date +%Y-%m-%d)` for date-only stamps and `$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")` for ISO timestamps — so the artifacts are correct on whatever day the executor actually runs them. NO literal `2026-04-29` strings inside any `<action>` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-VALIDATION.md
@.planning/phases/02-frontend-recon-toolchain-lock/02-02-SUMMARY.md
@.planning/intel/n11-recon.md
@CLAUDE.md

<interfaces>
<!-- Source-of-truth content for the toolchain decision row + recon enrichment. -->

From 02-RESEARCH.md "Decision Matrix" (lines 824-852) — paste-ready scoring matrix and rationale paragraph. The rationale paragraph in lines 850-852 is the binding language for the PROJECT.md Key Decisions row.

PROJECT.md Key Decisions row format (matching the existing 16-row table — see `.planning/PROJECT.md` lines 109-125; the existing rows use the format `| Decision | Rationale | Outcome |` with `Locked YYYY-MM-DD` outcome convention; the YYYY-MM-DD comes from `$(date +%Y-%m-%d)` substituted at execution time — Issue-2 fix):

```markdown
| Frontend toolchain: Vite 8 + React 19 SPA + TypeScript strict + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5 + react-hook-form + zod | Locked post-Phase-2 n11 recon. Three pieces of recon evidence: (1) <recon finding A from n11-recon.md>; (2) <recon finding B from n11-recon.md>; (3) <recon finding C — likely the no-chat-panel observation>. JWT-at-gateway means SSR-side auth would only add token-forwarding gymnastics for zero benefit; AI chat panel SSE consumption (Phase 11) maps cleanly to native EventSource in a Vite SPA. API base URL injected via `VITE_API_BASE_URL` env var — no hardcoded URLs in source (Pitfall #23 prevention). Decision matrix score: Vite 65 / Next 45 (see .planning/intel/n11-recon.md §Decision Matrix). | Locked $(date +%Y-%m-%d) (Phase 2) |
```

PROJECT.md Open Questions move (lines 127-133 has "Frontend toolchain" bullet currently in OPEN list; it should move to ### Resolved at line 135+):

Existing open question (lines 133-134):
```markdown
- **Frontend toolchain** — Vite + TypeScript + Tailwind + Zustand (likely) vs Next.js (only if n11 dynamic enough to need SSR) vs plain Vite + JS? *Decide post Playwright n11 recon.* Affects: Phase: Frontend Foundation.
```

Move to Resolved (matches format on line 137; date is `$(date +%Y-%m-%d)` substituted at execution time):
```markdown
- **Frontend toolchain** — RESOLVED $(date +%Y-%m-%d) (Phase 2): Vite 8 + React 19 SPA + TypeScript strict + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5. See Key Decisions row and `.planning/intel/n11-recon.md` Decision Matrix subsection for the audit trail.
```

STATE.md update — `stopped_at`, `last_updated`, `last_activity`, Current Position section:

The relevant lines in STATE.md (from earlier read — see lines 6-7 and 27-31):
```
stopped_at: "Phase 1 complete — all 8 plans across 4 waves landed. Foundations + Day-1 Contracts done. Ready for Phase 2 (Frontend Recon + Toolchain Lock)."
last_updated: "2026-04-28T23:20:00.000Z"
last_activity: 2026-04-28 -- Plan 01-08 complete + Phase 1 closed
```
Current Position section:
```
Phase: 01 (foundations-day-1-contracts) — ✓ COMPLETE (8/8 plans)
Next: Phase 02 — Frontend Recon + Toolchain Lock (Playwright session against n11.com + toolchain decision in PROJECT.md)
Status: Phase 1 closed
```

Update target (dates substituted at execution — Issue-2 fix):
```
stopped_at: "Phase 2 complete — all 3 plans landed. Recon evidence captured (.planning/intel/), toolchain locked to Vite 8 + React 19 SPA in PROJECT.md. Ready for Phase 2 verify, then Phase 3 (Identity + Gateway Auth)."
last_updated: "$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")"
last_activity: $(date +%Y-%m-%d) -- Plan 02-03 complete + Phase 2 closed; toolchain locked, recon report 8 sections committed
```
Current Position:
```
Phase: 02 (frontend-recon-toolchain-lock) — ✓ COMPLETE (3/3 plans) — Ready for verify
Next: /gsd-verify-work for Phase 2, then Phase 03 — Identity + Gateway Auth
Status: Phase 2 closed
Last activity: $(date +%Y-%m-%d) -- Plan 02-03 complete; toolchain locked (Vite 8 + React 19 SPA); recon report committed
```

Progress section: bump `completed_phases: 2`, `total_plans: 11`, `completed_plans: 11`, `percent: 18`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enrich n11-recon.md qualitative sections + append Decision Matrix subsection</name>
  <files>.planning/intel/n11-recon.md</files>
  <read_first>
    - .planning/intel/n11-recon.md (the file being modified — assembled by Plan 02 Task 2)
    - .planning/intel/screenshots/homepage-fullpage.png (visual reference for Layout Patterns section)
    - .planning/intel/screenshots/pdp-fullpage.png (visual reference for PDP layout observations)
    - .planning/intel/screenshots/cart-fullpage.png (visual reference for cart layout observations)
    - tools/recon/output/homepage-tokens.json (Typography Notes data: font-family, font-size from harvested computed-style)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md (§Recon Report Schema lines 856-1003 — section bodies are paste-ready; Decision Matrix lines 824-852 — to be added as a NEW §10 Decision Matrix subsection)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-02-SUMMARY.md (hand-off notes from Plan 02 — which sections need enrichment vs which are already complete)
  </read_first>
  <action>
    Edit `.planning/intel/n11-recon.md` in place. The assembler (Plan 01 Task 2) wrote the structural skeleton with all 8 section headings; this task fills in the qualitative content for §5-§8 from recon evidence, and APPENDS a new §10 Decision Matrix subsection (the existing §1-§8 schema is the Phase-10 contract, so we don't renumber; the Decision Matrix lives below §8 as supplementary content with a `## Decision Matrix` heading and an opening `> This subsection is referenced by .planning/PROJECT.md Key Decisions row for FE-01.` blockquote). Plan 02-01 Task 2's `check-recon.ts` 9th assertion verifies this subsection exists with the literal `Vite + React 19 SPA` and `65` score — see Plan 02-01 Issue-3 fix.

    Read the current `.planning/intel/n11-recon.md` first to see what the assembler produced, then enrich with the Edit tool (NOT rewrite-from-scratch).

    **§5 Typography Notes** — replace the assembler's `<recon will confirm>` placeholders with actual values from `tools/recon/output/homepage-tokens.json`. Specifically:
    - `Font family (body)` — the `fontFamily` field from the body's TokenRow (likely something like `"Open Sans", system-ui, sans-serif` or `"Helvetica Neue", Arial, sans-serif`).
    - `Font family (heading)` — the `fontFamily` field from the h1 TokenRow.
    - `Heading scale` — the `fontSize` field from h1 TokenRow.
    - `Body size` — the `fontSize` field from body TokenRow.
    - `Font weights observed` — list of distinct `fontWeight` values across all harvested tokens (e.g., `400 (body), 500 (subhead), 700 (heading, price)`).
    - `Line height (body)` — if a `lineHeight` field is present in tokens (it isn't, by current TokenRow type — leave the placeholder `<recon shows: standard 1.4-1.6 — see screenshot>` if so).

    If the harvested values are missing or the JSON file is empty, leave the assembler's placeholder text in place but ADD a sub-bullet: `> Recon underfilled — Phase 10 to read directly from screenshots.`

    **§6 Layout Patterns** — the assembler emitted RESEARCH lines 976-981 verbatim. Look at `homepage-fullpage.png`, `pdp-fullpage.png`, `cart-fullpage.png` and either confirm or correct the pre-seeded observations. Specifically:
    - Header sticky? — look at screenshot top.
    - Listing grid columns at desktop — count from `category-elektronik-fullpage.png` (default 1440 viewport).
    - PDP layout — confirm left=gallery / right=info or note the actual split.
    - Cart layout — confirm line-items-left + summary-right or note actual.

    If observations match the pre-seeded text, leave it. If they differ, EDIT the bullet to match what the screenshot shows. Add a closing bullet: `- **Captured at viewport 1440×900** — recon used a fixed desktop viewport. Phase 10 sets responsive breakpoints based on Tailwind 4 defaults.`

    **§7 Anti-pattern flags** — the assembler hard-coded the no-chat-panel callout (Pitfall #19 carry-forward — Plan 01 Task 2 acceptance criterion). Confirm it's present. ADD any anti-patterns observed in the screenshots:
    - If a countdown timer / "X kişi şu anda görüntülüyor" pattern appears in `homepage-fullpage.png` or `pdp-fullpage.png`, add: `- **Dark-pattern observed in <page>:** <description>. Will NOT be replicated.`
    - If autoplay video on PDP appears, add a row.
    - If a newsletter pop-up dominates a screenshot, add a row.

    The "no floating chat panel" callout MUST remain — it's the Pitfall #19 carry-forward to Phase 11.

    **§8 Open n11 questions for Phase 10 / 11** — the assembler emits 4 placeholder questions (RESEARCH lines 991-996). Either keep or refine each based on screenshot evidence:
    - Question 1 (Son N ürün! stock indicator) — keep unless `pdp-fullpage.png` clearly shows or excludes it.
    - Question 2 (Kapıda Ödeme visibility) — keep unless `checkout-step1-fullpage.png` shows the answer.
    - Question 3 (breadcrumbs on PDP) — keep unless `pdp-fullpage.png` shows the answer.
    - Question 4 (Çok Satanlar rail item count) — keep unless `homepage-fullpage.png` shows the answer.

    ADD any NEW open questions surfaced by the recon. Examples:
    - If checkout redirected to login, add: `- Does n11 require login before showing the address-step form? (Drives FE-09 multi-step flow.)`
    - If category pages show product counts, add: `- What does n11 display when a category has zero products? (Drives PROD-04 empty-state copy.)`

    **APPEND new §10 Decision Matrix** subsection (NOT renumbering — appears AFTER §8). Format:

    ```markdown
    ## Decision Matrix — Frontend Toolchain (Vite SPA vs Next.js 15)

    > Referenced by `.planning/PROJECT.md` Key Decisions row for FE-01. Scoring is 1–5 (5 = best). Weights are bootcamp-grading-lens-tuned: code-quality ×3, timeline ×2, recon-evidence ×1, JWT compat ×2, SSE compat ×2, pitfall avoidance ×2, brief literal ×1.

    | Criterion | Weight | Vite + React 19 SPA | Next.js 15 (App Router) |
    |-----------|--------|---------------------|-------------------------|
    | Code-quality signal (clean layering, hooks-first) | ×3 | 5 | 4 |
    | 6-day timeline fit | ×2 | 5 | 3 |
    | n11-recon-evidence support (does recon show SSR is essential?) | ×1 | 5 | 2 |
    | JWT-at-gateway compatibility | ×2 | 5 | 3 |
    | AI chat panel SSE consumption | ×2 | 5 | 3 |
    | Avoids Pitfall #16/#19/#23 | ×2 | 5 | 4 |
    | Bootcamp brief literal: "React.js storefront" | ×1 | 5 | 5 |

    **Weighted totals:**
    - **Vite + React 19 SPA: 5×3 + 5×2 + 5×1 + 5×2 + 5×2 + 5×2 + 5×1 = 15+10+5+10+10+10+5 = 65**
    - Next.js 15: 4×3 + 3×2 + 2×1 + 3×2 + 3×2 + 4×2 + 5×1 = 12+6+2+6+6+8+5 = 45

    **Decision:** Vite + React 19 SPA + TypeScript + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5 + react-hook-form + zod.

    **Recon evidence supporting the decision:**
    1. **n11 has no in-storefront chat panel** (see §7 Anti-pattern flags) — Phase 11's floating-bubble UX is greenfield. Vite SPA owns the DOM cleanly without RSC re-render gymnastics.
    2. **n11 PDP is fully client-rendered after initial HTML** (observed in `pdp-fullpage.png` — the listing grid + PDP layout do not require SSR-only data; our deliverable is a graded interview demo, not a public-search-engine-indexed marketplace).
    3. **JWT validated only at the gateway** (locked Phase 1, see CLAUDE.md and PROJECT.md) — SSR-side auth would force token-forwarding through the Node runtime. SPA dispatches `Authorization: Bearer <token>` directly from the browser through the api-gateway → service mesh.

    **Carry-forward to Phase 10:** API base URL injected via `VITE_API_BASE_URL` env var (no hardcoded `http://localhost:8080` in source — Pitfall #23 prevention). Frontend toolchain install command will be `npm create vite@latest frontend -- --template react-ts` followed by `npm install tailwindcss@4.x @tailwindcss/vite zustand react-router @tanstack/react-query react-hook-form zod @hookform/resolvers`.

    **Carry-forward to Phase 11:** Floating chat bubble = greenfield UX. Reference inspiration in `## 7. Anti-pattern flags`. SSE token streaming consumes via native `EventSource` — no RSC streaming needed.
    ```

    Use the Edit tool for in-place changes; do NOT rewrite the entire file. Preserve every existing line that isn't being explicitly changed (assembler-emitted §1-§4 tables, §2 catalog rows, §4 token rows — all stay as-is).

    UTF-8 hygiene: when you Edit through the tool, the encoding is preserved. Do not pipe through `sed` or `awk` (they may break Turkish characters depending on locale).

    Spot-check Turkish characters survive after edit: `grep -c 'Önceki\|Sepete\|Stokta\|Tükendi' .planning/intel/n11-recon.md` should still return ≥ 3.
  </action>
  <verify>
    <automated>grep -c '^## ' .planning/intel/n11-recon.md && awk '/^## Decision Matrix/,EOF' .planning/intel/n11-recon.md | grep -cE 'Vite \+ React 19 SPA: 5×3|Vite \+ React 19 SPA.*65'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/n11-recon.md` is non-empty and well-formed
    - All 8 original sections still present and in order: `awk '/^## [0-9]\. /' .planning/intel/n11-recon.md | head -8 | grep -cE '^## [1-8]\.'` returns 8
    - New `## Decision Matrix` heading present: `grep -q '^## Decision Matrix' .planning/intel/n11-recon.md` exits 0
    - Decision Matrix shows Vite winning 65 to Next 45: `grep -qE 'Vite \+ React 19 SPA.*65|: 65$' .planning/intel/n11-recon.md` exits 0 AND `grep -qE 'Next\.js 15.*45|: 45$' .planning/intel/n11-recon.md` exits 0
    - Decision Matrix section references VITE_API_BASE_URL: `awk '/^## Decision Matrix/,EOF' .planning/intel/n11-recon.md | grep -q 'VITE_API_BASE_URL'` exits 0
    - §7 still has the no-chat-panel callout: `awk '/^## 7\./,/^## 8\./' .planning/intel/n11-recon.md | grep -qiE 'no.{0,5}chat panel|chat panel.{0,5}observed'` exits 0
    - Phrase count in §2 still ≥ 30 (Plan 02 wrote them; Plan 03 must NOT delete): `awk '/^## 2\. Turkish/,/^## 3\./' .planning/intel/n11-recon.md | grep -cE '^\|\s+[0-9]+\s+\|'` returns ≥ 30
    - Token count in §4 still ≥ 10: `awk '/^## 4\. Color/,/^## 5\./' .planning/intel/n11-recon.md | grep -cE '^\|\s+--[a-z]'` returns ≥ 10
    - No `rgb(` in §4 (preserved from assembler): `awk '/^## 4\. Color/,/^## 5\./' .planning/intel/n11-recon.md | grep -c 'rgb('` returns 0
    - Turkish characters intact: `grep -cE 'Önceki|Sepete|Stokta|Tükendi|Kargo' .planning/intel/n11-recon.md` returns ≥ 3
    - §5 Typography Notes no longer contains every placeholder `<recon will confirm>` — at least the body font-family OR body font-size has a real value: `awk '/^## 5\. Typography/,/^## 6\./' .planning/intel/n11-recon.md | grep -cv 'recon will confirm' | head -1` returns ≥ 5 (header + table rows + at least one filled value)
    - The `cd tools/recon && npm run check` command exits 0 (the checker enforces 8 schema sections + the 9th-assertion Decision Matrix; this confirms the enrichment landed AND the previously-missing 9th-section gap is closed)
  </acceptance_criteria>
  <done>
    `.planning/intel/n11-recon.md` is the complete recon deliverable for Phase 2: 8 schema sections in order, Typography/Layout/Anti-pattern/Open-Questions sections enriched from actual recon evidence (not just assembler placeholders), and a new §Decision Matrix subsection that scores Vite 65 vs Next 45 with three recon-evidence-grounded rationale bullets and the `VITE_API_BASE_URL` carry-forward note. Turkish UTF-8 intact. The Plan 02 assembler outputs (§1-§4 tables) are preserved unchanged. The check-recon.ts 9th assertion (Plan 02-01 Issue-3 fix) now passes against the appended subsection.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append toolchain Key Decisions row + resolve Open Question in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the file being modified — Key Decisions table at lines ~109-125; Open Questions at lines ~127-137)
    - .planning/intel/n11-recon.md §Decision Matrix (Task 1's output — referenced by the new Key Decisions row)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-RESEARCH.md (§Decision Matrix lines 824-852 for the rationale paragraph wording)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-PATTERNS.md (per-file PROJECT.md Key Decisions contract — row format, "Locked YYYY-MM-DD" outcome convention, must reference ≥2 specific recon findings)
  </read_first>
  <action>
    Two edits in `.planning/PROJECT.md`. Use the Edit tool for both — never rewrite the entire file.

    **Date discipline (Issue-2 fix):** before composing the inserted strings, capture today's date into a shell variable via `TODAY=$(date +%Y-%m-%d)` and substitute it everywhere the row says "Locked …" or the Resolved bullet says "RESOLVED …". The PLAN MUST NOT contain a literal `2026-04-29` (or any other hard-coded YYYY-MM-DD) inside an Edit operand — the date is whatever the executor's clock shows on the day execution runs.

    **Edit 1: Append a new row to Key Decisions table.**

    Find the LAST data row of the Key Decisions table (the row containing "Deploy target = local docker-compose on the candidate's machine" — currently the last row, ending with `Locked 2026-04-28 (revised from EB+RDS) |`). The new row goes immediately after it, BEFORE the empty line that follows the table.

    The new row format (PATTERNS.md S-PROJECT.md per-file contract — three columns separated by `|`, ending with the row-end `|`, "Locked YYYY-MM-DD" outcome convention; the YYYY-MM-DD is `$(date +%Y-%m-%d)` at execution time):

    ```markdown
    | Frontend toolchain: Vite 8 + React 19 SPA + TypeScript strict (`noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`) + Tailwind 4 (`@theme` directive, no config file) + Zustand 5 + React Router 7 + TanStack Query 5 + react-hook-form 7 + zod 4 | Locked post-Phase-2 n11 recon ($(date +%Y-%m-%d)). Three pieces of recon evidence: (1) **n11 has no in-storefront chat panel** (see `.planning/intel/n11-recon.md` §7) — Phase 11's floating-bubble UX is greenfield; Vite SPA owns the DOM cleanly without RSC re-render gymnastics. (2) **n11 PDP is fully client-rendered after initial HTML** (observed in `.planning/intel/screenshots/pdp-fullpage.png`) — no SSR-only data we'd lose by going SPA; our deliverable is a graded interview demo, not a public-search-indexed marketplace. (3) **JWT validated only at the gateway** (locked Phase 1) — SSR-side auth would force token-forwarding gymnastics through a Node runtime for zero benefit. AI chat panel SSE consumption (Phase 11) maps cleanly to native `EventSource` in a Vite SPA. **Carry-forward to Phase 10:** API base URL injected via `VITE_API_BASE_URL` env var — no hardcoded URLs in source (Pitfall #23 prevention). **Decision matrix score:** Vite 65 / Next 45 (see `.planning/intel/n11-recon.md` §Decision Matrix for full breakdown — weights: code-quality ×3, timeline ×2, recon-evidence ×1, JWT compat ×2, SSE compat ×2, pitfall avoidance ×2, brief literal ×1). | Locked $(date +%Y-%m-%d) (Phase 2 — see `.planning/intel/n11-recon.md` Decision Matrix) |
    ```

    The rationale column is one long paragraph (no internal newlines — Markdown table cells must be a single line). The cell uses inline backticks for env var name + file paths.

    Important: the executor must compute `$(date +%Y-%m-%d)` ONCE up front (e.g., `TODAY=$(date +%Y-%m-%d)`) and substitute the captured value into the Edit tool's `new_string` argument before invoking Edit — the literal string `$(date +%Y-%m-%d)` must NOT appear in the final PROJECT.md file. After Edit completes, verify with `grep -E 'Locked [0-9]{4}-[0-9]{2}-[0-9]{2} \(Phase 2\)' .planning/PROJECT.md` (must match — date-agnostic regex per Issue-2).

    **Edit 2: Move "Frontend toolchain" from Open Questions to Resolved.**

    Find the existing Open Questions bullet at PROJECT.md (around lines 127-133) — the bullet that begins with `- **Frontend toolchain** — Vite + TypeScript + Tailwind + Zustand (likely) vs Next.js`. DELETE this bullet from the open list.

    Find the `### Resolved` heading (currently around line 135) and APPEND a new bullet immediately after the existing AWS resolved bullet (line 137) that follows the format of the existing line. Use the captured `$TODAY` variable from Edit 1:

    ```markdown
    - **Frontend toolchain** — RESOLVED $(date +%Y-%m-%d) (Phase 2): Vite 8 + React 19 SPA + TypeScript strict + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5 + react-hook-form + zod. Decision matrix score Vite 65 / Next 45. See Key Decisions row above and `.planning/intel/n11-recon.md` § Decision Matrix for the audit trail. Recon evidence: no in-storefront chat panel observed (Phase 11 invents); n11 PDP fully client-rendered (no SSR-only data); JWT-at-gateway makes SSR-side auth dead weight. API base URL via `VITE_API_BASE_URL` env var (Pitfall #23 prevention).
    ```

    Same date-substitution discipline as Edit 1: replace `$(date +%Y-%m-%d)` with the captured `$TODAY` value before passing to Edit.

    Use the Edit tool with appropriate `old_string` / `new_string` to:
    1. Insert the Key Decisions row (replace the trailing `\n` after the last existing row + the row-after-table blank line).
    2. Remove the Frontend toolchain bullet from Open Questions.
    3. Append the bullet to the Resolved section.

    Do NOT touch other rows in the Key Decisions table. Do NOT delete other Open Questions (Public tunnel choice and MCP server tunnel exposure remain — they resolve in Phase 6/9).
  </action>
  <verify>
    <automated>grep -c 'Frontend toolchain' .planning/PROJECT.md && grep -A 200 '## Key Decisions' .planning/PROJECT.md | grep -cE 'Vite 8|Vite \+ React 19'</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md mentions `Frontend toolchain` exactly twice: `grep -c 'Frontend toolchain' .planning/PROJECT.md` returns 2 (one in Key Decisions, one in Resolved)
    - Key Decisions table has Vite reference: `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -qE 'Vite\s*8|Vite \+ React 19'` exits 0
    - Key Decisions table has Tailwind 4 reference: `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -q 'Tailwind 4'` exits 0
    - Key Decisions table mentions VITE_API_BASE_URL: `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -q 'VITE_API_BASE_URL'` exits 0
    - Key Decisions table cites recon evidence (≥2 specific findings, mentions n11-recon.md or specific screenshot): `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -cE 'n11-recon\.md|chat panel|client-rendered|JWT|gateway'` returns ≥ 3
    - Key Decisions row has rationale length > 100 chars (per planning brief quality gate): `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -E '^\| Frontend toolchain' | awk -F'\\|' '{print length($3)}'` returns ≥ 100
    - **Issue-2 fix — date-agnostic Locked stamp present:** `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -qE 'Locked [0-9]{4}-[0-9]{2}-[0-9]{2}'` exits 0 (matches whatever YYYY-MM-DD the executor wrote on its run-day; replaces the prior date-coupled `Locked 2026-04-29` check).
    - **Issue-2 fix — Phase 2 stamp specifically present:** `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -qE 'Locked [0-9]{4}-[0-9]{2}-[0-9]{2} \(Phase 2'` exits 0 (the Frontend toolchain row's outcome cell has Phase-2 attribution alongside the date).
    - **Issue-2 fix — no unsubstituted shell expression leaked into the file:** `grep -F '$(date +%Y-%m-%d)' .planning/PROJECT.md` exits 1 (the literal shell expression must NOT appear in the final file — it should have been substituted before the Edit call).
    - Open Questions list does NOT contain "Frontend toolchain" outside of the Resolved subsection: `awk '/^## Open Questions$/,/^### Resolved$/' .planning/PROJECT.md | grep -c 'Frontend toolchain'` returns 0
    - Resolved section contains the Frontend toolchain entry: `awk '/^### Resolved$/,EOF' .planning/PROJECT.md | grep -q 'Frontend toolchain'` exits 0
    - Resolved entry mentions RESOLVED + Phase 2 + a date in YYYY-MM-DD format (Issue-2: date-agnostic): `awk '/^### Resolved$/,EOF' .planning/PROJECT.md | grep -E 'Frontend toolchain' | grep -qE 'RESOLVED [0-9]{4}-[0-9]{2}-[0-9]{2}.*Phase 2|Phase 2.*RESOLVED [0-9]{4}-[0-9]{2}-[0-9]{2}'` exits 0
    - Other Open Questions preserved (Public tunnel, MCP server): `awk '/^## Open Questions$/,/^### Resolved$/' .planning/PROJECT.md | grep -cE 'tunnel|MCP'` returns ≥ 2
    - Existing Key Decisions rows preserved (not lost): `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -c '^|.*|.*|.*|$'` returns ≥ 18 (header + divider + 16 existing + 1 new = 19 expected, ≥18 = lenient)
    - Document is well-formed Markdown (no broken table): `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -cE '^\|--' | head -1` returns 1 (exactly one divider row)
    - `bash scripts/check-phase-02-artifacts.sh` exits 0 (the previously-failing PROJECT.md Key Decisions check now passes — closing the single Plan-02 gap pinned by Plan-02 Task 2 Issue-4 acceptance)
  </acceptance_criteria>
  <done>
    `.planning/PROJECT.md` Key Decisions table has a new row for "Frontend toolchain" naming Vite 8 + React 19 SPA + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5 + react-hook-form + zod, with rationale citing ≥3 specific recon findings (no chat panel, client-rendered PDP, JWT-at-gateway), VITE_API_BASE_URL env-var convention (Pitfall #23 prevention), and Vite 65 / Next 45 matrix score. The Frontend toolchain Open Question is moved from the open list to ### Resolved with the executor's run-day RESOLVED date (Issue-2: date-agnostic via `$(date +%Y-%m-%d)`; the literal `$(date +%Y-%m-%d)` does NOT survive into the file). All other Key Decisions rows and Open Questions are preserved unchanged. `bash scripts/check-phase-02-artifacts.sh` (full mode) now exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md to mark Phase 2 ready-for-verify</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the file being modified — current state captured in `<interfaces>` block above; specifically the `stopped_at`, `last_updated`, `last_activity` fields and Current Position + Progress sections)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-01-SUMMARY.md (Plan 01 close — created by Plan 01 Task 3; gives the actual Playwright version pinned)
    - .planning/phases/02-frontend-recon-toolchain-lock/02-02-SUMMARY.md (Plan 02 close — created by Plan 02 Task 3; gives the actual phrase / token counts and any anti-bot fallback notes)
  </read_first>
  <action>
    Edit `.planning/STATE.md` in place to reflect Phase 2 completion. Use the Edit tool. Match the existing voice and verbosity of `stopped_at` field (the current Phase 1 close uses a single dense line with phase summary + readiness statement).

    **Date discipline (Issue-2 fix — same pattern as Task 2):** capture run-day date via `TODAY=$(date +%Y-%m-%d)` and capture the ISO timestamp via `NOW=$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")` ONCE up front, then substitute into the `new_string` arguments to Edit. The literal `$(date +%Y-%m-%d)` and `$(date -u …)` strings must NOT survive into the final STATE.md file.

    **Edits (in order):**

    1. **`stopped_at` field** (line 6) — replace with:
       ```
       stopped_at: "Phase 2 complete — all 3 plans (Wave 0/1/2) landed. Recon evidence captured: 7 screenshots in .planning/intel/screenshots/, 30+ Turkish phrases + 10+ color tokens harvested into .planning/intel/n11-recon.md (8 sections in order). Toolchain locked to Vite 8 + React 19 SPA + TypeScript strict + Tailwind 4 + Zustand 5 + React Router 7 + TanStack Query 5 in PROJECT.md Key Decisions (Vite 65 / Next 45 matrix). Ready for Phase 2 verify, then Phase 3 (Identity + Gateway Auth)."
       ```

    2. **`last_updated` field** (line 7) — set to today's ISO date with current UTC time. Compute via:
       ```bash
       NOW=$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")
       ```
       Use the captured `$NOW` value as the value (quoted). Do NOT write the literal shell expression.

    3. **`last_activity` field** (line 8) — replace with (substituting captured `$TODAY`):
       ```
       last_activity: $(date +%Y-%m-%d) -- Plan 02-03 complete + Phase 2 closed (recon report committed; toolchain locked; PROJECT.md Key Decisions row added with Pitfall #23 carry-forward note)
       ```

    4. **`progress` block** (lines 9-14) — update:
       ```yaml
       progress:
         total_phases: 11
         completed_phases: 2
         total_plans: 11
         completed_plans: 11
         percent: 18
       ```
       (Note: Phase 1 had 8 plans, Phase 2 has 3 plans, total 11 plans completed.)

    5. **Current Position section** (lines 27-31) — replace with (substituting captured `$TODAY`):
       ```markdown
       ## Current Position

       Phase: 02 (frontend-recon-toolchain-lock) — ✓ COMPLETE (3/3 plans) — Ready for verify
       Next: /gsd-verify-work for Phase 2, then Phase 03 — Identity + Gateway Auth
       Status: Phase 2 closed
       Last activity: $(date +%Y-%m-%d) -- Plan 02-03 complete; toolchain locked (Vite 8 + React 19 SPA); recon report committed (.planning/intel/n11-recon.md, 8 sections)

       Progress: [██░░░░░░░░] 18% (2 of 11 phases complete)
       ```

    6. **Performance Metrics section** (lines 36-52) — APPEND a new row to the "By Phase" table (after the Phase 01 row) noting Phase 02 plan count and durations from `02-0X-SUMMARY.md` files. If actual durations are not available in the SUMMARYs, use placeholder `~TBD min`. Also append a brief "Phase 02 narrative" paragraph to the "Recent Trend" section noting whether anti-bot fallback was needed and the final phrase/token counts.

    7. **Session Continuity section** (lines 104-108) — replace with (substituting captured `$TODAY`):
       ```markdown
       ## Session Continuity

       Last session: $(date +%Y-%m-%d) (execute-phase 2, plan 02-03)
       Stopped at: Plan 02-03 complete; Phase 2 closed. .planning/intel/n11-recon.md is the Phase 10/11 hand-off contract — 8 sections + Decision Matrix subsection. PROJECT.md Key Decisions has the toolchain row (Vite 8 + React 19 SPA, locked $(date +%Y-%m-%d)). Open Questions: "Frontend toolchain" moved to Resolved; "Public tunnel choice" and "MCP server tunnel exposure" remain open (Phase 6/9).
       Resume file: (next dispatch) /gsd-verify-work for Phase 2 -- if green, /gsd-plan-phase 3 to start Phase 3 (Identity + Gateway Auth).
       Next: /gsd-verify-work for Phase 2
       ```

    Do NOT touch the Decisions block (`### Decisions`) — those are accumulated audit-trail entries owned by individual plan summaries; Phase 2 plan summaries (02-01, 02-02, 02-03) will write their own decision entries when the orchestrator processes plan close.

    Do NOT touch the Pending Todos block — empty by design; Phase 2 didn't surface any new todos.

    Do NOT touch the Blockers/Concerns block — the existing AWS/Gemini/Iyzico entries are still relevant.

    Do NOT touch the Deferred Items section — IDE-generated `*/bin/main/` deferred item still applies.

    UTF-8 hygiene: same as Task 1 — Edit tool preserves encoding.
  </action>
  <verify>
    <automated>grep -E 'completed_phases:\s*2|completed_plans:\s*11|percent:\s*18' .planning/STATE.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md is well-formed YAML frontmatter + Markdown body: `head -15 .planning/STATE.md | grep -c '^---$'` returns 2 (frontmatter delimiters intact)
    - `stopped_at` field references Phase 2: `grep -E '^stopped_at:.*Phase 2' .planning/STATE.md` exits 0
    - **Issue-2 fix — date-agnostic last_updated:** `grep -qE '^last_updated:\s*"[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3}Z"' .planning/STATE.md` exits 0 (matches whatever ISO timestamp the executor's clock produced — replaces the prior date-coupled `2026-04-29` check).
    - **Issue-2 fix — date-agnostic last_activity:** `grep -qE '^last_activity:\s*[0-9]{4}-[0-9]{2}-[0-9]{2} -- Plan 02-03' .planning/STATE.md` exits 0.
    - **Issue-2 fix — no unsubstituted shell expression leaked:** `grep -F '$(date' .planning/STATE.md` exits 1 (the literal `$(date +%Y-%m-%d)` and `$(date -u …)` strings must NOT survive into the final file).
    - `progress.completed_phases` is 2: `grep -qE 'completed_phases:\s*2' .planning/STATE.md` exits 0
    - `progress.completed_plans` is 11: `grep -qE 'completed_plans:\s*11' .planning/STATE.md` exits 0
    - `progress.percent` is 18: `grep -qE 'percent:\s*18' .planning/STATE.md` exits 0
    - Current Position shows Phase 02 complete: `grep -qE 'Phase: 02.*COMPLETE.*3/3' .planning/STATE.md` exits 0
    - Current Position references next step: `grep -qE 'Next:.*Phase 03|Next:.*verify' .planning/STATE.md` exits 0
    - Existing Phase 1 audit trail preserved: `grep -c 'Plan 01-08' .planning/STATE.md` returns ≥ 1 (Plan 01-08 references survive)
    - Decisions block untouched: `grep -c '^### Decisions$' .planning/STATE.md` returns exactly 1
    - Blockers/Concerns block preserved: `grep -c '^### Blockers/Concerns$' .planning/STATE.md` returns exactly 1
    - Deferred Items block preserved: `grep -c '^## Deferred Items$' .planning/STATE.md` returns exactly 1
    - All three Phase-2 plan SUMMARY files exist (Plans 01, 02 closed before this task; this task's Plan 03 SUMMARY is created on plan close, not in this task): `ls .planning/phases/02-frontend-recon-toolchain-lock/02-0[12]-SUMMARY.md | wc -l` returns 2
  </acceptance_criteria>
  <done>
    `.planning/STATE.md` reflects Phase 2 closure: `stopped_at` describes the recon + toolchain-lock outcome, `last_updated` is the executor's run-day ISO timestamp (Issue-2: date-agnostic), `progress` shows 2/11 phases complete and 11/11 cumulative plans complete (18%), Current Position points to `/gsd-verify-work` then Phase 3, Session Continuity references the recon report + PROJECT.md Key Decisions row as the hand-off artifacts. Phase 1 audit trail (Plan 01-XX entries) is fully preserved. Decisions / Blockers / Deferred Items blocks are untouched. No literal `$(date …)` shell expressions leaked into the final file.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `grep -c 'Frontend toolchain' .planning/PROJECT.md` returns 2 (Key Decisions row + Resolved bullet).
2. `awk '/^## Key Decisions$/,/^## Open Questions$/' .planning/PROJECT.md | grep -qE 'Vite\s*8' && grep -q 'VITE_API_BASE_URL' .planning/PROJECT.md` exits 0.
3. `awk '/^## Decision Matrix/,EOF' .planning/intel/n11-recon.md | grep -qE 'Vite \+ React 19 SPA.*65|: 65$'` exits 0.
4. `cd tools/recon && npm run check` exits 0 (recon report still passes the sanity checker — including the new 9th assertion which Plan 02-01 Issue-3 added).
5. `bash scripts/check-phase-02-artifacts.sh` (full mode, no `--bootstrap`) exits 0 — every check including the PROJECT.md Key Decisions row passes (Issue-4: closes the single gap pinned by Plan 02 Task 2).
6. **Issue-2 fix:** `grep -qE 'Locked [0-9]{4}-[0-9]{2}-[0-9]{2} \(Phase 2\)' .planning/PROJECT.md` exits 0 (date-agnostic Phase-2 stamp). `grep -F '$(date' .planning/PROJECT.md` exits 1 AND `grep -F '$(date' .planning/STATE.md` exits 1 (no unsubstituted shell expressions).
7. `grep -qE 'completed_phases:\s*2' .planning/STATE.md` exits 0.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 satisfied: `.planning/intel/n11-recon.md` exists with screenshots of all 7 page types referenced from §1 Page Inventory
- ROADMAP Phase 2 success criterion 2 satisfied: §2 Turkish Copy Catalog has ≥30 verbatim phrases (Plan 02 captured; Plan 03 preserves)
- ROADMAP Phase 2 success criterion 3 satisfied: PROJECT.md Key Decisions row records the toolchain decision with rationale tying back to recon findings (≥2 specific findings cited, plus VITE_API_BASE_URL env-var convention)
- ROADMAP Phase 2 success criterion 4 satisfied: Category taxonomy table (§3) and Color Token Table (§4 with ≥10 hex tokens) are committed in n11-recon.md so Phase 10 doesn't re-research
- §7 Anti-pattern flags retains the no-chat-panel callout (Pitfall #19 carry-forward to Phase 11)
- §8 Open n11 questions for Phase 10/11 is enriched from recon evidence (4 baseline + any new questions surfaced)
- §Decision Matrix subsection is the auditable scoring sheet referenced by PROJECT.md Key Decisions
- STATE.md reflects Phase 2 closure (2/11 phases, 18%, ready-for-verify)
- `bash scripts/check-phase-02-artifacts.sh` (full mode) exits 0
- **Issue-2 fix:** every dated stamp is the executor's actual run-day date (via `$(date +%Y-%m-%d)` substitution); no literal hard-coded dates and no unsubstituted shell expressions in the final files; date-agnostic grep regexes verify the stamps.
- FE-01 requirement is verifiably complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-recon-toolchain-lock/02-03-SUMMARY.md` with:
- Final phrase count and token count in n11-recon.md after enrichment (should match Plan 02-02-SUMMARY)
- Specific recon-evidence findings cited in the PROJECT.md Key Decisions rationale (the three bullets)
- Any §8 Open Questions added beyond the four baseline ones
- Confirmation that `bash scripts/check-phase-02-artifacts.sh` exits 0 in full mode
- The actual run-day date that landed in PROJECT.md (`Locked YYYY-MM-DD`) and STATE.md `last_updated` ISO timestamp — Issue-2 audit trail
- Hand-off note for Phase 10: which sections of n11-recon.md to read for what (e.g., §2 → FE-13 Turkish UI copy; §4 → Tailwind 4 `@theme` tokens; §6 → component layout decisions; §7 → anti-patterns to NOT replicate; §Decision Matrix → toolchain audit trail)
- Hand-off note for Phase 11: §7 callout flags that the chat-bubble UX is greenfield (no n11 reference); the toolchain Key Decisions row carries the SSE-via-EventSource note
</output>
