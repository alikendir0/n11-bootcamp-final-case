---
phase: 08-ai-port-adapter-agent-toolset
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - agent-toolset/src/main/java/com/n11/agent/http/ToolHttpClients.java
  - agent-toolset/src/main/java/com/n11/agent/http/ProductToolClient.java
  - agent-toolset/src/main/java/com/n11/agent/http/CartToolClient.java
  - agent-toolset/src/main/java/com/n11/agent/http/OrderToolClient.java
  - agent-toolset/src/main/java/com/n11/agent/http/PaymentToolClient.java
  - agent-toolset/src/main/java/com/n11/agent/tools/AbstractAgentTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/SearchProductsTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/GetProductTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/ListCategoriesTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/ViewCartTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/AddToCartTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/UpdateCartItemTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/RemoveFromCartTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/CreateOrderTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/GetPaymentLinkTool.java
  - agent-toolset/src/main/java/com/n11/agent/tools/GetOrderStatusTool.java
  - agent-toolset/src/test/java/com/n11/agent/AgentToolRegistryTest.java
  - agent-toolset/src/test/java/com/n11/agent/tools/ToolSchemaContractTest.java
autonomous: true
requirements: [AI-05, AI-15]
must_haves:
  truths:
    - "ToolRegistry contains exactly 10 tools with the canonical names from CLAUDE.md Rule #2 / AI-05"
    - "Mutating tools (add_to_cart, update_cart_item, remove_from_cart, create_order, get_payment_link, get_order_status) report requiresAuth()=true (D-04)"
    - "Read tools (search_products, get_product, list_categories, view_cart) report requiresAuth()=false"
    - "Tool implementations have ZERO references to ai-service-specific types (Conversation, SseEmitter, ToolDispatcher) — Pitfall #16 / Phase 9 hand-off contract"
    - "All cart tools call cart-service REST surface and forward X-User-Id from ToolContext (AI-15: chat cart and REST cart are the same store)"
    - "Per-service ToolClients (ProductToolClient, CartToolClient, OrderToolClient, PaymentToolClient) hop directly via REST + Eureka discovery and forward X-User-Id/X-User-Roles from ToolContext (D-05) — no JWT minting, no extra gateway round-trip"
    - "search_products tool backs onto product-service `GET /products?q=` (Phase 4 PROD-04 ILIKE+GIN) in v1, NOT search-service or Gemini embedding (D-10); the tool interface stays stable across the v1→v2 transition"
  artifacts:
    - path: "agent-toolset/src/main/java/com/n11/agent/tools/SearchProductsTool.java"
      provides: "search_products tool — wraps GET /products?q= via ProductToolClient"
      contains: "search_products"
    - path: "agent-toolset/src/main/java/com/n11/agent/tools/AddToCartTool.java"
      provides: "add_to_cart tool — POST /cart/items with X-User-Id forwarding (AI-15)"
      contains: "add_to_cart"
    - path: "agent-toolset/src/main/java/com/n11/agent/tools/CreateOrderTool.java"
      provides: "create_order tool — POST /orders with Idempotency-Key header (Plan 05-03 lesson)"
      contains: "Idempotency-Key"
    - path: "agent-toolset/src/test/java/com/n11/agent/AgentToolRegistryTest.java"
      provides: "Wave 0 gate: registry.all().size() == 10 + every name in the canonical 10-tool list"
      contains: "hasSize(10)"
  key_links:
    - from: "agent-toolset/.../tools/*Tool.java"
      to: "agent-toolset/.../http/*ToolClient.java"
      via: "Constructor injection"
      pattern: "class \\w+Tool"
    - from: "Cart / Order / Payment ToolClient"
      to: "ToolContext.userId() => X-User-Id header"
      via: "RestClient.header(\"X-User-Id\", ctx.userId())"
      pattern: "X-User-Id"
    - from: "CreateOrderTool"
      to: "Idempotency-Key UUID per call"
      via: "RestClient.header(\"Idempotency-Key\", ...)"
      pattern: "Idempotency-Key"
---

<objective>
Implement the 10 canonical agent tools as @Component AgentTool beans inside the agent-toolset shared module. Each tool defines name/descriptionTr/requiresAuth/parametersJsonSchema and delegates execution to a thin per-service HTTP client. Mutating tools (D-04) gate on ToolContext.userId() before dispatch. Cart and order tools forward X-User-Id from ToolContext so cart added by chat is the same cart-service row a REST client sees (AI-15). create_order sends a fresh UUID Idempotency-Key per call (Plan 05-03 dedup contract).

Purpose: agent-toolset is the single source of truth for tool definitions (CLAUDE.md Non-negotiable Rule #2). ai-service consumes it in Plan 04; mcp-server (Phase 9) consumes the same beans without modification — so tools must NOT reference ai-service-specific types (Pitfall #7 mcp-server hand-off).

Output: 10 working tool beans + AgentToolRegistryTest passing + ToolSchemaContractTest verifying every tool has a parsable JSON Schema and the correct requiresAuth posture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/api-contracts.md
@.planning/phases/08-ai-port-adapter-agent-toolset/08-CONTEXT.md
@.planning/phases/08-ai-port-adapter-agent-toolset/08-RESEARCH.md
@.planning/phases/08-ai-port-adapter-agent-toolset/08-PATTERNS.md
@.planning/phases/08-ai-port-adapter-agent-toolset/08-01-PLAN.md
@CLAUDE.md
@cart-service/src/main/java/com/n11/cart/cart/CartController.java
@cart-service/src/main/java/com/n11/cart/product/ProductClient.java
@payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java
@product-service/src/main/java/com/n11/product/product/ProductController.java
@product-service/src/main/java/com/n11/product/category/CategoryController.java
@order-service/src/main/java/com/n11/order/order/OrderController.java
@payment-service/src/main/java/com/n11/payment/api/PaymentController.java

<interfaces>
<!-- Backing endpoint contracts (api-contracts.md §1 + verified controller mappings). -->

product-service @RequestMapping("/products"):
- GET /products?q=&categoryId=&page=&size=&sort= → Page<ProductSummaryDto> (PROD-04 ILIKE)
- GET /products/{id} → ProductDetailDto

product-service @RequestMapping("/categories"):
- GET /categories → List<CategoryDto>

cart-service @RequestMapping("/cart") — REQUIRES X-User-Id (CartController.resolveUserId throws 401 if missing):
- GET    /cart                       → CartView
- POST   /cart/items   {productId, qty}  → CartView (status 201)
- PATCH  /cart/items/{productId}  {qty}  → CartView
- DELETE /cart/items/{productId}        → 204

order-service @RequestMapping("/orders") — REQUIRES X-User-Id + Idempotency-Key:
- POST /orders   header Idempotency-Key=<uuid>  body {addressId, paymentMethod}  → OrderResponse (200 dedup or 202 new)
- GET  /orders/{id}                              → OrderDetail (with status timeline)

payment-service @RequestMapping("/payments") — REQUIRES X-User-Id:
- GET /payments/{orderId} → PaymentResponse (paymentPageUrl is the link the chat surfaces)

ToolContext (from Plan 01):
- userId (nullable for guests — D-03)
- correlationId (set by ai-service ToolDispatcher; tools forward to outbound HTTP)
- seenIds (managed by ToolDispatcher in Plan 04; tools do NOT touch this set directly)

ToolResult sealed:
- ToolResult.ok(JsonNode data) — Gemini sees this as a successful tool result
- ToolResult.err(String code, String message) — Gemini sees the message and replies with a Turkish apology

Pitfall #16 (mcp-server hand-off): tool implementations MUST NOT import any class from ai-service.* packages. ToolContext is the only context they receive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-service HTTP clients (Product / Cart / Order / Payment) + AbstractAgentTool helper + Spring config</name>
  <read_first>
    - cart-service/src/main/java/com/n11/cart/product/ProductClient.java (full file — RestClient.Builder + @Value base URL pattern)
    - payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java (full file — RestClient.Builder + onStatus + stripTrailingSlash)
    - cart-service/src/main/java/com/n11/cart/cart/CartController.java (X-User-Id resolution shape)
    - .planning/phases/08-ai-port-adapter-agent-toolset/08-PATTERNS.md "agent-toolset/.../tools/*.java" (lines 142-231) — RestClient call pattern, X-User-Id forwarding, backing endpoint mapping table
    - .planning/phases/08-ai-port-adapter-agent-toolset/08-RESEARCH.md §"Pattern 6: agent-toolset Module Shape" (lines 731-797), §"Architectural Responsibility Map" line 91
    - agent-toolset/build.gradle.kts (just written — confirms jackson-databind + spring-context only; NO spring-web)
    - agent-toolset/src/main/java/com/n11/agent/AgentTool.java (interface signature)
    - agent-toolset/src/main/java/com/n11/agent/ToolContext.java
    - agent-toolset/src/main/java/com/n11/agent/ToolResult.java
  </read_first>
  <files>
    agent-toolset/build.gradle.kts,
    agent-toolset/src/main/java/com/n11/agent/http/ToolHttpClients.java,
    agent-toolset/src/main/java/com/n11/agent/http/ProductToolClient.java,
    agent-toolset/src/main/java/com/n11/agent/http/CartToolClient.java,
    agent-toolset/src/main/java/com/n11/agent/http/OrderToolClient.java,
    agent-toolset/src/main/java/com/n11/agent/http/PaymentToolClient.java,
    agent-toolset/src/main/java/com/n11/agent/tools/AbstractAgentTool.java
  </files>
  <action>
    1) **Update agent-toolset/build.gradle.kts** — append `spring-web` (RestClient lives there) so per Pitfall #16 / D-05 we can forward headers without depending on ai-service:

       Replace the dependencies block (do NOT touch plugins block):
       ```kotlin
       dependencies {
           implementation(project(":ai-port"))
           implementation("com.fasterxml.jackson.core:jackson-databind")
           implementation("org.springframework:spring-context")
           // Spring Web brings RestClient (Spring 6.1+); we use blocking HTTP from tools (D-05).
           implementation("org.springframework:spring-web")

           testImplementation("org.junit.jupiter:junit-jupiter")
           testImplementation("org.assertj:assertj-core")
           testRuntimeOnly("org.junit.platform:junit-platform-launcher")
       }
       ```

    2) **agent-toolset/src/main/java/com/n11/agent/http/ToolHttpClients.java** — Spring `@Configuration` exposing one `RestClient.Builder` bean if no parent context provides one (kept as fallback only — when imported into ai-service the existing common-logging RestClientConfig wins):
       ```java
       package com.n11.agent.http;

       import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
       import org.springframework.context.annotation.Bean;
       import org.springframework.context.annotation.Configuration;
       import org.springframework.web.client.RestClient;

       /**
        * Provides a default RestClient.Builder if no parent module supplies one.
        * In ai-service / mcp-server, common-logging.RestClientConfig contributes a
        * pre-wired Builder with CorrelationIdRestClientInterceptor — that wins by
        * @ConditionalOnMissingBean.
        */
       @Configuration
       public class ToolHttpClients {

           @Bean
           @ConditionalOnMissingBean
           RestClient.Builder toolRestClientBuilder() {
               return RestClient.builder();
           }
       }
       ```

       Note: `@ConditionalOnMissingBean` lives in `spring-boot-autoconfigure`. Add `compileOnly("org.springframework.boot:spring-boot-autoconfigure")` to agent-toolset/build.gradle.kts dependencies block above (insert after `spring-web` line). Runtime resolution comes from the consuming Boot app.

    3) **ProductToolClient.java** under `com.n11.agent.http`:
       ```java
       package com.n11.agent.http;

       import com.fasterxml.jackson.databind.JsonNode;
       import com.fasterxml.jackson.databind.ObjectMapper;
       import org.springframework.beans.factory.annotation.Value;
       import org.springframework.stereotype.Component;
       import org.springframework.web.client.RestClient;
       import org.springframework.web.client.RestClientException;

       /**
        * Read-only product-service client used by search_products / get_product /
        * list_categories tools. No X-User-Id required (PROD-* endpoints are public).
        */
       @Component
       public class ProductToolClient {

           private final RestClient client;
           private final String baseUrl;
           private final ObjectMapper mapper = new ObjectMapper();

           public ProductToolClient(RestClient.Builder builder,
                                    @Value("${app.clients.product.base-url:http://product-service:8082}") String baseUrl) {
               this.baseUrl = stripTrailingSlash(baseUrl);
               this.client = builder.build();
           }

           public JsonNode searchProducts(String q, String categoryId, int page, int size) {
               StringBuilder uri = new StringBuilder(baseUrl).append("/products?page=").append(page).append("&size=").append(size);
               if (q != null && !q.isBlank()) uri.append("&q=").append(java.net.URLEncoder.encode(q, java.nio.charset.StandardCharsets.UTF_8));
               if (categoryId != null && !categoryId.isBlank()) uri.append("&categoryId=").append(categoryId);
               return getAsJson(uri.toString());
           }

           public JsonNode getProduct(String productId) {
               return getAsJson(baseUrl + "/products/" + productId);
           }

           public JsonNode listCategories() {
               return getAsJson(baseUrl + "/categories");
           }

           private JsonNode getAsJson(String url) {
               try {
                   String body = client.get().uri(url).retrieve().body(String.class);
                   return body == null ? mapper.nullNode() : mapper.readTree(body);
               } catch (RestClientException | java.io.IOException e) {
                   throw new ToolHttpException("PRODUCT_UPSTREAM", e.getMessage(), e);
               }
           }

           private static String stripTrailingSlash(String v) {
               if (v == null || v.isBlank()) return "http://product-service:8082";
               return v.endsWith("/") ? v.substring(0, v.length() - 1) : v;
           }
       }
       ```

    4) **CartToolClient.java** — forwards X-User-Id from ToolContext (AI-15):
       ```java
       package com.n11.agent.http;

       import com.fasterxml.jackson.databind.JsonNode;
       import com.fasterxml.jackson.databind.ObjectMapper;
       import org.springframework.beans.factory.annotation.Value;
       import org.springframework.http.MediaType;
       import org.springframework.stereotype.Component;
       import org.springframework.web.client.RestClient;
       import org.springframework.web.client.RestClientException;

       import java.util.Map;

       /**
        * cart-service client. Every method REQUIRES a non-null userId (cart endpoints
        * require X-User-Id; gateway-injected mesh per CONTEXT D-05). Mutating tools
        * gate on requiresAuth() before calling these methods.
        */
       @Component
       public class CartToolClient {

           private final RestClient client;
           private final String baseUrl;
           private final ObjectMapper mapper = new ObjectMapper();

           public CartToolClient(RestClient.Builder builder,
                                 @Value("${app.clients.cart.base-url:http://cart-service:8084}") String baseUrl) {
               this.baseUrl = stripTrailingSlash(baseUrl);
               this.client = builder.build();
           }

           public JsonNode getCart(String userId) {
               return execute(() -> client.get()
                   .uri(baseUrl + "/cart")
                   .header("X-User-Id", userId)
                   .retrieve()
                   .body(String.class));
           }

           public JsonNode addItem(String userId, String productId, int qty) {
               return execute(() -> client.post()
                   .uri(baseUrl + "/cart/items")
                   .header("X-User-Id", userId)
                   .contentType(MediaType.APPLICATION_JSON)
                   .body(Map.of("productId", productId, "qty", qty))
                   .retrieve()
                   .body(String.class));
           }

           public JsonNode updateItem(String userId, String productId, int qty) {
               return execute(() -> client.patch()
                   .uri(baseUrl + "/cart/items/" + productId)
                   .header("X-User-Id", userId)
                   .contentType(MediaType.APPLICATION_JSON)
                   .body(Map.of("qty", qty))
                   .retrieve()
                   .body(String.class));
           }

           public JsonNode removeItem(String userId, String productId) {
               try {
                   client.delete()
                       .uri(baseUrl + "/cart/items/" + productId)
                       .header("X-User-Id", userId)
                       .retrieve()
                       .toBodilessEntity();
                   return mapper.createObjectNode().put("removed", true).put("productId", productId);
               } catch (RestClientException e) {
                   throw new ToolHttpException("CART_UPSTREAM", e.getMessage(), e);
               }
           }

           private JsonNode execute(java.util.function.Supplier<String> call) {
               try {
                   String body = call.get();
                   return body == null ? mapper.nullNode() : mapper.readTree(body);
               } catch (RestClientException | java.io.IOException e) {
                   throw new ToolHttpException("CART_UPSTREAM", e.getMessage(), e);
               }
           }

           private static String stripTrailingSlash(String v) {
               if (v == null || v.isBlank()) return "http://cart-service:8084";
               return v.endsWith("/") ? v.substring(0, v.length() - 1) : v;
           }
       }
       ```

    5) **OrderToolClient.java** — forwards X-User-Id AND attaches a fresh UUID Idempotency-Key per createOrder call (Plan 05-03 dedup contract):
       ```java
       package com.n11.agent.http;

       import com.fasterxml.jackson.databind.JsonNode;
       import com.fasterxml.jackson.databind.ObjectMapper;
       import org.springframework.beans.factory.annotation.Value;
       import org.springframework.http.MediaType;
       import org.springframework.stereotype.Component;
       import org.springframework.web.client.RestClient;
       import org.springframework.web.client.RestClientException;

       import java.util.Map;
       import java.util.UUID;

       @Component
       public class OrderToolClient {

           private final RestClient client;
           private final String baseUrl;
           private final ObjectMapper mapper = new ObjectMapper();

           public OrderToolClient(RestClient.Builder builder,
                                  @Value("${app.clients.order.base-url:http://order-service:8085}") String baseUrl) {
               this.baseUrl = stripTrailingSlash(baseUrl);
               this.client = builder.build();
           }

           public JsonNode createOrder(String userId, String addressId, String paymentMethod) {
               UUID idempotencyKey = UUID.randomUUID();      // Plan 05-03: per-call dedup
               return execute(() -> client.post()
                   .uri(baseUrl + "/orders")
                   .header("X-User-Id", userId)
                   .header("Idempotency-Key", idempotencyKey.toString())
                   .contentType(MediaType.APPLICATION_JSON)
                   .body(Map.of("addressId", addressId, "paymentMethod", paymentMethod))
                   .retrieve()
                   .body(String.class));
           }

           public JsonNode getOrderStatus(String userId, String orderId) {
               return execute(() -> client.get()
                   .uri(baseUrl + "/orders/" + orderId)
                   .header("X-User-Id", userId)
                   .retrieve()
                   .body(String.class));
           }

           private JsonNode execute(java.util.function.Supplier<String> call) {
               try {
                   String body = call.get();
                   return body == null ? mapper.nullNode() : mapper.readTree(body);
               } catch (RestClientException | java.io.IOException e) {
                   throw new ToolHttpException("ORDER_UPSTREAM", e.getMessage(), e);
               }
           }

           private static String stripTrailingSlash(String v) {
               if (v == null || v.isBlank()) return "http://order-service:8085";
               return v.endsWith("/") ? v.substring(0, v.length() - 1) : v;
           }
       }
       ```

    6) **PaymentToolClient.java** — forwards X-User-Id; only get_payment_link tool uses it:
       ```java
       package com.n11.agent.http;

       import com.fasterxml.jackson.databind.JsonNode;
       import com.fasterxml.jackson.databind.ObjectMapper;
       import org.springframework.beans.factory.annotation.Value;
       import org.springframework.stereotype.Component;
       import org.springframework.web.client.RestClient;
       import org.springframework.web.client.RestClientException;

       @Component
       public class PaymentToolClient {

           private final RestClient client;
           private final String baseUrl;
           private final ObjectMapper mapper = new ObjectMapper();

           public PaymentToolClient(RestClient.Builder builder,
                                    @Value("${app.clients.payment.base-url:http://payment-service:8086}") String baseUrl) {
               this.baseUrl = stripTrailingSlash(baseUrl);
               this.client = builder.build();
           }

           public JsonNode getPaymentForOrder(String userId, String orderId) {
               try {
                   String body = client.get()
                       .uri(baseUrl + "/payments/" + orderId)
                       .header("X-User-Id", userId)
                       .retrieve()
                       .body(String.class);
                   return body == null ? mapper.nullNode() : mapper.readTree(body);
               } catch (RestClientException | java.io.IOException e) {
                   throw new ToolHttpException("PAYMENT_UPSTREAM", e.getMessage(), e);
               }
           }

           private static String stripTrailingSlash(String v) {
               if (v == null || v.isBlank()) return "http://payment-service:8086";
               return v.endsWith("/") ? v.substring(0, v.length() - 1) : v;
           }
       }
       ```

       Also create `agent-toolset/src/main/java/com/n11/agent/http/ToolHttpException.java`:
       ```java
       package com.n11.agent.http;

       public class ToolHttpException extends RuntimeException {
           private final String code;
           public ToolHttpException(String code, String message, Throwable cause) {
               super(message, cause);
               this.code = code;
           }
           public String code() { return code; }
       }
       ```

    7) **AbstractAgentTool.java** under `com.n11.agent.tools` — common helper for D-04 auth gate + ToolHttpException → ToolResult.Err mapping:
       ```java
       package com.n11.agent.tools;

       import com.fasterxml.jackson.databind.JsonNode;
       import com.n11.agent.AgentTool;
       import com.n11.agent.ToolContext;
       import com.n11.agent.ToolResult;
       import com.n11.agent.http.ToolHttpException;

       /**
        * Shared base: D-04 auth check + ToolHttpException → ToolResult.Err mapping.
        * Concrete subclasses implement name(), descriptionTr(), requiresAuth(),
        * parametersJsonSchema(), and doExecute(ctx, args).
        *
        * Tools NEVER throw out of execute(); errors come back as ToolResult.Err so
        * Gemini sees them as tool-result messages (Pitfall #6) and replies in Turkish.
        */
       public abstract class AbstractAgentTool implements AgentTool {

           protected abstract ToolResult doExecute(ToolContext ctx, JsonNode args);

           @Override
           public final ToolResult execute(ToolContext ctx, JsonNode args) {
               if (requiresAuth() && (ctx.userId() == null || ctx.userId().isBlank())) {
                   return ToolResult.err("AUTH_REQUIRED",
                       "Bu işlem için giriş yapman gerekiyor. Giriş yaptıktan sonra tekrar deneyelim.");
               }
               try {
                   return doExecute(ctx, args);
               } catch (ToolHttpException e) {
                   return ToolResult.err(e.code(),
                       "Araç çalıştırılırken hata oluştu: " + safeMessage(e.getMessage()));
               } catch (RuntimeException e) {
                   return ToolResult.err("TOOL_ERROR",
                       "Araç çalıştırılırken beklenmeyen bir hata oluştu.");
               }
           }

           private static String safeMessage(String raw) {
               if (raw == null) return "";
               // Strip stack-trace fragments / SQL fragments from upstream
               return raw.length() > 200 ? raw.substring(0, 200) + "…" : raw;
           }
       }
       ```
  </action>
  <verify>
    <automated>./gradlew :agent-toolset:compileJava :agent-toolset:test --tests AiPortContractTest 2>/dev/null; ./gradlew :agent-toolset:compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :agent-toolset:compileJava` exits 0
    - `agent-toolset/build.gradle.kts` contains `spring-web` and `spring-boot-autoconfigure`
    - `grep -c 'X-User-Id' agent-toolset/src/main/java/com/n11/agent/http/CartToolClient.java` returns at least 4 (one per cart method: getCart, addItem, updateItem, removeItem)
    - `grep -q 'Idempotency-Key' agent-toolset/src/main/java/com/n11/agent/http/OrderToolClient.java` succeeds
    - `grep -q 'UUID.randomUUID()' agent-toolset/src/main/java/com/n11/agent/http/OrderToolClient.java` succeeds
    - `grep -q 'AUTH_REQUIRED' agent-toolset/src/main/java/com/n11/agent/tools/AbstractAgentTool.java` succeeds
    - `grep -rn 'import com.n11.ai\.' agent-toolset/src/main/java/ | grep -v 'com.n11.ai.port' | wc -l` returns 0 (Pitfall #16: no ai-service imports; only ai-port allowed)
    - `grep -rn 'import com.google.genai' agent-toolset/src/main/java/ | wc -l` returns 0 (D-01: no Gemini SDK in agent-toolset)
  </acceptance_criteria>
  <done>4 thin per-service HTTP clients + AbstractAgentTool helper compile cleanly. X-User-Id forwarding wired into Cart/Order/Payment clients. Idempotency-Key wired into createOrder. Zero ai-service imports (Pitfall #16 / Phase 9 hand-off contract held).</done>
</task>

<task type="auto">
  <name>Task 2: 10 AgentTool @Component implementations + AgentToolRegistryTest + ToolSchemaContractTest</name>
  <read_first>
    - agent-toolset/src/main/java/com/n11/agent/AgentTool.java (interface signature)
    - agent-toolset/src/main/java/com/n11/agent/tools/AbstractAgentTool.java (just written)
    - agent-toolset/src/main/java/com/n11/agent/http/*.java (all four clients just written)
    - .planning/phases/08-ai-port-adapter-agent-toolset/08-RESEARCH.md §"System Prompt — Tool descriptions in Turkish (10 tools)" (lines 1128-1142)
    - .planning/phases/08-ai-port-adapter-agent-toolset/08-PATTERNS.md "Backing endpoint mapping per tool" table (lines 207-220)
    - .planning/phases/08-ai-port-adapter-agent-toolset/08-CONTEXT.md <decisions> D-04 (mutating tools require auth), D-08 (provenance — but that lives in ToolDispatcher, not in tools)
    - CLAUDE.md Non-negotiable Rule #2 (10 canonical tool names, single source of truth)
  </read_first>
  <files>
    agent-toolset/src/main/java/com/n11/agent/tools/SearchProductsTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/GetProductTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/ListCategoriesTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/ViewCartTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/AddToCartTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/UpdateCartItemTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/RemoveFromCartTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/CreateOrderTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/GetPaymentLinkTool.java,
    agent-toolset/src/main/java/com/n11/agent/tools/GetOrderStatusTool.java,
    agent-toolset/src/test/java/com/n11/agent/AgentToolRegistryTest.java,
    agent-toolset/src/test/java/com/n11/agent/tools/ToolSchemaContractTest.java
  </files>
  <action>
    Implement the 10 canonical tools. Names from CLAUDE.md Rule #2 / AI-05 — exact strings. Turkish descriptions verbatim from RESEARCH §"System Prompt — Tool descriptions in Turkish (10 tools)".

    All tools follow this skeleton (variations noted per tool):
    ```java
    package com.n11.agent.tools;

    import com.fasterxml.jackson.databind.JsonNode;
    import com.fasterxml.jackson.databind.ObjectMapper;
    import com.n11.agent.ToolContext;
    import com.n11.agent.ToolResult;
    import com.n11.agent.http.ProductToolClient;       // or Cart/Order/Payment
    import org.springframework.stereotype.Component;

    @Component
    public class XxxTool extends AbstractAgentTool {
        private final ProductToolClient client;
        public XxxTool(ProductToolClient client) { this.client = client; }
        @Override public String name() { return "..."; }
        @Override public String descriptionTr() { return "..."; }
        @Override public boolean requiresAuth() { return ...; }
        @Override public String parametersJsonSchema() { return "..."; }
        @Override protected ToolResult doExecute(ToolContext ctx, JsonNode args) { ... }
    }
    ```

    JSON Schemas: hand-written `String` literals, drafted as JSON Schema draft-07. Required keys in `required` array. Use one-line strings (escaped quotes) to keep records compact.

    **1) SearchProductsTool** (read; ProductToolClient):
    - `name() = "search_products"`
    - `descriptionTr() = "Ürün kataloğunda metin araması yapar. Önce bu araçla ara, sonra sonuçlardan gelen ID'leri kullan."`
    - `requiresAuth() = false`
    - Schema: object with optional string `q`, optional string `categoryId`, optional integer `page` (default 0), optional integer `size` (default 10).
    - `doExecute`: read `args.path("q").asText(null)`, `args.path("categoryId").asText(null)`, `args.path("page").asInt(0)`, `args.path("size").asInt(10)` → `client.searchProducts(q, categoryId, page, size)` → `ToolResult.ok(node)`.
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"q\":{\"type\":\"string\"},\"categoryId\":{\"type\":\"string\"},\"page\":{\"type\":\"integer\",\"minimum\":0,\"default\":0},\"size\":{\"type\":\"integer\",\"minimum\":1,\"maximum\":50,\"default\":10}}}"`

    **2) GetProductTool** (read; ProductToolClient):
    - `name() = "get_product"`
    - `descriptionTr() = "Bir ürünün tam detaylarını getirir (fiyat, stok, açıklama)."`
    - `requiresAuth() = false`
    - Schema: object, required `productId` (string).
    - `doExecute`: `String pid = args.required("productId").asText();` → `client.getProduct(pid)` → ok.
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"productId\":{\"type\":\"string\",\"description\":\"Önce search_products ile elde edilmiş ürün ID'si\"}},\"required\":[\"productId\"]}"`

    **3) ListCategoriesTool** (read; ProductToolClient):
    - `name() = "list_categories"`
    - `descriptionTr() = "Tüm ürün kategorilerini listeler."`
    - `requiresAuth() = false`
    - Schema: object, no properties required.
    - Schema literal: `"{\"type\":\"object\",\"properties\":{}}"`
    - `doExecute`: `client.listCategories()` → ok.

    **4) ViewCartTool** (read but auth-aware; CartToolClient):
    - `name() = "view_cart"`
    - `descriptionTr() = "Kullanıcının sepetini ve toplam tutarı gösterir."`
    - `requiresAuth() = true` — D-04: cart endpoints require X-User-Id; guests would get 401 from cart-service. Returning AUTH_REQUIRED before the call is the cleaner UX (Gemini suggests login).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{}}"`
    - `doExecute`: `client.getCart(ctx.userId())` → ok.

    **5) AddToCartTool** (mutating; CartToolClient):
    - `name() = "add_to_cart"`
    - `descriptionTr() = "Sepete ürün ekler. Giriş gerektirir."`
    - `requiresAuth() = true`
    - Schema: required `productId` (string), required `qty` (integer ≥ 1).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"productId\":{\"type\":\"string\"},\"qty\":{\"type\":\"integer\",\"minimum\":1}},\"required\":[\"productId\",\"qty\"]}"`
    - `doExecute`: read productId + qty → `client.addItem(ctx.userId(), productId, qty)` → ok.

    **6) UpdateCartItemTool** (mutating; CartToolClient):
    - `name() = "update_cart_item"`
    - `descriptionTr() = "Sepetteki bir ürünün adedini günceller. Giriş gerektirir."`
    - `requiresAuth() = true`
    - Schema: required `productId` (string), required `qty` (integer ≥ 1).
    - Schema literal: same shape as AddToCartTool.
    - `doExecute`: `client.updateItem(ctx.userId(), productId, qty)` → ok.

    **7) RemoveFromCartTool** (mutating; CartToolClient):
    - `name() = "remove_from_cart"`
    - `descriptionTr() = "Sepetten bir ürünü kaldırır. Giriş gerektirir."`
    - `requiresAuth() = true`
    - Schema: required `productId` (string).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"productId\":{\"type\":\"string\"}},\"required\":[\"productId\"]}"`
    - `doExecute`: `client.removeItem(ctx.userId(), productId)` → ok.

    **8) CreateOrderTool** (mutating; OrderToolClient):
    - `name() = "create_order"`
    - `descriptionTr() = "Sepetteki ürünlerden sipariş oluşturur ve ödeme bağlantısı döner. Giriş gerektirir."`
    - `requiresAuth() = true`
    - Schema: required `addressId` (string), required `paymentMethod` (string enum: CREDIT_CARD, CASH_ON_DELIVERY).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"addressId\":{\"type\":\"string\"},\"paymentMethod\":{\"type\":\"string\",\"enum\":[\"CREDIT_CARD\",\"CASH_ON_DELIVERY\"]}},\"required\":[\"addressId\",\"paymentMethod\"]}"`
    - `doExecute`: `client.createOrder(ctx.userId(), addressId, paymentMethod)` → ok.

    **9) GetPaymentLinkTool** (mutating-adjacent; PaymentToolClient):
    - `name() = "get_payment_link"`
    - `descriptionTr() = "Mevcut siparişin ödeme bağlantısını getirir. Giriş gerektirir."`
    - `requiresAuth() = true`
    - Schema: required `orderId` (string).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"orderId\":{\"type\":\"string\"}},\"required\":[\"orderId\"]}"`
    - `doExecute`: `client.getPaymentForOrder(ctx.userId(), orderId)` → ok.

    **10) GetOrderStatusTool** (read but auth-required; OrderToolClient):
    - `name() = "get_order_status"`
    - `descriptionTr() = "Bir siparişin güncel durumunu gösterir. Giriş gerektirir."`
    - `requiresAuth() = true` — order-service requires X-User-Id (orders are user-scoped).
    - Schema: required `orderId` (string).
    - Schema literal: `"{\"type\":\"object\",\"properties\":{\"orderId\":{\"type\":\"string\"}},\"required\":[\"orderId\"]}"`
    - `doExecute`: `client.getOrderStatus(ctx.userId(), orderId)` → ok.

    **AgentToolRegistryTest** (Wave 0 gate — RESEARCH §Validation Architecture Wave 0 Gaps):
    ```java
    package com.n11.agent;

    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.context.annotation.ComponentScan;
    import org.springframework.context.annotation.Configuration;

    import java.util.List;
    import java.util.Set;

    import static org.assertj.core.api.Assertions.assertThat;

    /**
     * Wave 0 gate (AI-05): exactly 10 canonical agent tools registered.
     * Names locked by CLAUDE.md Rule #2 — duplicates or missing tools fail this gate.
     */
    @SpringBootTest(classes = AgentToolRegistryTest.TestApp.class)
    class AgentToolRegistryTest {

        private static final Set<String> CANONICAL_NAMES = Set.of(
            "search_products", "get_product", "list_categories",
            "add_to_cart", "view_cart", "update_cart_item", "remove_from_cart",
            "create_order", "get_payment_link", "get_order_status"
        );

        private static final Set<String> MUTATING_NAMES = Set.of(
            "add_to_cart", "view_cart", "update_cart_item", "remove_from_cart",
            "create_order", "get_payment_link", "get_order_status"
        );

        @Autowired ToolRegistry registry;

        @Test
        void registry_contains_exactly_10_canonical_tools() {
            List<AgentTool> all = registry.all();
            assertThat(all).hasSize(10);
            assertThat(all).extracting(AgentTool::name).containsExactlyInAnyOrderElementsOf(CANONICAL_NAMES);
        }

        @Test
        void mutating_tools_require_auth() {
            for (AgentTool t : registry.all()) {
                if (MUTATING_NAMES.contains(t.name())) {
                    assertThat(t.requiresAuth()).as("%s must require auth (D-04)", t.name()).isTrue();
                } else {
                    assertThat(t.requiresAuth()).as("%s must NOT require auth", t.name()).isFalse();
                }
            }
        }

        @Test
        void every_tool_has_a_turkish_description() {
            for (AgentTool t : registry.all()) {
                assertThat(t.descriptionTr())
                    .as("%s description must be non-blank", t.name())
                    .isNotBlank();
            }
        }

        @Configuration
        @ComponentScan(basePackages = "com.n11.agent")
        static class TestApp { }
    }
    ```

    **ToolSchemaContractTest** — verifies every parametersJsonSchema() returns parseable JSON Schema with `type: object`:
    ```java
    package com.n11.agent.tools;

    import com.fasterxml.jackson.databind.JsonNode;
    import com.fasterxml.jackson.databind.ObjectMapper;
    import com.n11.agent.AgentTool;
    import com.n11.agent.ToolRegistry;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.context.annotation.ComponentScan;
    import org.springframework.context.annotation.Configuration;

    import static org.assertj.core.api.Assertions.assertThat;

    @SpringBootTest(classes = ToolSchemaContractTest.TestApp.class)
    class ToolSchemaContractTest {

        @Autowired ToolRegistry registry;
        private final ObjectMapper mapper = new ObjectMapper();

        @Test
        void every_tool_returns_parseable_object_schema() throws Exception {
            for (AgentTool t : registry.all()) {
                String schemaJson = t.parametersJsonSchema();
                assertThat(schemaJson).as("%s schema must not be null", t.name()).isNotNull();
                JsonNode schema = mapper.readTree(schemaJson);
                assertThat(schema.path("type").asText()).as("%s schema must have type=object", t.name()).isEqualTo("object");
                assertThat(schema.has("properties")).as("%s schema must have properties", t.name()).isTrue();
            }
        }

        @Configuration
        @ComponentScan(basePackages = "com.n11.agent")
        static class TestApp { }
    }
    ```

    Note: AgentToolRegistryTest + ToolSchemaContractTest use `@SpringBootTest` because they need the full @ComponentScan to find the @Component AgentTool beans. agent-toolset is a `java-library` module, so spring-boot-starter-test must be added to its testImplementation block. Update agent-toolset/build.gradle.kts (already done in Task 1's spring-web step) to also include:
    ```kotlin
    testImplementation("org.springframework.boot:spring-boot-starter-test")
    ```
    Add to the test deps block. The Spring Boot BOM is applied by the root subprojects block, so version comes from there.
  </action>
  <verify>
    <automated>./gradlew :agent-toolset:test --tests AgentToolRegistryTest --tests ToolSchemaContractTest</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :agent-toolset:test --tests AgentToolRegistryTest` exits 0
    - `./gradlew :agent-toolset:test --tests ToolSchemaContractTest` exits 0
    - `find agent-toolset/src/main/java/com/n11/agent/tools -name '*Tool.java' | grep -v Abstract | wc -l` returns 10 (exactly 10 concrete tool files)
    - `grep -c '"search_products"\|"get_product"\|"list_categories"\|"view_cart"\|"add_to_cart"\|"update_cart_item"\|"remove_from_cart"\|"create_order"\|"get_payment_link"\|"get_order_status"' agent-toolset/src/main/java/com/n11/agent/tools/*.java` returns at least 10 (each name appears in its file's `name()` body)
    - `grep -l 'requiresAuth() { return true' agent-toolset/src/main/java/com/n11/agent/tools/*.java | wc -l` returns 7 (the 7 mutating/authed tools per AgentToolRegistryTest.MUTATING_NAMES)
    - `grep -l 'requiresAuth() { return false' agent-toolset/src/main/java/com/n11/agent/tools/*.java | wc -l` returns 3 (search_products, get_product, list_categories)
    - `grep -rn 'import com.n11.ai\.' agent-toolset/src/main/java/ | grep -v 'com.n11.ai.port' | wc -l` returns 0 (Phase 9 hand-off contract: tools never import ai-service classes)
    - `grep -q 'CANONICAL_NAMES = Set.of' agent-toolset/src/test/java/com/n11/agent/AgentToolRegistryTest.java` succeeds
    - `grep -q 'hasSize(10)' agent-toolset/src/test/java/com/n11/agent/AgentToolRegistryTest.java` succeeds
    - `grep -q 'type=object' agent-toolset/src/test/java/com/n11/agent/tools/ToolSchemaContractTest.java || grep -q '"type=object"\|isEqualTo("object")' agent-toolset/src/test/java/com/n11/agent/tools/ToolSchemaContractTest.java` succeeds (schema-shape gate present)
  </acceptance_criteria>
  <done>10 canonical AgentTool implementations registered as @Component; AgentToolRegistryTest and ToolSchemaContractTest both green; mutating tools gate on requiresAuth() (D-04); cart tools forward X-User-Id (AI-15); create_order sends Idempotency-Key (Plan 05-03); zero ai-service imports (Phase 9 hand-off contract preserved).</done>
</task>

</tasks>

<verification>
- `./gradlew :agent-toolset:test` exits 0 with both AgentToolRegistryTest and ToolSchemaContractTest green
- `grep -rn 'import com.n11.ai\.' agent-toolset/src/main/java/ | grep -v 'com.n11.ai.port'` returns nothing (Pitfall #16: agent-toolset is reusable by mcp-server in Phase 9)
- `grep -rn 'import com.google.genai' agent-toolset/src/` returns nothing (D-01: Gemini stays in ai-service)
- 10 concrete `@Component` AgentTool classes exist; ToolRegistry exposes them all
</verification>

<success_criteria>
- 10 canonical agent tools (CLAUDE.md Rule #2 / AI-05) registered as Spring beans + auto-discovered by `ToolRegistry`.
- D-04 auth posture honored: 7 tools requireAuth(), 3 do not.
- AI-15 cart-bridge: cart tools call cart-service via `X-User-Id` forwarding from `ToolContext.userId()`.
- Pitfall #16 honored: zero ai-service imports in agent-toolset (Phase 9 mcp-server can import unchanged).
- Plan 05-03 Idempotency-Key contract honored by `create_order` (fresh UUID per call).
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-port-adapter-agent-toolset/08-02-SUMMARY.md` recording: (1) the 10 tool name → backing endpoint matrix actually shipped, (2) any deviation from the planned JSON Schema literals (e.g. additional optional fields), (3) confirmation that the agent-toolset module is import-clean for mcp-server (Phase 9), (4) recommended Plan 04 entry-point reads.
</output>
