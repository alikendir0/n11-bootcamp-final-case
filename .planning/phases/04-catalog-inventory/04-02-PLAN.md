---
phase: 04-catalog-inventory
plan: 02
plan_id: 04-02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - inventory-service/build.gradle.kts
  - inventory-service/src/main/java/com/n11/inventory/InventoryServiceApplication.java
  - inventory-service/src/main/java/com/n11/inventory/health/SampleHealthController.java
  - inventory-service/src/main/java/com/n11/inventory/messaging/InventoryRabbitConfig.java
  - inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java
  - inventory-service/src/main/java/com/n11/inventory/messaging/ProcessedEvent.java
  - inventory-service/src/main/java/com/n11/inventory/messaging/ProcessedEventRepository.java
  - inventory-service/src/main/java/com/n11/inventory/stock/Stock.java
  - inventory-service/src/main/java/com/n11/inventory/stock/StockRepository.java
  - inventory-service/src/main/java/com/n11/inventory/stock/StockService.java
  - inventory-service/src/main/java/com/n11/inventory/stock/StockController.java
  - inventory-service/src/main/java/com/n11/inventory/stock/StockStateDto.java
  - inventory-service/src/main/java/com/n11/inventory/stock/InsufficientStockException.java
  - inventory-service/src/main/java/com/n11/inventory/reservation/StockReservation.java
  - inventory-service/src/main/java/com/n11/inventory/reservation/StockReservationRepository.java
  - inventory-service/src/main/java/com/n11/inventory/outbox/OutboxEvent.java
  - inventory-service/src/main/java/com/n11/inventory/outbox/OutboxRepository.java
  - inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java
  - inventory-service/src/main/resources/application.yml
  - inventory-service/src/main/resources/logback-spring.xml
  - inventory-service/src/main/resources/db/migration/V1__init_processed_events.sql
  - inventory-service/src/main/resources/db/migration/V2__init_inventory.sql
  - inventory-service/src/main/resources/db/migration/V3__seed_stock.sql
  - inventory-service/src/test/resources/application-test.yml
  - inventory-service/src/test/java/com/n11/inventory/stock/StockStateComputationTest.java
  - inventory-service/src/test/java/com/n11/inventory/stock/StockEntityVersionTest.java
  - inventory-service/src/test/java/com/n11/inventory/stock/InventoryControllerIntegrationTest.java
  - inventory-service/src/test/java/com/n11/inventory/messaging/OrderCreatedConsumerIntegrationTest.java
  - inventory-service/README.md
  - config-server/src/main/resources/config/inventory-service.yml
  - docker-compose.yml
autonomous: true
requirements:
  - PROD-06
  - PROD-07
  - PROD-08
must_haves:
  truths:
    - "inventory-service listens on port 8083 and boots healthy (actuator/health UP)"
    - "Schema isolation: inventory_user owns only inventory schema; cross-schema queries against product schema fail with permission denied"
    - "Stock entity has @Version Long version; concurrent reservations trigger ObjectOptimisticLockingFailureException caught as RESERVATION_CONFLICT"
    - "GET /inventory/{productId} returns StockStateDto with Turkish labels: 'Stokta', 'Tükendi', 'Son N ürün!' (PROD-06)"
    - "CLAUDE.md Rule #3: OrderCreatedConsumer checks processedEventsRepository.existsById(eventId) BEFORE any state change; processed_events row + stock_reservations row + outbox row all written in the SAME @Transactional method"
    - "Re-delivering the same order.created eventId yields exactly ONE processed_events row, ONE stock_reservations row, ONE outbox row"
    - "OutboxPoller @Scheduled(fixedDelay=5000) drains inventory.outbox and publishes to inventory.tx exchange"
    - "DLQ wiring: inventory.q.order-created has x-dead-letter-routing-key=inventory.q.order-created.dlq"
    - "Springdoc Swagger UI reachable at http://localhost:8083/swagger-ui.html inside docker network (PROD-07)"
    - "docker-compose inventory-service block has NO host ports mapping (Pitfall #14)"
  artifacts:
    - path: inventory-service/src/main/resources/db/migration/V2__init_inventory.sql
      provides: "stock + stock_reservations + outbox tables with @Version column, low_stock_threshold, outbox partial index"
      contains: "low_stock_threshold INT"
    - path: inventory-service/src/main/java/com/n11/inventory/stock/Stock.java
      provides: "JPA entity with @Version Long version, getEffectiveAvailable() domain method"
      contains: "@Version"
    - path: inventory-service/src/main/java/com/n11/inventory/stock/StockStateDto.java
      provides: "Record with stockState enum and Turkish stockStateLabel"
      contains: "Stokta"
    - path: inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java
      provides: "Idempotent @RabbitListener @Transactional consumer with processed_events check"
      contains: "processedEventsRepository.existsById"
    - path: inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java
      provides: "@Scheduled(fixedDelay=5000) outbox drain to inventory.tx"
      contains: "fixedDelay = 5000"
    - path: inventory-service/src/test/java/com/n11/inventory/messaging/OrderCreatedConsumerIntegrationTest.java
      provides: "Testcontainers Postgres+RabbitMQ idempotency proof: publish twice, assert single side effect"
      contains: "Awaitility"
    - path: config-server/src/main/resources/config/inventory-service.yml
      provides: "Port 8083, inventory_user, default-schema: inventory, flyway create-schemas=false"
      contains: "default-schema: inventory"
    - path: docker-compose.yml
      provides: "inventory-service block n11/inventory-service:dev, no host ports, healthy depends_on chain"
      contains: "n11/inventory-service:dev"
  key_links:
    - from: inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java
      to: inventory-service/src/main/java/com/n11/inventory/outbox/OutboxRepository.java
      via: "@Transactional save of stock_reservations + outbox + processed_events in same TX"
      pattern: "outboxRepository.save"
    - from: inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java
      to: inventory-service/src/main/java/com/n11/inventory/messaging/InventoryRabbitConfig.java
      via: "Poller publishes to inventory.tx declared by InventoryRabbitConfig.inventoryExchange bean"
      pattern: "inventory.tx"
---

<objective>
Stand up inventory-service: Gradle module scaffold, V2 inventory DDL with @Version-protected stock + stock_reservations + outbox tables, StockController returning Turkish stock-state DTOs, saga consumer (orders.tx/order.created to inventory.tx/stock.reserved or stock.reserve_failed via transactional outbox + processed_events idempotency inbox), docker-compose registration, and idempotency proof via Testcontainers.

Purpose: PROD-06 (Turkish stock labels), PROD-07 (Springdoc), PROD-08 (stock reservation saga). This is the first saga consumer in the project. Phase 5 wires downstream consumers (payment-service binds payment.q.stock-reserved); Phase 4 only declares-and-publishes.

Output: Compilable inventory-service; `./gradlew :inventory-service:test` green; `GET /inventory/{productId}` returns StockStateDto; OrderCreatedConsumerIntegrationTest proves CLAUDE.md Rule #3 idempotency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/api-contracts.md
@.planning/saga-contracts.md
@.planning/phases/04-catalog-inventory/04-RESEARCH.md
@.planning/phases/04-catalog-inventory/04-PATTERNS.md
@.planning/phases/04-catalog-inventory/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Critical contracts extracted from saga-contracts.md and the codebase. -->

Exchange / queue topology (saga-contracts.md §2, §5.2, §3):
- `inventory.tx` -- durable topic, declared by inventory-service (producer exchange)
- `orders.tx` -- durable topic, also declared by inventory-service (idempotent re-declaration; actually owned by order-service Phase 5)
- `inventory.q.order-created` -- bound to `orders.tx` with routing key `order.created`, DLX x-dead-letter-routing-key=`inventory.q.order-created.dlq`
- `inventory.q.order-created.dlq` -- default exchange DLQ

Incoming order.created payload (order-created.schema.json):
  orderId, userId, currency="TRY", totalAmount, items[{ productId, qty, unitPrice, nameSnapshot }]

Outgoing stock.reserved payload (stock-reserved.schema.json):
  orderId, reservationId, reservedItems[{ productId, qty }]

Outgoing stock.reserve_failed payload (stock-reserve-failed.schema.json):
  orderId, reason in {INSUFFICIENT_STOCK, PRODUCT_NOT_FOUND, RESERVATION_CONFLICT}, failedItems[{ productId, requestedQty, availableQty }]

Saga Envelope (from common-events Envelope record -- use import, do NOT redefine):
  import com.n11.events.Envelope;
  new Envelope(eventId.toString(), "stock.reserved", 1, occurredAt,
               incomingEnvelope.correlationId(),  // saga-wide
               incomingEnvelope.eventId(),        // causationId
               "inventory-service", objectMapper.valueToTree(payload))

REST contract (api-contracts.md §1):
  GET /inventory/{productId}  ->  StockStateDto  (public, no auth)

Stock-state label mapping:
  effectiveAvailable <= 0                        -> stockState=TUKENDI, label="Tükendi"
  0 < effectiveAvailable <= lowStockThreshold    -> stockState=SON_URUN, label="Son {N} ürün!"
  effectiveAvailable > lowStockThreshold         -> stockState=STOKTA,  label="Stokta"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Module scaffold -- build.gradle.kts, application.yml, logback, V1 migration, Application class, health endpoint, docker-compose entry, config-server YAML, README</name>
  <files>
    inventory-service/build.gradle.kts,
    inventory-service/src/main/java/com/n11/inventory/InventoryServiceApplication.java,
    inventory-service/src/main/java/com/n11/inventory/health/SampleHealthController.java,
    inventory-service/src/main/resources/application.yml,
    inventory-service/src/main/resources/logback-spring.xml,
    inventory-service/src/main/resources/db/migration/V1__init_processed_events.sql,
    inventory-service/README.md,
    config-server/src/main/resources/config/inventory-service.yml,
    docker-compose.yml
  </files>
  <read_first>
    - identity-service/build.gradle.kts (analog -- remove oauth2/security; rename baseName + image + port)
    - identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java (analog -- keep @EnableScheduling for OutboxPoller)
    - identity-service/src/main/java/com/n11/identity/health/SampleHealthController.java (analog -- change package + default service name)
    - identity-service/src/main/resources/application.yml (spring.config.import shape)
    - identity-service/src/main/resources/db/migration/V1__init_processed_events.sql (copy verbatim)
    - identity-service/src/main/resources/logback-spring.xml (copy verbatim)
    - config-server/src/main/resources/config/identity-service.yml (analog for inventory-service.yml)
    - docker-compose.yml (identity-service block as shape reference; append after product-service block)
    - settings.gradle.kts (verify "inventory-service" include already present from 04-01)
    - .planning/phases/04-catalog-inventory/04-PATTERNS.md (sections: inventory-service/build.gradle.kts, InventoryServiceApplication, config-server inventory-service.yml, docker-compose modify)
  </read_first>
  <action>
    **Step 0 -- Verify settings.gradle.kts:**
    `grep -c '"inventory-service"' settings.gradle.kts` must return 1. If 0, add `"inventory-service",` after `"product-service",`. Do not create a duplicate.

    **Step 1 -- Create directory tree:**
    ```
    mkdir -p inventory-service/src/main/java/com/n11/inventory/health
    mkdir -p inventory-service/src/main/java/com/n11/inventory/messaging
    mkdir -p inventory-service/src/main/java/com/n11/inventory/stock
    mkdir -p inventory-service/src/main/java/com/n11/inventory/reservation
    mkdir -p inventory-service/src/main/java/com/n11/inventory/outbox
    mkdir -p inventory-service/src/main/resources/db/migration
    mkdir -p inventory-service/src/test/java/com/n11/inventory/stock
    mkdir -p inventory-service/src/test/java/com/n11/inventory/messaging
    mkdir -p inventory-service/src/test/resources
    ```

    **Step 2 -- build.gradle.kts:** Copy identity-service/build.gradle.kts and apply:
    - REMOVE spring-boot-starter-oauth2-resource-server, spring-boot-starter-security, testRuntimeOnly h2
    - KEEP testImplementation(libs.networknt.json.schema) -- needed for OrderCreatedConsumerIntegrationTest outbox-payload drift gate
    - KEEP testcontainers-postgresql, testcontainers-rabbitmq, testcontainers-junit-jupiter
    - ADD `testImplementation("org.awaitility:awaitility:4.2.0")` if not already present
    - archiveBaseName.set("inventory-service")
    - jib to { image = "n11/inventory-service:dev" }
    - jib container ports = listOf("8083")

    **Step 3 -- InventoryServiceApplication.java:**
    Package com.n11.inventory. @SpringBootApplication(scanBasePackages = "com.n11") @EnableDiscoveryClient @EnableScheduling. Keep @EnableScheduling for OutboxPoller.

    **Step 4 -- SampleHealthController.java:** Copy identity-service analog verbatim; change package to com.n11.inventory.health; change @Value default "identity-service" to "inventory-service".

    **Step 5 -- application.yml:**
    ```yaml
    spring:
      application:
        name: inventory-service
      config:
        import: configserver:http://config-server:8888?fail-fast=true&max-attempts=10&max-interval=1500&multiplier=1.2&initial-interval=1100
    ```

    **Step 6 -- logback-spring.xml:** Copy identity-service/src/main/resources/logback-spring.xml verbatim.

    **Step 7 -- V1__init_processed_events.sql:** Copy identity-service/src/main/resources/db/migration/V1__init_processed_events.sql verbatim. Flyway default-schema=inventory places the table in inventory schema.

    **Step 8 -- config-server/src/main/resources/config/inventory-service.yml:** Copy identity-service.yml analog and apply:
    - port: 8083
    - datasource.username: inventory_user, password: ${INVENTORY_DB_PASSWORD}
    - flyway.schemas: inventory, default-schema: inventory, create-schemas: false
    - flyway.placeholders.schema: inventory, flyway.schema: inventory
    - hibernate.default_schema: inventory
    - Remove jwt: block entirely
    - Remove adminSeedEmail / adminSeedPasswordHash Flyway placeholders
    - Keep springdoc:, management:, rabbitmq:, eureka: blocks verbatim from analog

    **Step 9 -- docker-compose.yml:** Append the inventory-service block after the product-service block (before volumes: section). NO ports: key (Pitfall #14). Exact shape:
    ```yaml
      inventory-service:
        image: n11/inventory-service:dev
        container_name: n11-inventory-service
        env_file:
          - .env
        environment:
          SPRING_CONFIG_IMPORT: configserver:http://config-server:8888?fail-fast=true&max-attempts=10&max-interval=1500&multiplier=1.2&initial-interval=1100
          EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
          SPRING_PROFILES_ACTIVE: docker
        depends_on:
          postgres:
            condition: service_healthy
          rabbitmq:
            condition: service_healthy
          eureka-server:
            condition: service_healthy
          config-server:
            condition: service_healthy
        healthcheck:
          test: ["CMD-SHELL", "wget -q -O- http://localhost:8083/actuator/health | grep -q '\"status\":\"UP\"'"]
          interval: 5s
          timeout: 3s
          retries: 6
          start_period: 25s
        restart: unless-stopped
        networks:
          - n11-net
    ```

    **Step 10 -- README.md:** Document: purpose, endpoints table (GET /inventory/{productId} public), saga table (IN: orders.tx/order.created -> inventory.q.order-created; OUT: inventory.tx/stock.reserved, inventory.tx/stock.reserve_failed), required env vars (INVENTORY_DB_PASSWORD, RABBITMQ_DEFAULT_USER/PASS), boot procedure (./gradlew :inventory-service:jibDockerBuild + docker compose up -d inventory-service), pitfall tripwires (CLAUDE.md Rule #3 idempotency, Pitfall #11 no cross-schema joins, RESERVATION_CONFLICT catch).
  </action>
  <verify>
    <automated>./gradlew :inventory-service:compileJava --no-daemon 2>&amp;1 | tail -5 &amp;&amp; grep -c '"inventory-service"' settings.gradle.kts &amp;&amp; grep -c 'default-schema: inventory' config-server/src/main/resources/config/inventory-service.yml &amp;&amp; docker compose config &gt; /dev/null &amp;&amp; echo "compose-ok"</automated>
  </verify>
  <done>
    inventory-service is a recognized Gradle subproject (settings.gradle.kts has entry); cloned skeleton compiles; docker-compose has the additive inventory-service block with no host port; config-server/config/inventory-service.yml sets port=8083, default-schema=inventory, create-schemas=false, inventory_user credentials.
  </done>
  <acceptance_criteria>
    - `grep -c '"inventory-service"' settings.gradle.kts` equals 1
    - `./gradlew projects --no-daemon 2>&1 | grep -c 'inventory-service'` equals 1
    - `docker compose config > /dev/null` exits 0
    - `grep -v '^#' docker-compose.yml | grep -c 'n11/inventory-service:dev'` equals 1
    - `grep -c 'default-schema: inventory' config-server/src/main/resources/config/inventory-service.yml` equals 1
    - `grep -c 'port: 8083' config-server/src/main/resources/config/inventory-service.yml` equals 1
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V2 DDL + JPA layer -- stock, stock_reservations, outbox tables; Stock @Version entity; StockService; StockStateDto; StockController; test slice tests</name>
  <files>
    inventory-service/src/main/resources/db/migration/V2__init_inventory.sql,
    inventory-service/src/main/resources/db/migration/V3__seed_stock.sql,
    inventory-service/src/main/java/com/n11/inventory/stock/Stock.java,
    inventory-service/src/main/java/com/n11/inventory/stock/StockRepository.java,
    inventory-service/src/main/java/com/n11/inventory/stock/StockService.java,
    inventory-service/src/main/java/com/n11/inventory/stock/StockController.java,
    inventory-service/src/main/java/com/n11/inventory/stock/StockStateDto.java,
    inventory-service/src/main/java/com/n11/inventory/stock/InsufficientStockException.java,
    inventory-service/src/main/java/com/n11/inventory/reservation/StockReservation.java,
    inventory-service/src/main/java/com/n11/inventory/reservation/StockReservationRepository.java,
    inventory-service/src/test/resources/application-test.yml,
    inventory-service/src/test/java/com/n11/inventory/stock/StockStateComputationTest.java,
    inventory-service/src/test/java/com/n11/inventory/stock/StockEntityVersionTest.java,
    inventory-service/src/test/java/com/n11/inventory/stock/InventoryControllerIntegrationTest.java,
    inventory-service/src/test/java/com/n11/inventory/migrations/InventoryFlywayMigrationsTest.java
  </files>
  <read_first>
    - identity-service/src/main/java/com/n11/identity/user/User.java (JPA entity structural pattern: protected no-arg ctor, @Column on every field, UUID PK updatable=false, accessor-only methods)
    - identity-service/src/main/resources/db/migration/V2__init_users_addresses.sql (DDL shape analog)
    - identity-service/src/test/java/com/n11/identity/user/UserEntityTest.java (@DataJpaTest + Testcontainers slice pattern)
    - identity-service/src/test/java/com/n11/identity/outbox/OutboxIntegrationTest.java (@SpringBootTest + @ServiceConnection pattern)
    - identity-service/src/test/resources/application-test.yml (test override shape: eureka disabled, config import disabled, flyway create-schemas=true for TC)
    - .planning/phases/04-catalog-inventory/04-PATTERNS.md (sections: Stock.java, StockReservation.java, Testcontainers Slice Test Pattern #6, SpringBootTest Integration Test Pattern #7)
    - .planning/phases/04-catalog-inventory/04-RESEARCH.md (Pattern 3: Stock State Computation and stock-state threshold rules)
  </read_first>
  <behavior>
    StockStateComputationTest (pure unit -- no Spring, no DB):
    - Stock(productId, availableQty=0, reservedQty=0, threshold=5) -> getEffectiveAvailable()=0 -> StockStateDto.stockState=TUKENDI, label contains "Tükendi"
    - Stock(productId, availableQty=3, reservedQty=0, threshold=5) -> getEffectiveAvailable()=3 -> stockState=SON_URUN, label="Son 3 ürün!"
    - Stock(productId, availableQty=10, reservedQty=0, threshold=5) -> getEffectiveAvailable()=10 -> stockState=STOKTA, label="Stokta"
    - Stock(productId, availableQty=5, reservedQty=0, threshold=5) -> boundary getEffectiveAvailable()=5=threshold -> stockState=SON_URUN (<=threshold)

    StockEntityVersionTest (@DataJpaTest + Testcontainers):
    - optimisticLockConflictThrowsException: save stock with qty=10; load in two EntityManagers; first EM updates to qty=8 and saves; second EM attempts update -> throws ObjectOptimisticLockingFailureException
    - versionIncrements: save stock, update once, reload, assert version==1
  </behavior>
  <action>
    **V2__init_inventory.sql** -- exact DDL (Flyway runs inside inventory schema via default-schema=inventory):
    ```sql
    -- V2__init_inventory.sql -- inventory schema

    CREATE TABLE stock (
        product_id          UUID        PRIMARY KEY,
        available_qty       INT         NOT NULL DEFAULT 0 CHECK (available_qty >= 0),
        reserved_qty        INT         NOT NULL DEFAULT 0 CHECK (reserved_qty >= 0),
        low_stock_threshold INT         NOT NULL DEFAULT 5,
        version             BIGINT      NOT NULL DEFAULT 0,
        created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),
        updated_at          TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    CREATE TABLE stock_reservations (
        id           UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
        order_id     UUID        NOT NULL,
        product_id   UUID        NOT NULL REFERENCES stock(product_id),
        reserved_qty INT         NOT NULL CHECK (reserved_qty > 0),
        status       VARCHAR(20) NOT NULL DEFAULT 'RESERVED'
                                 CHECK (status IN ('RESERVED', 'RELEASED', 'COMMITTED')),
        created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
        UNIQUE (order_id, product_id)
    );

    CREATE TABLE outbox (
        id          UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
        aggregate   VARCHAR(64)  NOT NULL,
        event_type  VARCHAR(128) NOT NULL,
        payload     JSONB        NOT NULL,
        occurred_at TIMESTAMPTZ  NOT NULL DEFAULT now(),
        sent_at     TIMESTAMPTZ
    );

    CREATE INDEX outbox_unsent_idx ON outbox (occurred_at) WHERE sent_at IS NULL;
    ```

    **V3__seed_stock.sql** -- cross-schema restriction: inventory_user is REVOKED from product schema, cannot SELECT FROM product.products. Use HARDCODED UUID literals that MATCH the product UUIDs in product-service's V3__seed_products.sql. No cross-schema FK — this is a seed-data convention only (Pitfall #11 is about runtime queries, not about matching UUID values in seed files). Do NOT use gen_random_uuid() — UUIDs must be stable.

    Stock distribution: 5 rows qty=0 (Tükendi), 5 rows qty=2 (Son N ürün! with threshold=5), remainder qty=10-50 (Stokta). All rows use low_stock_threshold=5 (default).

    ```sql
    -- V3__seed_stock.sql
    -- 52 stock rows matching product UUIDs from product-service V3__seed_products.sql.
    -- UUIDs are '00000000-0000-4000-8000-000000000001' through '...000052'.
    -- inventory_user cannot query product schema (Pitfall #11), so product_id values
    -- are hardcoded here -- same values as the product id column in V3__seed_products.sql.
    -- Do NOT regenerate UUIDs. Distribution: 5 rows qty=0 (Tükendi),
    -- 5 rows qty=2 (Son N ürün! at threshold=5), rest qty=10-50 (Stokta).

    INSERT INTO stock (product_id, available_qty, reserved_qty, low_stock_threshold, version)
    VALUES
        -- qty=0: Tükendi (5 rows: products 001-005)
        ('00000000-0000-4000-8000-000000000001', 0,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000002', 0,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000003', 0,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000004', 0,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000005', 0,  0, 5, 0),
        -- qty=2: Son N ürün! (5 rows: products 006-010)
        ('00000000-0000-4000-8000-000000000006', 2,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000007', 2,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000008', 2,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000009', 2,  0, 5, 0),
        ('00000000-0000-4000-8000-000000000010', 2,  0, 5, 0),
        -- qty=10-50: Stokta (42 rows: products 011-052)
        ('00000000-0000-4000-8000-000000000011', 15, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000012', 20, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000013', 10, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000014', 30, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000015', 25, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000016', 12, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000017', 18, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000018', 50, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000019', 40, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000020', 22, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000021', 35, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000022', 10, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000023', 28, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000024', 17, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000025', 45, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000026', 33, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000027', 14, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000028', 21, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000029', 11, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000030', 38, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000031', 16, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000032', 24, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000033', 19, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000034', 42, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000035', 13, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000036', 29, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000037', 36, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000038', 10, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000039', 48, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000040', 27, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000041', 31, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000042', 20, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000043', 15, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000044', 43, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000045', 26, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000046', 32, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000047', 10, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000048', 23, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000049', 37, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000050', 19, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000051', 44, 0, 5, 0),
        ('00000000-0000-4000-8000-000000000052', 11, 0, 5, 0)
    ON CONFLICT (product_id) DO NOTHING;
    ```

    **Stock.java** -- @Entity @Table(name = "stock"), follow identity-service User.java structural pattern (PATTERNS.md §5):
    - @Id @Column(name = "product_id", nullable = false, updatable = false) UUID productId
    - @Column(name = "available_qty") int availableQty
    - @Column(name = "reserved_qty") int reservedQty = 0
    - @Column(name = "low_stock_threshold") int lowStockThreshold = 5
    - @Version @Column(name = "version", nullable = false) Long version
    - @Column(name = "created_at", updatable = false) Instant createdAt
    - @Column(name = "updated_at") Instant updatedAt
    - Protected no-arg JPA constructor
    - Public constructor Stock(UUID productId, int availableQty): sets reservedQty=0, lowStockThreshold=5, version=0, createdAt/updatedAt=Instant.now()
    - public int getEffectiveAvailable() { return availableQty - reservedQty; }
    - public void reserve(int qty) { this.reservedQty += qty; this.updatedAt = Instant.now(); }
    - public void release(int qty) { this.reservedQty = Math.max(0, this.reservedQty - qty); this.updatedAt = Instant.now(); }
    - Accessor getters only, no raw setters for qty fields except the domain methods above

    **StockRepository.java** -- JpaRepository<Stock, UUID>; no extra queries needed for Phase 4.

    **StockReservation.java** -- @Entity @Table(name = "stock_reservations"):
    - @Id UUID id; @Column UUID orderId; @Column UUID productId; @Column int reservedQty; @Column String status = "RESERVED"; @Column Instant createdAt
    - Public constructor sets id=UUID.randomUUID(), createdAt=Instant.now()

    **StockReservationRepository.java** -- JpaRepository<StockReservation, UUID>; add boolean existsByOrderIdAndProductId(UUID orderId, UUID productId).

    **InsufficientStockException.java** -- RuntimeException with fields: UUID productId, int requested, int available. Constructor sets message = "Insufficient stock for product " + productId.

    **StockStateDto.java** -- Java record:
    ```java
    public record StockStateDto(UUID productId, int availableQty, String stockState,
                                String stockStateLabel, int displayQty) {
        public static StockStateDto from(Stock stock) {
            int eff = stock.getEffectiveAvailable();
            String state, label;
            if (eff <= 0) {
                state = "TUKENDI"; label = "Tükendi";
            } else if (eff <= stock.getLowStockThreshold()) {
                state = "SON_URUN"; label = "Son " + eff + " ürün!";
            } else {
                state = "STOKTA"; label = "Stokta";
            }
            return new StockStateDto(stock.getProductId(), eff, state, label, eff);
        }
        public static StockStateDto notFound(UUID productId) {
            return new StockStateDto(productId, 0, "TUKENDI", "Tükendi", 0);
        }
    }
    ```

    **StockService.java** -- @Service:
    - @Transactional(readOnly = true) StockStateDto getStockState(UUID productId): finds stock or returns StockStateDto.notFound(productId) for unknown productId
    - @Transactional StockReservation reserveStock(UUID orderId, UUID productId, int qty): load stock (throw ProductNotFoundException if missing), check getEffectiveAvailable() >= qty (throw InsufficientStockException if not), call stock.reserve(qty), stockRepository.save(stock) [triggers @Version check], save and return new StockReservation. Caller (OrderCreatedConsumer) catches ObjectOptimisticLockingFailureException at the consumer level.

    **StockController.java** -- @RestController @RequestMapping("/inventory"):
    - @GetMapping("/{productId}") public StockStateDto get(@PathVariable UUID productId): calls stockService.getStockState(productId)
    - @Operation(summary = "Stock state for product") annotation for Springdoc
    - Public endpoint, no auth required

    **application-test.yml** -- copy identity-service test analog; change schema references to inventory:
    ```yaml
    spring:
      cloud:
        config:
          enabled: false
      config:
        import: ""
      jpa:
        hibernate:
          ddl-auto: none
      flyway:
        enabled: true
        create-schemas: true
        schemas: inventory
        default-schema: inventory
        placeholders:
          schema: inventory
          flyway.schema: inventory
      rabbitmq:
        listener:
          simple:
            auto-startup: false
    eureka:
      client:
        enabled: false
        register-with-eureka: false
        fetch-registry: false
    ```

    **StockStateComputationTest.java** -- pure unit test, no Spring context:
    Class name must be exactly StockStateComputationTest (VALIDATION.md 4-02-02).
    Test four cases in behavior block above using StockStateDto.from(stock) directly.
    Import: org.junit.jupiter.api.Test, static org.junit.jupiter.api.Assertions.*

    **StockEntityVersionTest.java** -- @DataJpaTest + @Testcontainers + pgvector/pgvector:pg16:
    Class name must be exactly StockEntityVersionTest (VALIDATION.md 4-02-01).
    @DynamicPropertySource: url, username, password, flyway.enabled=true, ddl-auto=none, flyway.schemas=inventory, flyway.default-schema=inventory, flyway.create-schemas=true, flyway.placeholders.schema=inventory, flyway.placeholders.flyway.schema=inventory.
    Test optimisticLockConflictThrowsException and versionIncrements per behavior block above.

    **InventoryControllerIntegrationTest.java** -- @SpringBootTest(webEnvironment=RANDOM_PORT) + @ActiveProfiles("test") + @Testcontainers:
    Class name must be exactly InventoryControllerIntegrationTest (VALIDATION.md 4-02-04).
    @ServiceConnection containers: pgvector/pgvector:pg16 + rabbitmq:4.0-management.
    @DynamicPropertySource: override flyway.user, flyway.password, flyway.schemas=inventory, flyway.default-schema=inventory, flyway.create-schemas=true, flyway.placeholders.schema=inventory.
    Seed a Stock row via @Autowired StockRepository; then GET /inventory/{productId} via TestRestTemplate; assert HTTP 200; assert response.stockState is one of STOKTA/SON_URUN/TUKENDI; assert stockStateLabel is non-null.

    **InventoryFlywayMigrationsTest.java** -- Wave 0 Nyquist test (B-03). @JdbcTest + @ActiveProfiles("test") + @Testcontainers + @ServiceConnection pgvector:pg16. Class name must be exactly InventoryFlywayMigrationsTest (VALIDATION.md row 4-02 migrations). Package: com.n11.inventory.migrations.
    ```java
    package com.n11.inventory.migrations;

    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.autoconfigure.jdbc.AutoConfigureTestDatabase;
    import org.springframework.boot.test.autoconfigure.jdbc.JdbcTest;
    import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
    import org.springframework.jdbc.core.JdbcTemplate;
    import org.springframework.test.context.ActiveProfiles;
    import org.testcontainers.containers.PostgreSQLContainer;
    import org.testcontainers.junit.jupiter.Container;
    import org.testcontainers.junit.jupiter.Testcontainers;
    import org.testcontainers.utility.DockerImageName;

    import static org.assertj.core.api.Assertions.assertThat;

    /**
     * Wave 0 Nyquist test: V1/V2/V3 migrations apply cleanly; expected tables exist.
     * B-03 fix for VALIDATION.md nyquist_compliant.
     */
    @JdbcTest
    @AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
    @ActiveProfiles("test")
    @Testcontainers
    class InventoryFlywayMigrationsTest {

        @Container
        @ServiceConnection
        static final PostgreSQLContainer<?> POSTGRES =
                new PostgreSQLContainer<>(
                        DockerImageName.parse("pgvector/pgvector:pg16")
                                .asCompatibleSubstituteFor("postgres"))
                        .withDatabaseName("n11")
                        .withUsername("inventory_user")
                        .withPassword("test-password");

        @Autowired
        JdbcTemplate jdbcTemplate;

        @Test
        void v1_processedEventsTableExists() {
            Integer count = jdbcTemplate.queryForObject(
                "SELECT count(*) FROM information_schema.tables " +
                "WHERE table_schema = 'inventory' AND table_name = 'processed_events'",
                Integer.class);
            assertThat(count).isEqualTo(1);
        }

        @Test
        void v2_stockTableExists() {
            Integer count = jdbcTemplate.queryForObject(
                "SELECT count(*) FROM information_schema.tables " +
                "WHERE table_schema = 'inventory' AND table_name = 'stock'",
                Integer.class);
            assertThat(count).isEqualTo(1);
        }

        @Test
        void v2_stockReservationsTableExists() {
            Integer count = jdbcTemplate.queryForObject(
                "SELECT count(*) FROM information_schema.tables " +
                "WHERE table_schema = 'inventory' AND table_name = 'stock_reservations'",
                Integer.class);
            assertThat(count).isEqualTo(1);
        }

        @Test
        void v2_outboxTableExists() {
            Integer count = jdbcTemplate.queryForObject(
                "SELECT count(*) FROM information_schema.tables " +
                "WHERE table_schema = 'inventory' AND table_name = 'outbox'",
                Integer.class);
            assertThat(count).isEqualTo(1);
        }

        @Test
        void v3_stockSeedFiftyTwoRows() {
            Integer count = jdbcTemplate.queryForObject(
                "SELECT count(*) FROM inventory.stock", Integer.class);
            assertThat(count).isEqualTo(52);
        }
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :inventory-service:test --tests "*StockStateComputationTest" --tests "*StockEntityVersionTest" --tests "*InventoryFlywayMigrationsTest" --no-daemon 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    Stock @Version entity compiles; V2 migration has low_stock_threshold + outbox partial index; V3 seed has 52 hardcoded-UUID stock rows; StockStateDto returns Turkish labels "Tükendi"/"Son N ürün!"/"Stokta"; StockStateComputationTest, StockEntityVersionTest, InventoryControllerIntegrationTest, and InventoryFlywayMigrationsTest all exit 0.
  </done>
  <acceptance_criteria>
    - `./gradlew :inventory-service:test --tests '*StockStateComputationTest' --no-daemon` exits 0
    - `./gradlew :inventory-service:test --tests '*StockEntityVersionTest' --no-daemon` exits 0
    - `./gradlew :inventory-service:test --tests '*InventoryControllerIntegrationTest' --no-daemon` exits 0
    - `./gradlew :inventory-service:test --tests '*InventoryFlywayMigrationsTest' --no-daemon` exits 0
    - `grep -c '@Version' inventory-service/src/main/java/com/n11/inventory/stock/Stock.java` equals 1
    - `grep -c 'Stokta' inventory-service/src/main/java/com/n11/inventory/stock/StockStateDto.java` equals 1
    - `grep -c 'low_stock_threshold INT' inventory-service/src/main/resources/db/migration/V2__init_inventory.sql` equals 1
    - `grep -c 'sent_at IS NULL' inventory-service/src/main/resources/db/migration/V2__init_inventory.sql` equals 1
    - `grep -c '00000000-0000-4000-8000-000000000001' inventory-service/src/main/resources/db/migration/V3__seed_stock.sql` equals 1
    - File `inventory-service/src/test/java/com/n11/inventory/migrations/InventoryFlywayMigrationsTest.java` exists
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Saga messaging -- InventoryRabbitConfig (exchanges + queue + DLQ), OrderCreatedConsumer (idempotent @Transactional), OutboxEvent + OutboxRepository + OutboxPoller; ProcessedEvent entity</name>
  <files>
    inventory-service/src/main/java/com/n11/inventory/messaging/InventoryRabbitConfig.java,
    inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java,
    inventory-service/src/main/java/com/n11/inventory/messaging/ProcessedEvent.java,
    inventory-service/src/main/java/com/n11/inventory/messaging/ProcessedEventRepository.java,
    inventory-service/src/main/java/com/n11/inventory/outbox/OutboxEvent.java,
    inventory-service/src/main/java/com/n11/inventory/outbox/OutboxRepository.java,
    inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java
  </files>
  <read_first>
    - identity-service/src/main/java/com/n11/identity/outbox/IdentityRabbitConfig.java (exchange-only bean pattern; InventoryRabbitConfig extends with queue + binding + DLQ declarations on top)
    - identity-service/src/main/java/com/n11/identity/outbox/OutboxEvent.java (copy verbatim, change package only to com.n11.inventory.outbox)
    - identity-service/src/main/java/com/n11/identity/outbox/OutboxRepository.java (FOR UPDATE SKIP LOCKED query -- change schema name from identity to inventory)
    - identity-service/src/main/java/com/n11/identity/outbox/OutboxPoller.java (copy verbatim, change schema name in native query to inventory)
    - identity-service/src/main/java/com/n11/identity/outbox/OutboxBackedUserRegistrationOutboxPublisher.java (Envelope construction pattern with correlationId + causationId wiring)
    - .planning/phases/04-catalog-inventory/04-PATTERNS.md (sections: InventoryRabbitConfig, OrderCreatedConsumer, Transactional Outbox+Poller #3, Processed Events Idempotency Inbox #4)
    - .planning/saga-contracts.md (§2 exchange names, §3 DLQ policy, §5.2 inventory binding, §7 outgoing payload schemas)
    - common-events/src/main/java/ (Envelope record -- import this, do NOT redefine)
    - common-events/src/main/resources/saga-schemas/stock-reserved.schema.json (drift gate schema)
    - common-events/src/main/resources/saga-schemas/stock-reserve-failed.schema.json
  </read_first>
  <behavior>
    OrderCreatedConsumer idempotency (CLAUDE.md Rule #3):
    - First delivery of eventId=E1: processedEventsRepository.existsById(E1) = false; reserve stock; save outbox row (stock.reserved); save processed_events row; commit. Result: 1 stock_reservations row, 1 outbox row, 1 processed_events row.
    - Second delivery of same eventId=E1: processedEventsRepository.existsById(E1) = true; return immediately. No additional rows created.
    - All side effects (stock update, stock_reservations save, outbox save, processed_events save) occur within the SAME @Transactional method -- if any save throws, all roll back.
  </behavior>
  <action>
    **InventoryRabbitConfig.java** -- declare all topology per saga-contracts.md §2+§3+§5.2:
    ```java
    @Configuration
    public class InventoryRabbitConfig {
        public static final String EXCHANGE_INVENTORY_TX  = "inventory.tx";
        public static final String EXCHANGE_ORDERS_TX     = "orders.tx";
        public static final String QUEUE_INVENTORY_ORDERS = "inventory.q.order-created";
        public static final String DLQ_INVENTORY_ORDERS   = "inventory.q.order-created.dlq";

        @Bean public TopicExchange inventoryExchange() {
            return ExchangeBuilder.topicExchange(EXCHANGE_INVENTORY_TX).durable(true).build();
        }
        // Idempotent re-declaration; Phase 5 order-service is the actual owner
        @Bean public TopicExchange ordersExchange() {
            return ExchangeBuilder.topicExchange(EXCHANGE_ORDERS_TX).durable(true).build();
        }
        @Bean public Queue orderCreatedQueue() {
            return QueueBuilder.durable(QUEUE_INVENTORY_ORDERS)
                    .withArgument("x-dead-letter-exchange", "")
                    .withArgument("x-dead-letter-routing-key", DLQ_INVENTORY_ORDERS)
                    .build();
        }
        @Bean public Queue orderCreatedDlq() {
            return QueueBuilder.durable(DLQ_INVENTORY_ORDERS).build();
        }
        @Bean public Binding orderCreatedBinding(Queue orderCreatedQueue, TopicExchange ordersExchange) {
            return BindingBuilder.bind(orderCreatedQueue).to(ordersExchange).with("order.created");
        }
    }
    ```

    **ProcessedEvent.java** -- @Entity @Table(name = "processed_events") mapping to V1 migration table:
    Fields: @Id @Column(name = "event_id") UUID eventId; @Column String consumer; @Column String eventType; @Column Instant processedAt.
    Public constructor: ProcessedEvent(UUID eventId, String consumer, String eventType) sets processedAt=Instant.now().
    Protected no-arg JPA constructor.

    **ProcessedEventRepository.java** -- JpaRepository<ProcessedEvent, UUID>. existsById(UUID) comes from JpaRepository -- no custom queries needed.

    **OutboxEvent.java** -- copy identity-service/outbox/OutboxEvent.java verbatim; change only package from com.n11.identity.outbox to com.n11.inventory.outbox. @Table(name = "outbox") stays unchanged; Flyway schema isolation lands it in inventory schema.

    **OutboxRepository.java** -- copy identity-service analog; change native query schema reference:
    ```java
    @Query(value = "SELECT * FROM inventory.outbox WHERE sent_at IS NULL ORDER BY occurred_at LIMIT :batchSize FOR UPDATE SKIP LOCKED",
           nativeQuery = true)
    List<OutboxEvent> findUnsentBatch(@Param("batchSize") int batchSize);
    ```

    **OutboxPoller.java** -- copy identity-service/outbox/OutboxPoller.java verbatim; change only package to com.n11.inventory.outbox. The formula `event.getAggregate() + ".tx"` produces "inventory.tx" when aggregate="inventory" is stored in the outbox row.

    **OrderCreatedConsumer.java** -- CLAUDE.md Rule #3 idempotent saga consumer:
    Class: @Component, constructor-inject ProcessedEventRepository, StockRepository, StockReservationRepository, OutboxRepository, ObjectMapper.

    Method signature: @RabbitListener(queues = InventoryRabbitConfig.QUEUE_INVENTORY_ORDERS) @Transactional public void handleOrderCreated(Message amqpMessage) throws Exception

    Method body outline:
    1. Deserialize amqpMessage.getBody() to Envelope (from com.n11.events.Envelope)
    2. UUID eventId = UUID.fromString(envelope.eventId())
    3. IDEMPOTENCY CHECK: if (processedEventsRepository.existsById(eventId)) return;
    4. Deserialize envelope.payload() to OrderCreatedPayload (inner record/class with orderId UUID, items List)
    5. Try block -- for each item in payload.items():
       a. Load Stock or throw ProductNotFoundException (produces PRODUCT_NOT_FOUND outbox row)
       b. Check stock.getEffectiveAvailable() >= item.qty() or throw InsufficientStockException
       c. stock.reserve(item.qty()); stockRepository.save(stock) [ObjectOptimisticLockingFailureException caught below]
       d. reservationRepository.save(new StockReservation(UUID.randomUUID(), payload.orderId(), item.productId(), item.qty()))
    6. On success: build Envelope{eventType="stock.reserved", correlationId=envelope.correlationId(), causationId=envelope.eventId(), producer="inventory-service", payload=stockReservedPayload}; save OutboxEvent{aggregate="inventory", eventType="stock.reserved"}
    7. Catch InsufficientStockException / ProductNotFoundException: save outbox row with eventType="stock.reserve_failed", reason=INSUFFICIENT_STOCK or PRODUCT_NOT_FOUND
    8. Catch ObjectOptimisticLockingFailureException: save outbox row with reason=RESERVATION_CONFLICT
    9. ALWAYS (success or exception): processedEventsRepository.save(new ProcessedEvent(eventId, "OrderCreatedConsumer", envelope.eventType()))

    Define inner private records for OrderCreatedPayload, StockReservedPayload, StockReserveFailedPayload, ReservedItem, FailedItem within the same file. Keep all within the messaging package scope.
    ProductNotFoundException is an unchecked exception similar to InsufficientStockException.
  </action>
  <verify>
    <automated>./gradlew :inventory-service:compileJava --no-daemon 2>&amp;1 | tail -5 &amp;&amp; grep -c 'processedEventsRepository.existsById' inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java &amp;&amp; grep -c 'fixedDelay = 5000' inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java</automated>
  </verify>
  <done>
    OrderCreatedConsumer compiles with processedEventsRepository.existsById idempotency check; DLQ wiring x-dead-letter-routing-key present in InventoryRabbitConfig; OutboxPoller has fixedDelay=5000; OutboxRepository queries inventory.outbox schema; RESERVATION_CONFLICT handled in consumer catch block; ./gradlew :inventory-service:compileJava exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c 'processedEventsRepository.existsById' inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java` equals 1
    - `grep -c 'x-dead-letter-routing-key' inventory-service/src/main/java/com/n11/inventory/messaging/InventoryRabbitConfig.java` equals 1
    - `grep -c 'inventory.q.order-created.dlq' inventory-service/src/main/java/com/n11/inventory/messaging/InventoryRabbitConfig.java` equals 1
    - `grep -c 'fixedDelay = 5000' inventory-service/src/main/java/com/n11/inventory/outbox/OutboxPoller.java` equals 1
    - `grep -c 'inventory.outbox' inventory-service/src/main/java/com/n11/inventory/outbox/OutboxRepository.java` equals 1
    - `grep -c 'RESERVATION_CONFLICT' inventory-service/src/main/java/com/n11/inventory/messaging/OrderCreatedConsumer.java` equals 1
    - `./gradlew :inventory-service:compileJava --no-daemon` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test -- OrderCreatedConsumerIntegrationTest proving CLAUDE.md Rule #3 idempotency</name>
  <files>
    inventory-service/src/test/java/com/n11/inventory/messaging/OrderCreatedConsumerIntegrationTest.java
  </files>
  <read_first>
    - identity-service/src/test/java/com/n11/identity/outbox/OutboxIntegrationTest.java (@SpringBootTest + @ServiceConnection + @Testcontainers + Awaitility -- primary analog)
    - identity-service/src/test/resources/application-test.yml (override shape -- Task 2 already created inventory analog)
    - .planning/phases/04-catalog-inventory/04-PATTERNS.md (section: SpringBootTest Full Integration Test Pattern #7)
    - .planning/phases/04-catalog-inventory/04-VALIDATION.md (row 4-02-03 -- test class name and gradle filter must match exactly)
    - .planning/saga-contracts.md (§1 envelope shape, §5.2 routing key order.created)
    - common-events/src/main/resources/saga-schemas/stock-reserved.schema.json (networknt drift gate)
  </read_first>
  <behavior>
    publishOrderCreatedTwice_assertSingleSideEffect:
    - Seed Stock(productId=P1, availableQty=100)
    - Build and serialize Envelope{eventId=E1, eventType="order.created", payload={orderId=O1, items=[{productId=P1, qty=5}]}}
    - rabbitTemplate.convertAndSend("orders.tx", "order.created", envelopeJson)
    - Awaitility.await().atMost(15, SECONDS).until(() -> processedEventsCount(E1) == 1)
    - Publish same Envelope{eventId=E1} again
    - Thread.sleep(2000) or Awaitility for 3s stable
    - Assert processedEventsCount(E1) == 1 (no duplicate)
    - Assert stockReservationsCount(orderId=O1, productId=P1) == 1
    - Assert outboxCount(eventType="stock.reserved") == 1

    stockReserveFailed_whenInsufficientStock:
    - Seed Stock(productId=P2, availableQty=2)
    - Publish order with qty=10 for P2
    - Awaitility until outbox has row with event_type="stock.reserve_failed"
    - Assert outbox payload contains "INSUFFICIENT_STOCK"

    outboxPayloadMatchesStockReservedSchema (drift gate):
    - After publishOrderCreatedTwice test, load outbox row for stock.reserved from DB
    - Parse payload JSON, extract envelope.payload node
    - Validate against classpath:/saga-schemas/stock-reserved.schema.json via networknt JsonSchemaFactory
    - Assert errors.isEmpty()
  </behavior>
  <action>
    Test class name must be exactly OrderCreatedConsumerIntegrationTest (VALIDATION.md row 4-02-03).

    Annotations: @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.NONE) @ActiveProfiles("test") @Testcontainers

    Containers:
    ```java
    @Container @ServiceConnection
    static final PostgreSQLContainer<?> POSTGRES = new PostgreSQLContainer<>(
            DockerImageName.parse("pgvector/pgvector:pg16").asCompatibleSubstituteFor("postgres"))
            .withDatabaseName("n11").withUsername("inventory_user").withPassword("test-password");

    @Container @ServiceConnection
    static final RabbitMQContainer RABBIT =
            new RabbitMQContainer(DockerImageName.parse("rabbitmq:4.0-management"));
    ```

    @DynamicPropertySource:
    ```java
    registry.add("spring.flyway.user", POSTGRES::getUsername);
    registry.add("spring.flyway.password", POSTGRES::getPassword);
    registry.add("spring.flyway.schemas", () -> "inventory");
    registry.add("spring.flyway.default-schema", () -> "inventory");
    registry.add("spring.flyway.create-schemas", () -> "true");
    registry.add("spring.flyway.placeholders.schema", () -> "inventory");
    registry.add("spring.flyway.placeholders.flyway.schema", () -> "inventory");
    ```

    Autowire: @Autowired RabbitTemplate rabbitTemplate; @Autowired StockRepository stockRepository; @Autowired JdbcTemplate jdbcTemplate; @Autowired ObjectMapper objectMapper.

    Helper methods:
    - buildOrderCreatedEnvelope(UUID eventId, UUID orderId, UUID productId, int qty): builds Envelope record + serializes to JSON String
    - int countProcessedEvents(UUID eventId): jdbcTemplate.queryForObject("SELECT count(*) FROM inventory.processed_events WHERE event_id = ?", Integer.class, eventId)
    - int countReservations(UUID orderId, UUID productId): jdbcTemplate.queryForObject("SELECT count(*) FROM inventory.stock_reservations WHERE order_id = ? AND product_id = ?", ...)
    - int countOutbox(String eventType): jdbcTemplate.queryForObject("SELECT count(*) FROM inventory.outbox WHERE event_type = ?", ...)

    Use `import static org.awaitility.Awaitility.await; import java.util.concurrent.TimeUnit.SECONDS;`

    networknt drift gate imports:
    ```java
    import com.networknt.schema.JsonSchema;
    import com.networknt.schema.JsonSchemaFactory;
    import com.networknt.schema.SpecVersion;
    import com.networknt.schema.ValidationMessage;
    ```

    Run command per VALIDATION.md: `./gradlew :inventory-service:test --tests '*OrderCreatedConsumerIntegrationTest' --no-daemon`
  </action>
  <verify>
    <automated>./gradlew :inventory-service:test --tests "*OrderCreatedConsumerIntegrationTest" --no-daemon 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    OrderCreatedConsumerIntegrationTest exits 0 with all three test methods green (idempotency proof, insufficient-stock path, JSON schema drift gate); Awaitility present; full module test suite ./gradlew :inventory-service:test exits 0.
  </done>
  <acceptance_criteria>
    - `./gradlew :inventory-service:test --tests '*OrderCreatedConsumerIntegrationTest' --no-daemon` exits 0
    - Test method publishOrderCreatedTwice_assertSingleSideEffect present and green
    - Test method stockReserveFailed_whenInsufficientStock present and green
    - Test method outboxPayloadMatchesStockReservedSchema present and green
    - `grep -c 'Awaitility' inventory-service/src/test/java/com/n11/inventory/messaging/OrderCreatedConsumerIntegrationTest.java` equals 1
    - `./gradlew :inventory-service:test --no-daemon` exits 0 (all four test classes green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ -> OrderCreatedConsumer | Untrusted event payload; malformed JSON must not crash consumer or corrupt stock |
| HTTP -> StockController | Public endpoint; productId path param must be validated as UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | OrderCreatedConsumer payload deserialization | mitigate | Wrap objectMapper.readValue in try-catch; malformed payload routes to DLQ via AMQP default requeue=false after 3 retries |
| T-04-02-02 | Denial of Service | StockController GET /inventory/{productId} | accept | Public read-only endpoint; no PII; rate limiting deferred to api-gateway layer |
| T-04-02-03 | Tampering | Optimistic lock bypass via repeated AMQP retry | mitigate | Catch ObjectOptimisticLockingFailureException in consumer; publish RESERVATION_CONFLICT outbox event; do not re-throw (prevents infinite AMQP retry loop) |
| T-04-02-04 | Information Disclosure | processed_events table exposed via actuator/env | accept | Contains only event UUIDs and types (no PII); inventory schema isolated; actuator env endpoint not exposed (api-gateway.yml management.endpoints.web.exposure.include does not include env) |
</threat_model>

<verification>
Full module verify:
```
./gradlew :inventory-service:test --no-daemon
```
Expected: all 4 test classes green (StockStateComputationTest, StockEntityVersionTest, InventoryControllerIntegrationTest, OrderCreatedConsumerIntegrationTest).

Cross-schema isolation check:
```
grep -r "product\.products\|product_user\|product schema" inventory-service/src/ 2>/dev/null
```
Expected: 0 matches.

Compile + image:
```
./gradlew :inventory-service:jibDockerBuild --no-daemon
docker images | grep n11/inventory-service
```
Expected: n11/inventory-service:dev image present.

Config verify:
```
grep -c 'default-schema: inventory' config-server/src/main/resources/config/inventory-service.yml
grep -c 'port: 8083' config-server/src/main/resources/config/inventory-service.yml
```
Both expected: 1.
</verification>

<success_criteria>
- ./gradlew :inventory-service:test --no-daemon exits 0; 4 test classes, all green
- OrderCreatedConsumerIntegrationTest re-delivery test asserts exactly 1 processed_events row on duplicate event (CLAUDE.md Rule #3 proof)
- GET /inventory/{productId} returns StockStateDto with stockStateLabel "Stokta", "Tükendi", or "Son N ürün!"
- grep -c '@Version' inventory-service/src/main/java/com/n11/inventory/stock/Stock.java equals 1
- No host ports mapped for inventory-service in docker-compose.yml (Pitfall #14)
- config-server/src/main/resources/config/inventory-service.yml has default-schema: inventory and port: 8083
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalog-inventory/04-02-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
