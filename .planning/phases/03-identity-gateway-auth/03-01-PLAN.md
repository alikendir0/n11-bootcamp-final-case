---
phase: 03-identity-gateway-auth
plan: 01
plan_id: 03-01
type: execute
wave: 0
depends_on: []
files_modified:
  - identity-service/build.gradle.kts
  - identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java
  - identity-service/src/main/resources/application.yml
  - identity-service/src/main/resources/logback-spring.xml
  - settings.gradle.kts
  - docker-compose.yml
  - .env.example
  - identity-service/README.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
  - AUTH-07
  - QUAL-02
must_haves:
  truths:
    - "`./gradlew projects` lists `identity-service` as a subproject after the include is added"
    - "`./gradlew :identity-service:compileJava` succeeds with the cloned skeleton sources"
    - "`docker compose config` parses cleanly with the `identity-service` block present and zero host-port mappings (Pitfall #14)"
    - "`.env.example` declares JWT_PRIVATE_KEY, JWT_KEY_ID, ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD_HASH placeholder slots, and the existing IDENTITY_DB_PASSWORD line is unchanged"
    - "D-21: identity-service is the FIRST business service — clones service-template/skeleton/ via Plan 01-07 archetype, listens on port 8081 (gateway is 8080; convention 808x for business services)"
    - "D-22: per-service config-server YAML pins db.user=identity_user, db.password=${IDENTITY_DB_PASSWORD}, flyway.schema=identity (identity_user already created by Plan 01-03 init.sh)"
  artifacts:
    - path: identity-service/build.gradle.kts
      provides: "identity-service module Gradle build (clones service-template) with oauth2-resource-server + security starters"
      contains: "spring-boot-starter-oauth2-resource-server"
    - path: identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java
      provides: "Spring Boot main class with @EnableScheduling for outbox poller"
      contains: "@EnableScheduling"
    - path: identity-service/src/main/resources/application.yml
      provides: "spring.application.name=identity-service + spring.config.import to config-server (production posture, no `optional:`)"
      contains: "name: identity-service"
    - path: settings.gradle.kts
      provides: "Root settings includes the identity-service subproject"
      contains: '"identity-service"'
    - path: docker-compose.yml
      provides: "identity-service container block with port 8081 internal-only, env_file .env, healthy depends_on chain"
      contains: "n11/identity-service:dev"
    - path: .env.example
      provides: "Placeholder slots for JWT_PRIVATE_KEY (multiline PEM), JWT_KEY_ID, ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD_HASH"
      contains: "JWT_PRIVATE_KEY="
    - path: identity-service/README.md
      provides: "Per-service env-var matrix + RSA keypair generation runbook (`openssl genrsa 2048`) + bcrypt hash generation runbook (`htpasswd -bnBC 10`)"
      contains: "openssl genrsa 2048"
  key_links:
    - from: settings.gradle.kts
      to: identity-service/build.gradle.kts
      via: "Gradle subproject include"
      pattern: '"identity-service"'
    - from: docker-compose.yml
      to: identity-service/build.gradle.kts
      via: "Jib image n11/identity-service:dev produced by `./gradlew :identity-service:jibDockerBuild`"
      pattern: "image: n11/identity-service:dev"
    - from: .env.example
      to: identity-service/README.md
      via: "Env-var matrix documents how to populate each placeholder"
      pattern: "JWT_PRIVATE_KEY"
---

<objective>
Stand up the identity-service Gradle module by cloning service-template/skeleton/, register it in settings.gradle.kts, add the docker-compose service block, and seed the env-var matrix (.env.example + README runbook). This plan does NOT add the JWT keypair beans or business code — only the module shell and orchestration. Subsequent plans (03-02, 03-03) build the schema and code on this foundation.

Purpose: Phase 3's first task is "go from 0 services using service-template/skeleton/ to 1 service using it." This plan walks through that clone procedure end-to-end and documents any rough edges so Phase 4+ (product-service, inventory-service, cart-service, …) hits a frictionless path. The clone procedure lives in `service-template/skeleton/README.md` lines 24-50 — follow it verbatim, then add the identity-specific dependency overrides (oauth2-resource-server, security, testcontainers).

Output: A compilable identity-service module that boots through Spring Boot to the point of failing on a missing JWT keypair env var (which is fine — Plan 03-03 wires the keypair). A docker-compose entry that pulls n11/identity-service:dev. An updated .env.example with all four new placeholder slots. A README runbook for the operator (the candidate) to generate real values for those slots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-identity-gateway-auth/03-CONTEXT.md
@.planning/phases/03-identity-gateway-auth/03-RESEARCH.md
@.planning/phases/03-identity-gateway-auth/03-PATTERNS.md
@.planning/phases/03-identity-gateway-auth/03-VALIDATION.md
@.planning/api-contracts.md
@service-template/skeleton/README.md
@service-template/build.gradle.kts
@settings.gradle.kts
@docker-compose.yml
@.env.example

<interfaces>
<!-- Critical contracts and ports the executor must honor -->

From service-template/skeleton/README.md (clone procedure):
- `<SERVICE_NAME>` token → `identity-service`
- `__SERVICE_PACKAGE__` token → `identity`
- Skeleton tree lives at `service-template/skeleton/src-main/` (NOT `src/main`)
- After clone, drop the `.template` suffix from every file via `find ... -name '*.template' | mv`

From service-template/build.gradle.kts (canonical analog — clone-and-edit source):
- Ships: spring-boot-starter-{web,actuator,validation,data-jpa,amqp}, springdoc-openapi-starter-webmvc-ui:2.8.17, flyway-core:12.5.0 + flyway-database-postgresql:12.5.0, postgresql, logstash-logback-encoder:8.0
- Project deps: :common-error, :common-logging, :common-events
- Test: spring-boot-starter-test + junit-platform-launcher (testRuntimeOnly)

From docker-compose.yml lines 172-194 (api-gateway block — analog for new identity-service block):
- env_file pattern not used in api-gateway — but identity-service NEEDS env_file: .env to inject JWT_PRIVATE_KEY, ADMIN_SEED_*, IDENTITY_DB_PASSWORD
- depends_on chain: postgres + rabbitmq + eureka-server + config-server (all condition: service_healthy)
- Healthcheck idiom: `wget -q -O- http://localhost:<PORT>/actuator/health | grep -q '"status":"UP"'`
- restart: unless-stopped, networks: n11-net

Port allocation (per ARCHITECTURE.md 808x convention + D-21):
- 8080 = api-gateway (existing)
- 8081 = identity-service (THIS plan, internal-only — NO `ports:` mapping)
- 8761 = eureka-server, 8888 = config-server (existing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clone service-template/skeleton/ into identity-service module</name>
  <files>identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java, identity-service/src/main/java/com/n11/identity/health/SampleHealthController.java, identity-service/src/main/resources/application.yml, identity-service/src/main/resources/logback-spring.xml, identity-service/src/main/resources/db/migration/V1__init_processed_events.sql</files>
  <read_first>
    - service-template/skeleton/README.md (clone procedure, full)
    - service-template/skeleton/src-main/java/com/n11/__SERVICE_PACKAGE__/ServiceApplication.java.template
    - service-template/skeleton/src-main/java/com/n11/__SERVICE_PACKAGE__/health/SampleHealthController.java.template
    - service-template/skeleton/src-main/resources/application.yml.template
    - service-template/skeleton/src-main/resources/logback-spring.xml.template
    - service-template/skeleton/src-main/resources/db/migration/V1__init_processed_events.sql.template
    - .planning/phases/03-identity-gateway-auth/03-PATTERNS.md (sections: IdentityServiceApplication.java, application.yml, logback-spring.xml)
  </read_first>
  <action>
    Execute the canonical clone procedure verbatim from service-template/skeleton/README.md (steps 1-4 of the bash block):

    ```bash
    SERVICE_NAME=identity-service
    SERVICE_PACKAGE=identity

    # Step 1: copy skeleton tree
    mkdir -p ${SERVICE_NAME}/src
    cp -r service-template/skeleton/src-main ${SERVICE_NAME}/src/main

    # Step 2: rename placeholder package directory
    mv ${SERVICE_NAME}/src/main/java/com/n11/__SERVICE_PACKAGE__ \
       ${SERVICE_NAME}/src/main/java/com/n11/${SERVICE_PACKAGE}

    # Step 3: drop the .template suffix from every file
    find ${SERVICE_NAME}/src/main -type f -name '*.template' | while read f; do
      mv "$f" "${f%.template}"
    done

    # Step 4: substitute placeholder tokens in file contents
    find ${SERVICE_NAME}/src/main -type f \( -name '*.java' -o -name '*.yml' -o -name '*.xml' -o -name '*.sql' \) -exec \
      sed -i \
        -e "s/<SERVICE_NAME>/${SERVICE_NAME}/g" \
        -e "s/__SERVICE_PACKAGE__/${SERVICE_PACKAGE}/g" \
      {} \;
    ```

    After the clone, MANUALLY edit `identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java`:
    - The class will currently be named `ServiceApplication` after token substitution (the skeleton name is not tokenized). Rename the class to `IdentityServiceApplication` and update the inner `SpringApplication.run(...)` reference. File name should be `IdentityServiceApplication.java`.
    - Add `@EnableScheduling` annotation on the class (required by Plan 03-05's OutboxPoller). Imports: `org.springframework.scheduling.annotation.EnableScheduling`.
    - Final shape:
      ```java
      package com.n11.identity;

      import org.springframework.boot.SpringApplication;
      import org.springframework.boot.autoconfigure.SpringBootApplication;
      import org.springframework.cloud.client.discovery.EnableDiscoveryClient;
      import org.springframework.scheduling.annotation.EnableScheduling;

      @SpringBootApplication(scanBasePackages = "com.n11")
      @EnableDiscoveryClient
      @EnableScheduling
      public class IdentityServiceApplication {
          public static void main(String[] args) {
              SpringApplication.run(IdentityServiceApplication.class, args);
          }
      }
      ```

    Production posture for application.yml — strip `optional:` from the `spring.config.import` line per skeleton README "Production posture" section. Final content of `identity-service/src/main/resources/application.yml`:
    ```yaml
    spring:
      application:
        name: identity-service
      config:
        import: configserver:http://config-server:8888?fail-fast=true&max-attempts=10&max-interval=1500&multiplier=1.2&initial-interval=1100
    ```

    Leave `logback-spring.xml` and `db/migration/V1__init_processed_events.sql` UNTOUCHED post-substitution. The skeleton already contains the right correlationId MDC encoder (`<includeMdcKeyName>correlationId</includeMdcKeyName>`) and the processed_events DDL inherited per-service per CONTEXT D-22 / saga-contracts.md §5.2.

    DO NOT delete `health/SampleHealthController.java` — keep the smoke endpoint active during Phase 3. (Clean-up of the demo controller can happen at Phase 11; Phase 3 keeps it as the actuator-independent liveness check.)
  </action>
  <verify>
    <automated>test -f identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java &amp;&amp; test -f identity-service/src/main/resources/application.yml &amp;&amp; test -f identity-service/src/main/resources/logback-spring.xml &amp;&amp; test -f identity-service/src/main/resources/db/migration/V1__init_processed_events.sql &amp;&amp; ! find identity-service/src/main -name '*.template' | grep -q . &amp;&amp; ! grep -r '__SERVICE_PACKAGE__\|<SERVICE_NAME>' identity-service/src/main</automated>
  </verify>
  <acceptance_criteria>
    - File `identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java` exists (class renamed from ServiceApplication)
    - `grep -c '@EnableScheduling' identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java` → 1
    - `grep -c 'public class IdentityServiceApplication' identity-service/src/main/java/com/n11/identity/IdentityServiceApplication.java` → 1
    - `find identity-service/src/main -name '*.template' | wc -l` → 0
    - `grep -r '__SERVICE_PACKAGE__\|<SERVICE_NAME>' identity-service/src/main` returns nothing
    - File `identity-service/src/main/resources/application.yml` contains `name: identity-service`
    - `grep -c '^\s*import: optional:' identity-service/src/main/resources/application.yml` → 0 (production posture)
    - `grep -c '^\s*import: configserver:' identity-service/src/main/resources/application.yml` → 1
    - File `identity-service/src/main/resources/db/migration/V1__init_processed_events.sql` exists
    - `grep -c 'CREATE TABLE processed_events' identity-service/src/main/resources/db/migration/V1__init_processed_events.sql` → 1
    - `grep -c '<includeMdcKeyName>correlationId</includeMdcKeyName>' identity-service/src/main/resources/logback-spring.xml` → 1
    - File `identity-service/src/main/java/com/n11/identity/health/SampleHealthController.java` exists (smoke endpoint preserved)
  </acceptance_criteria>
  <done>
    The skeleton clone is complete: package directory renamed, .template suffixes dropped, both placeholder tokens substituted, IdentityServiceApplication class boots Spring Boot, processed_events inbox migration in place. Code does not yet compile because no build.gradle.kts exists (Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author identity-service/build.gradle.kts and update settings.gradle.kts</name>
  <files>identity-service/build.gradle.kts, settings.gradle.kts</files>
  <read_first>
    - service-template/build.gradle.kts (analog — clone-and-edit source, full file)
    - settings.gradle.kts (current root settings — must add the new include)
    - .planning/phases/03-identity-gateway-auth/03-PATTERNS.md (section: identity-service/build.gradle.kts adaptation notes)
    - .planning/phases/03-identity-gateway-auth/03-RESEARCH.md (section: identity-service Dependencies — `spring-boot-starter-oauth2-resource-server` rationale)
  </read_first>
  <action>
    Create `identity-service/build.gradle.kts` by copying `service-template/build.gradle.kts` verbatim and applying these specific additions/changes:

    1. Add the Spring Boot Jib plugin to the `plugins { }` block (gateway/eureka/config-server pattern from Plan 01-05/01-06):
       ```kotlin
       plugins {
           id("org.springframework.boot")
           id("com.google.cloud.tools.jib")
       }
       ```

    2. Add THESE three implementation lines into the existing `dependencies { }` block (placement: alphabetical within the Boot core block):
       ```kotlin
       // JWT signing (NimbusJwtEncoder, JwtClaimsSet, JwsHeader from spring-security-oauth2-jose).
       // Identity-service does NOT validate JWTs (D-15: gateway is the only validator) — but the
       // starter ships the encoder classes and we need them.
       implementation("org.springframework.boot:spring-boot-starter-oauth2-resource-server")

       // Spring Security — provides BCryptPasswordEncoder + servlet SecurityFilterChain (D-16, AUTH-07)
       implementation("org.springframework.boot:spring-boot-starter-security")
       ```

    3. Add Testcontainers for Plan 03-04/03-05 integration tests:
       ```kotlin
       testImplementation("org.springframework.boot:spring-boot-testcontainers")
       testImplementation("org.testcontainers:postgresql")
       testImplementation("org.testcontainers:rabbitmq")
       ```

    4. Change the bootJar archiveBaseName from `service-template` to `identity-service`:
       ```kotlin
       tasks.named<org.springframework.boot.gradle.tasks.bundling.BootJar>("bootJar") {
           archiveBaseName.set("identity-service")
       }
       ```

    5. Add a `jib { }` block at the end of the file (matches eureka-server / config-server / api-gateway pattern; see existing api-gateway/build.gradle.kts lines 79-91):
       ```kotlin
       jib {
           from {
               image = "eclipse-temurin:21-jre"
           }
           to {
               image = "n11/identity-service:dev"
           }
           container {
               ports = listOf("8081")
               jvmFlags = listOf("-XX:+UseG1GC", "-XX:+UseContainerSupport", "-XX:MaxRAMPercentage=75.0")
           }
       }
       ```

    Then modify `settings.gradle.kts` — INSERT `"identity-service",` into the `include(...)` block alphabetically between `"common-events"` and `"infra-tests"`:
    ```kotlin
    include(
        "eureka-server",
        "config-server",
        "api-gateway",
        "common-error",
        "common-logging",
        "common-events",
        "identity-service",
        "service-template",
        "infra-tests"
    )
    ```

    DO NOT touch the `dependencyResolutionManagement` block — it stays convention-only.
  </action>
  <verify>
    <automated>./gradlew :identity-service:compileJava --no-daemon 2>&amp;1 | tee /tmp/03-01-T2-compile.log; ./gradlew projects --no-daemon 2>&amp;1 | grep -c 'identity-service'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"identity-service"' settings.gradle.kts` → 1 (added to include block)
    - `grep -c 'spring-boot-starter-oauth2-resource-server' identity-service/build.gradle.kts` → 1
    - `grep -c 'spring-boot-starter-security' identity-service/build.gradle.kts` → 1
    - `grep -c 'spring-boot-testcontainers' identity-service/build.gradle.kts` → 1
    - `grep -c 'org.testcontainers:postgresql' identity-service/build.gradle.kts` → 1
    - `grep -c 'archiveBaseName.set("identity-service")' identity-service/build.gradle.kts` → 1
    - `grep -c 'n11/identity-service:dev' identity-service/build.gradle.kts` → 1
    - `grep -c 'id("com.google.cloud.tools.jib")' identity-service/build.gradle.kts` → 1
    - `./gradlew :identity-service:compileJava --no-daemon` exits 0
    - `./gradlew projects --no-daemon | grep -c "identity-service"` → at least 1
  </acceptance_criteria>
  <done>
    Identity-service is a recognized Gradle subproject. `./gradlew :identity-service:compileJava` succeeds (the cloned skeleton sources are minimal — health controller + main app — and require no JWT or domain code yet). Jib block is wired so Plan 03-06 can run `./gradlew :identity-service:jibDockerBuild`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add identity-service block to docker-compose.yml + update .env.example + README runbook</name>
  <files>docker-compose.yml, .env.example, identity-service/README.md</files>
  <read_first>
    - docker-compose.yml (lines 100-200 covering eureka-server / config-server / api-gateway blocks — analog patterns)
    - .env.example (current file — must extend without breaking existing IDENTITY_DB_PASSWORD)
    - .planning/phases/03-identity-gateway-auth/03-PATTERNS.md (section: docker-compose.yml MODIFY — additive)
    - .planning/phases/03-identity-gateway-auth/03-RESEARCH.md (sections: Pitfall #6 mitigation, Pitfall #14 mitigation)
    - CLAUDE.md (rule 5: no secrets in source)
  </read_first>
  <action>
    **Step 1 — docker-compose.yml additive merge.**
    INSERT a new `identity-service:` block under the `services:` map. Place it AFTER the existing `api-gateway:` block (lines ~172-194) and BEFORE the `volumes:` section. Do NOT rewrite the file — read it, find the position, write the new block. Use this EXACT YAML:

    ```yaml
      # ---------------------------------------------------------------------------
      # identity-service -- first business service (Phase 3, D-21).
      # Port 8081 (gateway is 8080; 808x convention per ARCHITECTURE.md).
      # Image built by: ./gradlew :identity-service:jibDockerBuild
      #
      # IMPORTANT: NO `ports:` mapping -- Pitfall #14 (gateway path validation /
      # direct-service bypass). Identity-service is reachable ONLY through the
      # gateway on port 8080 from outside the Docker network.
      # ---------------------------------------------------------------------------
      identity-service:
        image: n11/identity-service:dev
        container_name: n11-identity-service
        env_file:
          - .env
        environment:
          SPRING_CONFIG_IMPORT: configserver:http://config-server:8888?fail-fast=true&max-attempts=10&max-interval=1500&multiplier=1.2&initial-interval=1100
          EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
          SPRING_PROFILES_ACTIVE: docker
          # Plan 03-02 V2 admin-seed migration consumes ADMIN_SEED_EMAIL +
          # ADMIN_SEED_PASSWORD_HASH (loaded from .env via env_file above).
          # Plan 03-03 JwtConfig consumes JWT_PRIVATE_KEY + JWT_KEY_ID (same source).
        depends_on:
          postgres:
            condition: service_healthy
          rabbitmq:
            condition: service_healthy
          eureka-server:
            condition: service_healthy
          config-server:
            condition: service_healthy
        healthcheck:
          test: ["CMD-SHELL", "wget -q -O- http://localhost:8081/actuator/health | grep -q '\"status\":\"UP\"'"]
          interval: 5s
          timeout: 3s
          retries: 6
          start_period: 20s    # Flyway migration adds ~5s vs infra services
        restart: unless-stopped
        networks:
          - n11-net
    ```

    Verify additive shape: `grep -c '^  identity-service:' docker-compose.yml` MUST equal exactly 1 (single insertion, no duplication).

    **Step 2 — .env.example additions.**
    Add a new section to `.env.example` under the existing "Phase 3+ (placeholder slots)" comment block. REPLACE the current commented-out `# JWT_SIGNING_KEY=` line (which referenced the wrong env-var name) with the real Phase 3 keys. Keep ALL other existing lines (IDENTITY_DB_PASSWORD etc.) untouched.

    Insert these lines after the existing `# Phase 3+ (placeholder slots)` comment:

    ```
    # ── Phase 3 (Identity service) ──────────────────────────────────────
    # JWT keypair (D-02) -- RS256 private key in PEM PKCS#8 format.
    # Generate with: openssl genrsa 2048 | tee private.pem (then paste contents below
    # with literal \n line separators OR use docker-compose env_file multi-line shape).
    # NEVER commit private.pem; .gitignore covers *.pem.
    # JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----"
    # JWT_KEY_ID=n11-jwt-2026-04
    JWT_PRIVATE_KEY=
    JWT_KEY_ID=n11-jwt-2026-04

    # Admin seed (D-06) -- one-time bootstrap user inserted by Flyway V2 migration.
    # Generate hash with: htpasswd -bnBC 10 "" "your-admin-password" | tr -d ':\n'
    # ADMIN_SEED_PASSWORD_HASH must be the bcrypt $2a$10$... shape.
    ADMIN_SEED_EMAIL=admin@n11-demo.local
    ADMIN_SEED_PASSWORD_HASH=
    ```

    Also DELETE the now-obsolete commented-out `# JWT_SIGNING_KEY=` line at the bottom of `.env.example` (it referenced a key name we are not using — the right variable is `JWT_PRIVATE_KEY`).

    **Step 3 — Author identity-service/README.md.**
    Create the per-service README with the env-var matrix and operator runbooks. Use this EXACT content as the floor (extend if useful):

    ```markdown
    # identity-service

    First business service in the n11-clone microservices fleet. Issues RS256 JWTs, serves JWKS, manages user signup/login, address book, and publishes the `user.registered` saga event via the transactional outbox.

    Phase: 3 — Identity + Gateway Auth.

    ## Endpoints (api-contracts.md §1)

    | Verb | Path | Auth | Purpose |
    |------|------|------|---------|
    | POST | /auth/register | public | Create user (email + password). AUTH-01. |
    | POST | /auth/login | public | Issue 24h RS256 JWT. AUTH-02. |
    | GET | /auth/me | gateway-injected X-User-Id | Return user profile. AUTH-03. |
    | GET | /addresses | gateway-injected X-User-Id | List user's saved Türkiye addresses. AUTH-08. |
    | POST | /addresses | gateway-injected X-User-Id | Add address. AUTH-08. |
    | GET | /.well-known/jwks.json | public | JWKS for gateway validation. AUTH-04. |

    Identity-service does NOT validate JWTs itself (D-15). All endpoint security is the gateway's job.

    ## Required environment variables (.env)

    | Variable | Source | Purpose |
    |----------|--------|---------|
    | `IDENTITY_DB_PASSWORD` | random per-host secret | Postgres password for identity_user (created by infra/postgres/init.sh) |
    | `JWT_PRIVATE_KEY` | `openssl genrsa 2048` | RS256 private key (PEM PKCS#8). Public key derived at boot. NEVER commit. |
    | `JWT_KEY_ID` | author-chosen stable ID | JWK `kid` field (e.g. `n11-jwt-2026-04`). Bump on rotation. |
    | `ADMIN_SEED_EMAIL` | author-chosen | Email of the one-time seeded ROLE_ADMIN user. |
    | `ADMIN_SEED_PASSWORD_HASH` | `htpasswd -bnBC 10 "" "<plaintext>"` | BCrypt cost-10 hash of the admin password. |

    ## Operator runbooks

    ### Generate the JWT RS256 keypair (one-time per deployment)

    ```bash
    # Generate a fresh PEM PKCS#8 RSA private key (always emits CRT form per Risk 4 in 03-RESEARCH.md).
    openssl genrsa 2048 > private.pem

    # Single-line escape for .env (replace real newlines with literal \n):
    awk 'NR>1{printf "\\n"} {printf "%s", $0}' private.pem
    # Paste the output as JWT_PRIVATE_KEY="..." in .env (note the surrounding quotes).
    ```

    Verify the .env value loads at boot: identity-service logs `Loaded RSA keypair, kid=n11-jwt-2026-04` (Plan 03-03 wires this log line).

    ### Generate the admin bcrypt hash (one-time per deployment)

    ```bash
    # On Linux / macOS (htpasswd from apache2-utils / httpd-tools):
    htpasswd -bnBC 10 "" "your-admin-password" | tr -d ':\n'
    # Output starts with $2y$10$... -- but identity-service's BCryptPasswordEncoder
    # accepts $2a/$2b/$2y interchangeably. Paste the value as ADMIN_SEED_PASSWORD_HASH.
    ```

    ### Pre-compose Jib build (per Plan 01-05/01-06 pattern)

    ```bash
    ./gradlew :identity-service:jibDockerBuild
    docker compose up -d identity-service
    ```

    ## Pitfall tripwires

    - **Pitfall #6** — JWT private key is env-only; `.gitignore` covers `*.pem`; gitleaks runs in CI.
    - **Pitfall #14** — identity-service has NO `ports:` mapping in docker-compose.yml; reachable only through the gateway (port 8080).
    - **Pitfall #18** — clock skew: gateway uses `JwtTimestampValidator(Duration.ofSeconds(30))`. On WSL2 demos, run `wsl --shutdown` before the demo to reset host clock drift.

    ## Test pattern

    Smoke unit test (D-16, QUAL-02 starter): `./gradlew :identity-service:test --tests "*PasswordEncoderTest"`. Integration tests (Plan 03-04, 03-05): `./gradlew :identity-service:test`.
    ```
  </action>
  <verify>
    <automated>docker compose config &gt; /dev/null 2&gt;&amp;1 &amp;&amp; test "$(grep -c '^  identity-service:' docker-compose.yml)" -eq 1 &amp;&amp; ! grep -q 'ports:' &lt;&lt;&lt;"$(awk '/^  identity-service:/,/^  [a-z]/' docker-compose.yml | head -50)" &amp;&amp; grep -q '^JWT_PRIVATE_KEY=' .env.example &amp;&amp; grep -q '^JWT_KEY_ID=' .env.example &amp;&amp; grep -q '^ADMIN_SEED_EMAIL=' .env.example &amp;&amp; grep -q '^ADMIN_SEED_PASSWORD_HASH=' .env.example &amp;&amp; ! grep -q '^# JWT_SIGNING_KEY=' .env.example &amp;&amp; test -f identity-service/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 (parses cleanly with the new block)
    - `grep -c '^  identity-service:' docker-compose.yml` → exactly 1 (additive merge, no duplication)
    - `grep -c 'image: n11/identity-service:dev' docker-compose.yml` → 1
    - `grep -c 'container_name: n11-identity-service' docker-compose.yml` → 1
    - `awk '/^  identity-service:/,/^  [a-z]/' docker-compose.yml | grep -c 'ports:'` → 0 (Pitfall #14: no host port published)
    - `awk '/^  identity-service:/,/^  [a-z]/' docker-compose.yml | grep -c 'env_file:'` → 1
    - `awk '/^  identity-service:/,/^  [a-z]/' docker-compose.yml | grep -c 'condition: service_healthy'` → 4 (postgres, rabbitmq, eureka-server, config-server)
    - `grep -c '^JWT_PRIVATE_KEY=' .env.example` → 1
    - `grep -c '^JWT_KEY_ID=' .env.example` → 1
    - `grep -c '^ADMIN_SEED_EMAIL=' .env.example` → 1
    - `grep -c '^ADMIN_SEED_PASSWORD_HASH=' .env.example` → 1
    - `grep -c 'JWT_SIGNING_KEY' .env.example` → 0 (obsolete name removed)
    - `grep -c '^IDENTITY_DB_PASSWORD=' .env.example` → 1 (existing line preserved untouched)
    - File `identity-service/README.md` exists and `grep -c 'openssl genrsa 2048' identity-service/README.md` → at least 1
    - `grep -c 'htpasswd -bnBC 10' identity-service/README.md` → at least 1
  </acceptance_criteria>
  <done>
    docker-compose.yml parses, the identity-service block is in place with strict internal-only port, .env.example documents all four new placeholder slots, README explains how to populate them. The infra is ready for the next wave to author the actual JWT and DB code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell ↔ docker-compose container | `.env` is read by docker-compose and injected as environment variables; never copied into the image (Jib does not bake env at build time) |
| public CIDR ↔ docker-compose network | Only port 8080 (api-gateway) is published; identity-service port 8081 stays internal |
| git repo ↔ developer workstation | `.env` is gitignored; `.env.example` is committed (placeholder values only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | JWT private key | mitigate | `.env` gitignored; `JWT_PRIVATE_KEY` referenced from `env_file:` (never embedded in YAML); README runbook directs operator to `openssl genrsa` locally; gitleaks (Phase 1) catches accidental commits |
| T-3-04 (Pitfall #14) | Spoofing | Direct identity-service bypass | mitigate | `ports:` mapping deliberately ABSENT from identity-service block in docker-compose.yml; service reachable only through api-gateway on the n11-net Docker network |
| T-3-05 | Tampering | docker-compose merge race | accept | Single-author repo; additive-merge idiom verified by `grep -c '^  identity-service:'` returning exactly 1 |
</threat_model>

<verification>
After the plan executes:

1. **Compile check:** `./gradlew :identity-service:compileJava --no-daemon` exits 0
2. **Settings inclusion:** `./gradlew projects --no-daemon | grep -c "identity-service"` ≥ 1
3. **Compose parse:** `docker compose config` exits 0
4. **Internal-only port:** `awk '/^  identity-service:/,/^  [a-z]/' docker-compose.yml | grep -c 'ports:'` → 0
5. **Env-var matrix:** all four new keys (`JWT_PRIVATE_KEY`, `JWT_KEY_ID`, `ADMIN_SEED_EMAIL`, `ADMIN_SEED_PASSWORD_HASH`) appear in `.env.example`
6. **README runbook:** `openssl genrsa 2048` and `htpasswd -bnBC 10` both documented in `identity-service/README.md`
</verification>

<success_criteria>
- identity-service is a recognized Gradle subproject
- Cloned skeleton compiles (no business code yet — IdentityServiceApplication + SampleHealthController only)
- docker-compose.yml has the identity-service block with no host-port mapping
- .env.example documents the four new env-var placeholders
- README explains how the operator generates real values for those placeholders
- Phase 1 invariants preserved: gitleaks still passes; settings.gradle.kts still parses; api-gateway / eureka-server / config-server blocks unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-identity-gateway-auth/03-01-SUMMARY.md` per the template.
</output>
