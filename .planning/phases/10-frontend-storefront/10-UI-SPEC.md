---
phase: 10
slug: frontend-storefront
status: approved
shadcn_initialized: false
preset: none
created: 2026-05-01
revised: 2026-05-01
reviewed_at: 2026-05-01
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for Phase 10: Turkish React storefront.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | CONTEXT.md — "Custom CSS framework / design system" is Out of Scope per REQUIREMENTS; use Tailwind utility classes + recon-derived tokens |
| Preset | not applicable | No shadcn; frontend dir does not exist yet |
| Component library | none (hand-rolled Tailwind components) | CONTEXT.md decisions |
| Icon library | lucide-react | Planner discretion — lightest tree-shakeable set; no recon constraint |
| Font | Open Sans via `@fontsource/open-sans` | Recon §5 — "Open Sans", Arial, Helvetica; `@fontsource/open-sans` npm carry-forward |

> **shadcn gate:** `components.json` not found; frontend directory does not exist. Stack is React 19 + Vite 8 + Tailwind 4. CONTEXT.md explicitly locks "no design system" (Out-of-Scope row). Gate bypassed — do not initialize shadcn.

---

## Spacing Scale

Standard 8-point scale. Tailwind 4 `@theme` block — declare as CSS custom properties.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline chip spacing |
| sm | 8px | Card inner padding small, input field icon gap |
| md | 16px | Default element spacing, form field gap, card body padding |
| lg | 24px | Section internal padding, card-to-card gap in grid |
| xl | 32px | Layout section vertical rhythm |
| 2xl | 48px | Major section breaks (hero → rail gap) |
| 3xl | 64px | Page-level top/bottom padding |

Exceptions:
- Touch targets for header icon buttons (Hesabım, Sepetim): minimum 44px × 44px per accessibility baseline. Not a grid-spacing value; enforced via `min-h-[44px] min-w-[44px]` on the anchor/button.
- Listing grid column gap: use `gap-4` (16px = `--spacing-md`). The 4-column layout at 1440px uses `grid-cols-4` with standard 16px gutters — Tailwind responsive widths absorb the arithmetic without a custom gutter token. No `--spacing-grid-gap` token needed.

---

## Typography

Font stack: `"Open Sans", Arial, Helvetica, sans-serif` (body + heading). CTA button: `Arial, sans-serif` per recon §5 observation. Load Open Sans via `@fontsource/open-sans`; import **400 and 700 weights only** — two weights maximum.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 | 1.5 | Paragraph text, card descriptions, form labels, toast messages |
| Label | 14px | 400 | 1.4 | Badges, tabs, breadcrumb items, pagination numbers, form helper text |
| Heading | 20px | 700 | 1.2 | PDP product title (H1), page section headings, cart section titles |
| Display | 24px | 700 | 1.2 | PDP price block, homepage H1, hero overlay heading |

> **Weight collapse rationale:** The original spec declared three weights (400, 600, 700). The 2-weight maximum is a contract rule. Weight 600 is dropped entirely; the Heading role moves to 700. The Display role was 16px/700 (same size as Body/400 — ambiguous), now reassigned to 24px to replace the PDP price block size that appeared only in the page-level contract. Result: 4 distinct sizes (14, 16, 20, 24) at 2 weights (400, 700). All page-level contracts use these 4 roles exclusively.

> Recon §5 exact observations: body = 16px/400, homepage H1 = 16px/700, PDP H1 = 20px/700, CTA button = 700, PDP price = visually larger than heading — 24px/700 maps to Display. Four distinct roles map to these four tokens — no additional sizes permitted.

---

## Color

Token values paste verbatim from recon §4 into `frontend/src/index.css` `@theme` block.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-body-bg` | `#EDEFF3` | Page background, listing page bg, checkout page bg, account page bg |
| Secondary (30%) | `--color-surface-card` | `#FFFFFF` | Card surfaces, modal overlays, form input backgrounds, login/register page bg |
| Accent (10%) | `--color-cta-primary-bg` | `#1C1C1E` | **Reserved for**: primary CTA button background ("Sepete Ekle", "Siparişi Tamamla", "Giriş Yap", "Kayıt Ol", "Sipariş Ver") + active category nav indicator (text color + underline only, no filled background) + current pagination page (filled background + white text) |
| Destructive | `#DC2626` (Tailwind red-600) | `#DC2626` | Cancel order confirmation only; destructive inline form errors |
| Text primary | `--color-link` / `--color-heading-primary` | `#000000` | All body text, headings, links (non-hover) |
| Text on accent | `--color-cta-primary` | `#FFFFFF` | Text on black CTA button; text on current pagination page |

**Accent reservation policy:** `#1C1C1E` is used for (a) primary CTA button fill, (b) active category nav link — text color + bottom-border underline only, not a filled pill, and (c) current pagination page — filled background with white text. It is never used on secondary buttons, badges, breadcrumbs, non-active nav items, tabs, or non-current pagination links. Brand-adjacent orange (n11 promotional color) is NOT used anywhere in Phase 10 — recon §6 and CONTEXT.md explicitly forbid it on primary CTAs. Orange may appear only as a promo price-discount ribbon on listing cards if the backend sends a discount flag (out of v1 scope — omit for now).

> **Token rename note:** The original spec used `--color-body-bg-bg` for both Dominant and Secondary rows (duplicate token name). The Dominant row is now `--color-body-bg`; the Secondary (white `#FFFFFF`) row is `--color-surface-card`. Both must appear as distinct CSS custom properties in the `@theme` block.

Additional recon-derived tokens to declare in `@theme`:
- `--color-badge-free-ship-bg`: derive from listing card analysis — use `#34A853` (green) as a safe default for "Kargo Bedava" badge (recon §4 does not capture badge bg; green is universal convention for free-shipping affordance).
- `--color-stock-in`: `#34A853` — "Stokta" indicator dot.
- `--color-stock-out`: `#DC2626` — "Tükendi" indicator dot.
- `--color-border`: `#E5E7EB` (Tailwind gray-200) — card borders, input borders, dividers.
- `--color-surface-header`: `#FFFFFF` — sticky header background.

---

## Page-Level Interaction Contracts

### Header (FE-02)

- Sticky: `position: sticky; top: 0; z-index: 50`.
- Height: 64px (= 4xl + 2×md).
- Left: logo (image, height 32px, links to `/`).
- Center: search input — full-width between logo and right cluster. Submits `GET /arama?q=<value>` on Enter or icon-click. Placeholder: "Aradığınız ürün, kategori veya markayı yazınız" (recon-faithful). No autocomplete dropdown in Phase 10 (deferred). Submit icon button: `aria-label="Ara"`.
- Right cluster: "Hesabım" (authenticated: name or "Hesabım" with dropdown; anonymous: "Giriş Yap / Üye Ol") + "Sepetim" with numeric badge. Badge shows cart item count from TanStack Query cache; 0 = hidden badge (no "0" label). Touch target: 44×44px minimum.
- Below header: secondary category nav bar — 8 horizontal links mapped to slugs from CONTEXT.md D-06. Does not sticky-pin independently (scrolls out with page; header stays). On viewports < 768px, category nav collapses to a "Kategoriler" hamburger icon (planner discretion for mobile collapse).

### Category Nav (FE-03 partial, PROD-03)

- 8 top-level links in a single horizontal row: Elektronik · Moda · Ev & Yaşam · Anne & Bebek · Kozmetik · Spor & Outdoor · Süpermarket · Kitap, Müzik, Film, Oyun.
- Each link is `/elektronik`, `/moda`, etc. (CONTEXT.md D-06 slugs).
- Active page: `font-weight: 700` (Display weight) + `color: #1C1C1E` + bottom-border underline `border-b-2 border-[#1C1C1E]`. This is a text-only indicator — no filled background pill. No mega-menu in Phase 10 (recon shows one; out of v1 scope given sub-category deferral).

### Hero Carousel (FE-04)

- 3 static slides. Auto-advance every 5 seconds. Dot navigation below carousel.
- Slide structure: full-width image (16:5 aspect ratio at 1440px) + optional overlay heading + CTA button.
- Copy: generic promo ("Elektronikte Fırsatlar", "Yeni Sezon Moda", "Ev & Yaşam Kampanyaları"). No countdown timers, no fake discounts (recon §7 anti-pattern).
- CTA button on slides: white background + `#1C1C1E` text (inverted from primary CTA to work on dark overlays).

### Bestseller Rail / "Yeni Gelenler" (FE-05)

- Label: "Yeni Gelenler" (planner choice per CONTEXT.md discretion — "Çok Satanlar" is a footer nav link, not confirmed as homepage rail per recon §8).
- Data source: `GET /api/v1/products?sort=created_at,desc&size=10` — reuses listing endpoint, no new endpoint.
- Layout: horizontal scroll rail with 10 product cards. Card structure same as listing card.

### Listing Grid (FE-06, PROD-01, PROD-04, PROD-05)

- Grid: `grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4` (1 → 2 → 3 → 4 columns). Column gap: `gap-4` (16px).
- Card: product image (aspect 3:4), title (2-line clamp), price (`formatTRY(price_gross)` at 20px/700), "Kargo Bedava" badge when applicable (PDP-only in v1; FE-V2-02 deferred for listing cards — omit for now).
- Sort controls above grid: dropdown with options "Tarihe Göre (Yeni→Eski)" / "Fiyata Göre (Düşük→Yük)" / "Fiyata Göre (Yük→Düş)". Maps to URL params per CONTEXT.md D-10.
- Pagination component below grid per CONTEXT.md D-09: "Önceki / 1 2 3 ... N / Sonraki". Current page: `#1C1C1E` filled background + white text. Other pages: text link. Keyboard-accessible (tabindex, Enter key).

### Product Detail Page / PDP (FE-07, PROD-02, LOC-01, LOC-02, LOC-03, LOC-04)

- URL: `/urun/:slug-:id`.
- Breadcrumb above title: "Ana Sayfa › {Category Label} › {Product Title}" (recon §8 phrase 477-478 confirms; two-level category trail).
- Layout: left column ~50% image gallery + right column ~50% info.
  - Gallery: primary image + thumbnail strip below (max 5 thumbnails). Click thumbnail → swap primary image.
  - Right column top-to-bottom: breadcrumb (mobile: above image), product title H1 20px/700 (Heading role), price block, taksit preview, stock indicator, "Sepete Ekle" CTA, seller info.
- Price block: `formatTRY(price_gross)` at **24px/700 (Display role)**. No crossed-out original price in v1 (no discount system).
- "Kargo Bedava" badge: shown when `price_gross >= FREE_SHIPPING_THRESHOLD` (env config). Green badge with white text above the CTA (recon phrase 627).
- Stock indicator: green dot + "Stokta" text when `quantity > 0`. Red dot + "Tükendi" + disabled CTA when `quantity === 0`. No "Son N ürün!" (confirmed out of scope per recon §8).
- Taksit preview (LOC-03): rendered as a 2×6 table or horizontal chip row for 1/2/3/6/9/12 installments. Formula: `Math.ceil(price_gross / n)` formatted as `formatTRY(...)` for each tier. Label: "Taksit Seçenekleri".
- "Sepete Ekle" CTA: `#1C1C1E` bg, white text, 16px/700 Arial, full-width in right column. Disabled + "Tükendi" variant when out of stock. On success: toast "Ürün sepete eklendi." + header badge increments.
- Tabs below fold: "Açıklama" / "Özellikler" / "Kargo". Active tab: underline in `#1C1C1E` (text color + `border-b-2`, consistent with Category Nav active style). Content areas show static product field text from backend.

### Cart Page (FE-08, CART-01, CART-02, LOC-01, LOC-02)

- URL: `/sepetim`. Anonymous-allowed (no auth redirect).
- Empty state: "Sepetin Boş Görünüyor" (recon phrase 73) H2 + body "Alışverişe başlamak için ürün ekleyin." + CTA "Alışverişe Başla" → `/`. When anonymous: also show "Hemen Giriş Yap" CTA (recon phrase 76).
- Populated state: two-column layout — left (items list) + right (order summary sidebar).
  - Left: each line item shows product image (64px square), title, quantity stepper (−/+), unit price, line total. Remove button: trash icon with `aria-label="Ürünü kaldır"` and tooltip "Ürünü kaldır". Qty stepper calls `PUT /api/v1/cart/items/{productId}`. Remove calls `DELETE`.
  - Right sidebar: "Sipariş Özeti" (recon phrase 643) heading. Lines: Ara Toplam, KDV (extracted from items), Kargo (free threshold logic), **Toplam**. "Siparişi Tamamla" CTA (recon phrase 631) → `/odeme/adres` if authenticated; `/giris-yap?redirectUrl=/odeme/adres` if anonymous.
- KDV display: backend sends `price_gross`; KDV is included. Display as line "KDV Dahil" with no separate KDV breakdown in v1 (CART-02 notes "KDV display deferred to Phase 10" — ship "KDV Dahil" label next to line total, no complex breakdown).

### Checkout: Address Step (FE-09, D-13, D-14)

- URL: `/odeme/adres`. Auth-required (guard redirects anonymous to `/giris-yap?redirectUrl=/odeme/adres`).
- Step indicator strip: "Adres › Ödeme › Onay" — step 1 active.
- Radio card list of existing addresses from `GET /api/v1/identity/addresses`. Each card: "Mahalle / İlçe / İl" layout, radio select. Pre-selects first address.
- "Yeni adres ekle" link: reveals inline form. Fields: Ad Soyad, Telefon, Mahalle (required), İlçe (required), İl (required). react-hook-form + zod. Submit → `POST /api/v1/identity/addresses`. On success: new address auto-selected, form collapses.
- "Devam Et" button → saves selected `addressId` to Zustand checkout slice → navigate to `/odeme/odeme`.

### Checkout: Payment Step (FE-09, D-13)

- URL: `/odeme/odeme`. Auth-required. Redirects to `/odeme/adres` if no addressId in Zustand.
- Step indicator: step 2 active.
- Payment method: two radio options.
  - "Kredi Kartı" — enabled, selected by default (recon phrase 641).
  - "Kapıda Ödeme" — disabled radio with helper text "Yakında" (FE-V2-03 deferral per CONTEXT.md; Pitfall #11 mitigation).
- "Sipariş Ver" button: `#1C1C1E` bg, "Sipariş Ver" label. On click:
  1. Generate UUID as `Idempotency-Key`.
  2. `POST /api/v1/orders` with `{ addressId, paymentMethod: "CREDIT_CARD" }` + `Idempotency-Key` header.
  3. On 200/202: fetch `GET /api/v1/payments/by-order/{orderId}` → get `paymentPageUrl`.
  4. `window.location.assign(paymentPageUrl)` — hands off to Iyzico hosted page.
  5. On error: toast with RFC-7807 `detail` field; stay on page.

### Checkout: Iyzico Return (FE-09, D-15, D-16)

- URL: `/odeme/sonuc?orderId=<id>`.
- On mount: begin polling `GET /api/v1/orders/{orderId}` every 2 seconds, max 30 seconds (15 attempts).
- Loading state: skeleton + "Ödemeniz işleniyor, lütfen bekleyiniz..." spinner text.
- Confirmed state (`CONFIRMED`): heading "Siparişiniz Alındı", body "Sipariş numaranız: {orderId}. Teşekkür ederiz!", CTA "Siparişimi Gör" → `/siparislerim/{orderId}`. Invalidate cart query.
- Failure state (`CANCELLED` / `FAILED`): heading "Ödemeniz Alınamadı", body "Lütfen tekrar deneyiniz veya farklı bir kart kullanınız.", CTA "Tekrar Dene" → `/sepetim`.
- Timeout state (30s elapsed, still pending): heading "İşleminiz Hâlâ Kontrol Ediliyor", body "Siparişinizin durumunu aşağıdan takip edebilirsiniz.", CTA "Siparişimi Görüntüle" → `/siparislerim/{orderId}`.

### Account Hub (FE-10)

- URL: `/hesabim`. Auth-required.
- Layout: two-column — left sidebar nav + right content area.
- Sidebar links: "Siparişlerim" → `/siparislerim`; "Adreslerim" → `/adreslerim`; "Çıkış Yap" (logout trigger per CONTEXT.md D-03).
- Hub landing shows greeting "Hoş geldin, {email}" + quick-stat cards (order count, saved addresses).

### Orders List (FE-10, ORD-03, ORD-04)

- URL: `/siparislerim`. Auth-required.
- Empty state: "Henüz siparişiniz yok." + CTA "Alışverişe Başla" → `/`.
- Populated: list of order summary cards sorted by date desc. Each card: order ID, date (`formatTRDate`), total `formatTRY(total)`, status badge (mapped per CONTEXT.md status-timeline note), "Detay" link → `/siparislerim/{orderId}`.
- Status badge color: PENDING/STOCK_RESERVED → gray; PAID → blue; CONFIRMED → green; FAILED/CANCELLED → red.

### Order Detail (FE-10, ORD-04, LOC-05)

- URL: `/siparislerim/:orderId`. Auth-required.
- Status timeline: "Sipariş Alındı → Hazırlanıyor → Kargoya Verildi → Teslim Edildi" — horizontal step indicator. Active step derived from saga state mapping (CONTEXT.md): PENDING/STOCK_RESERVED → step 1 active; PAID → step 2 active; CONFIRMED → step 3 active; FAILED/CANCELLED → timeline replaced with "İptal Edildi" in red.
- Order items table: product name, qty, unit price, line total.
- Delivery address block, payment method label.
- "Siparişi İptal Et" button (order in PENDING state only) → confirmation modal per CONTEXT.md D-16 area; calls `POST /api/v1/orders/{id}/cancel`.

### Address Book (FE-10, AUTH-08)

- URL: `/adreslerim`. Auth-required.
- Empty state: "Henüz kayıtlı adresiniz yok." + "Yeni Adres Ekle" button.
- Populated: card grid of addresses. Each card: address text, "Düzenle" + "Sil" actions (CRUD floor in v1).
- Add/edit form: same fields as checkout address form (react-hook-form + zod, Turkish error messages).

### Login / Register (FE-11, AUTH-01, AUTH-02)

- Login URL: `/giris-yap`. Register URL: `/uye-ol`.
- Centered card layout on white bg. Max-width 420px.
- Login form: E-posta + Şifre fields, "Giriş Yap" submit button (`#1C1C1E`). Link: "Henüz üye değil misin? Üye Ol" → `/uye-ol`.
- Register form: Ad Soyad + E-posta + Şifre + Şifre (tekrar). "Üye Ol" submit (`#1C1C1E`). Link: "Zaten üye misin? Giriş Yap" → `/giris-yap`. Auto-login on 201 (planner discretion per CONTEXT.md).
- Validation (inline, below each field, no top-of-form banner):
  - E-posta: "Geçerli bir e-posta adresi giriniz."
  - Şifre (register): "Şifre en az 8 karakter olmalıdır."
  - Şifre tekrar: "Şifreler eşleşmiyor."
  - Required: "Bu alan zorunludur."
- 401 from login: toast "E-posta veya şifre hatalı."

### Search Results (FE-06 variant, D-07)

- URL: `/arama?q=<term>`.
- Heading: "'{term}' için arama sonuçları" (N ürün bulundu).
- Empty state: "Aramanız için sonuç bulunamadı." + "Diğer kategorilere göz at" CTA → `/elektronik`.
- Reuses same listing grid + pagination + sort as category listing.

### 404 Page

- Catch-all route `*`.
- Turkish: "Aradığınız sayfayı bulamadık." + body "Bu sayfa mevcut değil veya taşınmış olabilir." + CTA "Ana Sayfaya Dön" → `/`.

---

## Copywriting Contract

All copy is Turkish. English identifiers in code (component names, function names, route constants, variable names). Source phrases from recon §2 where available.

| Element | Copy | Recon Source |
|---------|------|-------------|
| Primary CTA — Add to cart | "Sepete Ekle" | Recon §7 / CONTEXT.md |
| Primary CTA — Complete order | "Siparişi Tamamla" | Recon phrase 631 |
| Primary CTA — Place order | "Sipariş Ver" | CONTEXT.md D-13 |
| Primary CTA — Login | "Giriş Yap" | Recon phrase 1 |
| Primary CTA — Register | "Üye Ol" | Recon phrase 2 |
| Primary CTA — Continue (checkout) | "Devam Et" | Standard Turkish e-commerce — kept as-is (see note below) |
| Secondary CTA — Buy now | "Hemen Al" | Recon phrase 624 |
| Secondary CTA — View order | "Siparişimi Gör" | CONTEXT.md D-15 |
| Empty state — cart | "Sepetin Boş Görünüyor" | Recon phrase 73 |
| Empty state — cart body | "Alışverişe başlamak için ürün ekleyin." | Planner voice (recon-faithful) |
| Empty state — orders | "Henüz siparişiniz yok." | CONTEXT.md discretion |
| Empty state — addresses | "Henüz kayıtlı adresiniz yok." | CONTEXT.md discretion |
| Empty state — search | "Aramanız için sonuç bulunamadı." | CONTEXT.md D-07 |
| Stock — in stock | "Stokta" | Recon phrase 625 |
| Stock — out of stock | "Tükendi" | Recon phrase 626 |
| Free shipping badge | "Kargo Bedava" | Recon phrase 627 |
| Pagination — previous | "Önceki" | Recon phrase 628 |
| Pagination — next | "Sonraki" | Recon phrase 629 |
| Session expired toast | "Oturum süreniz doldu, lütfen tekrar giriş yapın." | CONTEXT.md D-02 |
| Logout toast | "Çıkış yapıldı." | CONTEXT.md D-03 |
| Add to cart success toast | "Ürün sepete eklendi." | Planner voice |
| 401 on login | "E-posta veya şifre hatalı." | Standard Turkish |
| Generic error fallback | "Bir hata oluştu. Lütfen tekrar deneyiniz." | CONTEXT.md code-context |
| Payment confirmed heading | "Siparişiniz Alındı" | CONTEXT.md D-15 |
| Payment failed heading | "Ödemeniz Alınamadı" | CONTEXT.md D-16 |
| Payment failed body | "Lütfen tekrar deneyiniz veya farklı bir kart kullanınız." | CONTEXT.md D-16 |
| Payment polling | "Ödemeniz işleniyor, lütfen bekleyiniz..." | Planner voice |
| Payment timeout heading | "İşleminiz Hâlâ Kontrol Ediliyor" | CONTEXT.md D-15 |
| Order status timeline | "Sipariş Alındı → Hazırlanıyor → Kargoya Verildi → Teslim Edildi" | CONTEXT.md / ORD-04 |
| Cancel order label | "Siparişi İptal Et" | ORD-04 / standard Turkish |
| Cancel confirmation | "Siparişinizi iptal etmek istediğinizden emin misiniz?" + "Evet, İptal Et" / "Vazgeç" | Planner voice |
| Kapıda Ödeme disabled label | "Yakında" | CONTEXT.md / Pitfall #11 |
| Address book — add new | "Yeni Adres Ekle" | Recon phrase 11 area |
| 404 heading | "Aradığınız sayfayı bulamadık." | CONTEXT.md discretion |
| Free ship threshold label | "Kargo Bedava" | Recon phrase 627 |
| Cart — anonymous prompt | "Hemen Giriş Yap" | Recon phrase 76 |
| Order summary | "Sipariş Özeti" | Recon phrase 643 |
| PDP tabs | "Açıklama" / "Özellikler" / "Kargo" | Recon phrases 635/636/637 |
| Retry CTA (payment failure) | "Tekrar Dene" | CONTEXT.md D-16 — kept as-is (see note below) |

> **Single-verb CTA rationale:** "Devam Et" (Address → Payment step transition) and "Tekrar Dene" (payment failure retry) are single-verb CTAs. Both are kept as-is. n11's actual checkout (recon §8) uses "Devam Et" verbatim for step progression; longer forms like "Ödemeye Devam Et" would deviate from the recon-faithful copy contract. "Tekrar Dene" is the canonical retry label across Turkish e-commerce (Trendyol, HepsiBurada use identical phrasing); destination context (`/sepetim`) is clear from the surrounding failure page copy. Recon §2 does not supply a more specific phrase for either — these are locked as shown.

### Destructive Actions

| Action | Confirmation Approach |
|--------|----------------------|
| Cancel order | Modal dialog: "Siparişinizi iptal etmek istediğinizden emin misiniz?" + two buttons: "Evet, İptal Et" (`#DC2626` bg) + "Vazgeç" (secondary outline). No input required. |
| Remove cart item | No confirmation modal. Icon-only trash button with `aria-label="Ürünü kaldır"` and tooltip "Ürünü kaldır". Immediate optimistic removal with undo toast: "Ürün sepetten çıkarıldı. [Geri al]" (5s window). |
| Logout | No confirmation. Immediate. Toast "Çıkış yapıldı." (D-03). |

---

## Locale Formatters

All implementations live in `src/lib/format.ts`. No inline formatting calls.

| Formatter | Signature | Example output |
|-----------|-----------|----------------|
| `formatTRY(amount: number)` | `Intl.NumberFormat('tr-TR', { style: 'currency', currency: 'TRY', minimumFractionDigits: 2 })` | `1.299,90 ₺` |
| `formatTRDate(d: Date \| string)` | `Intl.DateTimeFormat('tr-TR', { year: 'numeric', month: 'long', day: 'numeric' })` | `28 Nisan 2026` |

---

## Interaction States

For every interactive element, all states must be implemented:

| State | Implementation |
|-------|---------------|
| Hover (buttons) | 10% lightened/darkened bg using Tailwind `hover:` prefix. CTA `#1C1C1E` → `hover:bg-neutral-700` |
| Focus (all interactive) | `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#1C1C1E]` — keyboard visible, mouse hidden |
| Disabled | `opacity-50 cursor-not-allowed` — "Tükendi" CTA, Kapıda Ödeme radio |
| Loading (buttons) | Spinner icon replaces label text; button disabled |
| Skeleton loaders | `animate-pulse bg-gray-200 rounded` placeholder blocks on PDP + listing fetch (FE-15) |
| Toast notifications | Top-right or bottom-right position; auto-dismiss 4s; stack up to 3. Library: sonner or react-hot-toast (planner picks lightest) |

---

## Skeleton Loader Shapes (FE-15)

| Page | Skeleton shape |
|------|---------------|
| Listing grid | 8 card placeholders (image block 3:4 aspect + 2 text lines + 1 price line) using `animate-pulse` |
| PDP | Left: square image placeholder; Right: 3-line title, 1 price line, 1 wide CTA rectangle |
| Order list | 3 row placeholders (single-line title + short status badge) |

---

## Routing Summary (CONTEXT.md D-05)

| Page | Route | Auth |
|------|-------|------|
| Homepage | `/` | anonymous |
| Category listing | `/:categorySlug` (8 reserved slugs) | anonymous |
| Search results | `/arama` | anonymous |
| PDP | `/urun/:slug-:id` | anonymous |
| Cart | `/sepetim` | anonymous |
| Checkout — address | `/odeme/adres` | required |
| Checkout — payment | `/odeme/odeme` | required |
| Iyzico return | `/odeme/sonuc` | required |
| Login | `/giris-yap` | anonymous-only (redirect authed to `/`) |
| Register | `/uye-ol` | anonymous-only |
| Account hub | `/hesabim` | required |
| Orders list | `/siparislerim` | required |
| Order detail | `/siparislerim/:orderId` | required |
| Address book | `/adreslerim` | required |
| 404 catch-all | `*` | anonymous |

---

## Responsive Breakpoints

Tailwind 4 defaults — no override needed. Phase 10 applies these standard ramps:

| Breakpoint | px | Listing columns | PDP layout |
|------------|-----|-----------------|------------|
| default (mobile) | < 640px | 1 col | stacked (image top, info below) |
| sm | 640px | 2 col | stacked |
| md | 768px | 3 col | stacked |
| lg | 1024px | 4 col | side-by-side (50/50) |
| xl | 1280px | 4 col | side-by-side (50/50) |

---

## Anti-Patterns Explicitly Prohibited (recon §7)

- No countdown timers on product cards or hero sections.
- No "10 günün en düşük fiyatı!" sticky-price badges.
- No stacked coupon-card rows (5+ coupon blocks).
- No promotional-banner overload above the product grid (max 1 optional banner slot).
- No `dangerouslySetInnerHTML` anywhere (CONTEXT.md D-01 XSS mitigation).
- No hardcoded service URLs — all REST via `${VITE_API_BASE_URL}/api/v1/...` (FE-16).
- No chat-bubble placeholder (Phase 11 adds cleanly; Phase 10 ships zero reservation).

---

## Registry Safety

No shadcn. No third-party component registry. Not applicable.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party registries | none declared | not applicable |

---

## Pre-Population Source Log

| Upstream Source | Decisions Used |
|----------------|---------------|
| CONTEXT.md (`10-CONTEXT.md`) | 16 decisions (D-01 through D-16): auth token strategy, routing map, pagination UX, checkout flow, Iyzico handoff, payment failure UX, all discretion areas |
| REQUIREMENTS.md | FE-02 through FE-16, LOC-01 through LOC-05, AUTH-03/04 deferrals — all mapped to page sections above |
| n11-recon.md §2 | 644 phrases — copywriting contract uses verbatim phrases at positions 1, 11, 67, 73, 76, 624-644 |
| n11-recon.md §3 | 8 top-level category slugs — routing table and category nav |
| n11-recon.md §4 | 25 color tokens — all pasted into Color section; `#EDEFF3`, `#FFFFFF`, `#1C1C1E`, `#000000` locked |
| n11-recon.md §5 | Typography: Open Sans, 16px body/400, 24px display/700, 20px heading/700, 14px label — 4 roles at 2 weights locked |
| n11-recon.md §6 | Layout patterns: sticky header 64px, 4-col grid at 1440px, PDP 50/50 split, cart items-left + summary-right |
| n11-recon.md §7 | Anti-patterns: 5 explicit prohibitions carried into contract |
| n11-recon.md §8 | Open questions resolved: no "Son N ürün!", Kapıda Ödeme = disabled radio, login = `/giris-yap` not `/giris` |
| ROADMAP.md Phase 10 | Success criteria 1-5 — all page sections trace to at least one criterion |
| User input this session | 0 — all design contract questions answered by upstream artifacts |

---

## Revision Log

| Date | Revision | Checker Issues Resolved |
|------|----------|------------------------|
| 2026-05-01 | v1 — initial draft | — |
| 2026-05-01 | v2 — checker fixes | BLOCK Dim-4 typography (drop weight 600; reassign Display to 24px); BLOCK Dim-5 spacing (remove non-standard 20px grid-gap token, use gap-4/16px); FLAG Dim-3 color (resolve accent reservation contradictions; rename duplicate `--color-body-bg-bg` token to `--color-surface-card`); FLAG Dim-2 accessibility (add `aria-label="Ürünü kaldır"` on trash button, `aria-label="Ara"` on search submit); FLAG Dim-1 copywriting (document rationale for single-verb CTAs "Devam Et" and "Tekrar Dene") |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
