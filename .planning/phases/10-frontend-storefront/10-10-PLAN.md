---
phase: 10-frontend-storefront
plan: 10
type: execute
wave: 1
depends_on: [10-03, 10-04, 10-09]
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/api/productApi.ts
  - frontend/src/api/productApi.test.ts
  - frontend/src/components/listing/ProductCard.tsx
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/pages/RegisterPage.tsx
  - frontend/src/pages/RegisterPage.test.tsx
  - frontend/src/components/feedback/ErrorBoundary.tsx
  - frontend/src/router.tsx
  - frontend/src/components/feedback/ErrorBoundary.test.tsx
autonomous: true
gap_closure: true
requirements: [FE-04, FE-05, FE-06, FE-11, FE-13, FE-15, FE-16, LOC-02, AUTH-01, AUTH-02]
user_setup: []
must_haves:
  truths:
    - "Homepage and listing routes render backend ProductSummaryDto data without ProductCard crashes."
    - "Register form shows Turkish inline validation errors for Zod failures and mirrors backend password policy."
    - "Route render failures show the custom Turkish fallback, not React Router's default developer error boundary."
  artifacts:
    - path: "frontend/src/api/productApi.ts"
      provides: "Backend ProductSummaryDto to frontend Product normalization"
      exports: ["fetchProducts", "fetchProductById", "normalizeProductPage"]
    - path: "frontend/src/components/listing/ProductCard.tsx"
      provides: "Crash-safe product card rendering and slug generation"
      contains: "product.name"
    - path: "frontend/src/pages/RegisterPage.tsx"
      provides: "Zod 4 compatible react-hook-form register validation and backend 400 surfacing"
      contains: "en az bir harf ve bir rakam"
    - path: "frontend/src/router.tsx"
      provides: "React Router errorElement using Turkish fallback"
      contains: "errorElement"
  key_links:
    - from: "frontend/src/api/productApi.ts"
      to: "frontend/src/components/listing/ProductCard.tsx"
      via: "fetchProducts returns normalized Product.content before ProductCard receives product"
      pattern: "normalizeProductPage"
    - from: "frontend/src/pages/RegisterPage.tsx"
      to: "identity-service/src/main/java/com/n11/identity/auth/dto/RegisterRequest.java"
      via: "frontend password regex mirrors backend at least one letter plus one digit rule"
      pattern: "(?=.*\\d)"
    - from: "frontend/src/router.tsx"
      to: "frontend/src/components/feedback/ErrorBoundary.tsx"
      via: "route-level errorElement renders Turkish fallback"
      pattern: "errorElement"
---

<objective>
Close the diagnosed Phase 10 UAT blockers without replanning the storefront: normalize backend catalog DTOs before listing/homepage rendering, restore Zod 4 compatible register validation with backend-aligned password rules, and replace React Router's developer error page with the custom Turkish fallback.

Purpose: unblock UAT so Tests 1, 3, and 4 can be rerun and the remaining storefront tests can proceed.
Output: one focused frontend gap-closure patch with regression tests for DTO normalization, register validation, and route error fallback.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-storefront/10-UAT.md
@.planning/phases/10-frontend-storefront/10-UI-SPEC.md
@.planning/phases/10-frontend-storefront/10-03-SUMMARY.md
@.planning/phases/10-frontend-storefront/10-04-SUMMARY.md
@.planning/phases/10-frontend-storefront/10-09-SUMMARY.md

<interfaces>
From backend listing DTO diagnosis in `10-UAT.md`:
```typescript
// ProductSummaryDto shape observed from product-service listing endpoint
{
  id: string;
  nameTr: string;
  priceGross: number;
  firstImageUrl: string | null;
  categoryName: string;
  categoryId?: string;
  stockQty?: number;
  createdAt?: string;
}
```

Current frontend `Product` contract in `frontend/src/lib/types.ts`:
```typescript
export interface Product {
  id: string;
  name: string;
  description: string;
  priceGross: number;
  kdvRate: number;
  imageUrl: string;
  categoryId: string;
  categoryLabel: string;
  stockQty: number;
  createdAt: string;
}

export interface ProductPage {
  content: Product[];
  totalElements: number;
  totalPages: number;
  number: number;
  size: number;
}
```

Current crash site in `frontend/src/components/listing/ProductCard.tsx`:
```typescript
function productSlug(p: Product): string {
  const slug = p.name.toLocaleLowerCase('tr-TR')
  return `${slug}-${p.id}`;
}
```

Current register gap in `frontend/src/pages/RegisterPage.tsx`:
```typescript
password: z.string().min(8, { message: 'Şifre en az 8 karakter olmalıdır.' })
```

Current route setup in `frontend/src/router.tsx` has no `errorElement`; React Router therefore displays its default developer error boundary for render crashes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: normalize product listing DTOs before ProductCard rendering</name>
  <files>frontend/src/lib/types.ts, frontend/src/api/productApi.ts, frontend/src/api/productApi.test.ts, frontend/src/components/listing/ProductCard.tsx</files>
  <behavior>
    - Given a backend page whose content items use `nameTr`, `firstImageUrl`, and `categoryName`, `fetchProducts()` returns frontend `Product` items with `name`, `imageUrl`, and `categoryLabel` populated.
    - Given nullable or missing optional listing fields, normalization supplies safe defaults for non-card fields (`description`, `kdvRate`, `stockQty`, `createdAt`) without throwing.
    - Given a normalized backend product, `ProductCard` links to `/urun/{turkish-kebab-name}-{id}` and never calls `toLocaleLowerCase` on `undefined`.
  </behavior>
  <action>
    Add backend DTO types in `frontend/src/lib/types.ts` without weakening the existing `Product` UI contract: define `BackendProductSummaryDto` and `BackendProductPage` for listing responses. In `frontend/src/api/productApi.ts`, make `fetchProducts()` call `apiFetch&lt;BackendProductPage&gt;`, then pass the response through an exported `normalizeProductPage(page)` helper that maps `nameTr → name`, `firstImageUrl → imageUrl`, `categoryName → categoryLabel`, preserves pagination metadata, and supplies safe defaults only for fields absent from the listing DTO. Keep `fetchProductById()` returning the PDP `Product` contract unless its live DTO also exposes Turkish names; do not change PDP behavior unless required by TypeScript.

    Harden `ProductCard` as a second line of defense: compute `displayName = product.name?.trim() || 'Ürün'`, use it for `aria-label`, `alt`, title text, and slug generation, and keep React JSX escaping intact. Do not add `dangerouslySetInnerHTML`. Create `frontend/src/api/productApi.test.ts` with a mocked `apiFetch` regression test for the diagnosed backend ProductSummaryDto shape and a direct test for `normalizeProductPage()` defaults.
  </action>
  <verify>
    <automated>npm --prefix frontend test -- src/api/productApi.test.ts</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Homepage `Yeni Gelenler` rail and category/search listings can render backend listing DTOs without `Cannot read properties of undefined (reading 'toLocaleLowerCase')`, and regression tests fail if `nameTr`/`firstImageUrl` mapping is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: restore Zod 4 compatible register validation and backend-aligned password policy</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/pages/RegisterPage.tsx, frontend/src/pages/RegisterPage.test.tsx</files>
  <behavior>
    - Submitting `/uye-ol` with a password shorter than 8 characters shows an inline Turkish field error instead of throwing an uncaught `ZodError`.
    - Submitting a password with 8+ characters but no digit or no letter shows an inline Turkish field error that explains the backend rule.
    - A backend register `400 Bad Request` problem detail is visible to the user as Turkish inline form feedback or a Turkish toast; it is not console-only.
  </behavior>
  <action>
    Upgrade `@hookform/resolvers` from `^3.10.0` to a Zod 4 compatible current major (`^5` or the latest version npm resolves that explicitly supports zod 4), and update `frontend/package-lock.json` by running npm install in `frontend/`. Keep `zod` at `^4.4.1`.

    In `frontend/src/pages/RegisterPage.tsx`, replace the password schema with the backend-equivalent rule from `identity-service/src/main/java/com/n11/identity/auth/dto/RegisterRequest.java`: at least 8 characters, at least one letter, and at least one digit. Use Turkish-aware letters in the regex (`A-Za-zÇĞİÖŞÜçğıöşü`) and message `Şifre en az 8 karakter olmalı, en az bir harf ve bir rakam içermelidir.` Keep the password-confirm match rule. In mutation `onError`, handle status 400 as user-correctable validation feedback: use `setError('root.server', { message: err.problem?.detail ?? 'Kayıt bilgilerinizi kontrol ediniz.' })` or an equivalent visible Turkish form alert, while keeping 409 duplicate-email behavior. Do not rely on console output.

    Add `frontend/src/pages/RegisterPage.test.tsx` using Testing Library and Vitest. Mock `registerRequest`, `useNavigate`, and auth store as needed. Test that invalid passwords render field errors and do not call `registerRequest`; test that a mocked 400 ApiError surfaces a visible message.
  </action>
  <verify>
    <automated>npm --prefix frontend test -- src/pages/RegisterPage.test.tsx</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Register validation failures stay in the form UI with Turkish messages, frontend password acceptance matches backend password acceptance, and resolver/Zod incompatibility no longer produces uncaught `ZodError`.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: install Turkish route-level error fallback</name>
  <files>frontend/src/components/feedback/ErrorBoundary.tsx, frontend/src/router.tsx, frontend/src/components/feedback/ErrorBoundary.test.tsx</files>
  <behavior>
    - If a route element throws during render, React Router renders the Turkish fallback copy `Bir hata oluştu` / `Sayfayı Yenile` instead of `Unexpected Application Error!`.
    - The fallback logs the route error for developers but exposes only safe Turkish user-facing copy.
    - The existing app-level ErrorBoundary remains in place for non-router render errors.
  </behavior>
  <action>
    Refactor `frontend/src/components/feedback/ErrorBoundary.tsx` to export a reusable `ErrorFallback` component and a `RouteErrorFallback` component that calls `useRouteError()` and renders the same Turkish fallback UI. Keep the class `ErrorBoundary` for `App.tsx` unchanged in behavior, but have it render `ErrorFallback` to avoid copy drift.

    In `frontend/src/router.tsx`, import `RouteErrorFallback` and set `errorElement: &lt;RouteErrorFallback /&gt;` on the root route object so all child route render/load errors use the Turkish fallback. Do not remove the catch-all 404 route; this task is for exceptions, not unknown URLs. Add `frontend/src/components/feedback/ErrorBoundary.test.tsx` that renders a memory router route which throws and asserts the Turkish fallback is visible and the default React Router text `Unexpected Application Error` is absent.
  </action>
  <verify>
    <automated>npm --prefix frontend test -- src/components/feedback/ErrorBoundary.test.tsx</automated>
    <automated>npm --prefix frontend run lint:invariants</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Route-level render crashes use the custom Turkish fallback; UAT no longer sees React Router's default developer error boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product-service → frontend | Backend catalog DTOs are untrusted shape at runtime; frontend must normalize before rendering. |
| browser form → identity-service | User-controlled register input crosses into auth backend; frontend validation is UX only, backend remains authoritative. |
| route exception → user UI | Internal exception details must not leak into user-facing developer error pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10G-01 | T | productApi normalization | mitigate | Map only allowlisted DTO fields into `Product`; no raw HTML rendering; keep `dangerouslySetInnerHTML` absent and invariant gate passing. |
| T-10G-02 | I | RouteErrorFallback | mitigate | Render generic Turkish copy only; log details to console for local debugging without exposing stack traces in DOM. |
| T-10G-03 | S | Register validation | accept | Frontend validation improves UX but does not authenticate or authorize; identity-service remains source of truth for password policy and account creation. |
| T-10G-04 | D | ProductCard image URL | accept | Broken/missing images should render fallback/alt text and not crash; remote image availability is outside this gap's security surface. |
</threat_model>

<verification>
Run the targeted tests after each task, then run the aggregate frontend checks before summary:

```bash
npm --prefix frontend test -- src/api/productApi.test.ts src/pages/RegisterPage.test.tsx src/components/feedback/ErrorBoundary.test.tsx
npm --prefix frontend test
npm --prefix frontend run lint:invariants
npm --prefix frontend run build
```

Optional live UAT rerun after starting the same stack used in `10-UAT.md`:

```bash
PLAYWRIGHT_BASE_URL=http://localhost:8083 npm --prefix frontend run test:e2e
```
</verification>

<success_criteria>
- `10-UAT.md` Tests 1 and 4 no longer reproduce the ProductCard `toLocaleLowerCase` crash when the backend returns `nameTr` / `firstImageUrl` / `categoryName` listing DTOs.
- `/uye-ol` invalid password attempts show Turkish inline validation and do not throw uncaught `ZodError`; backend 400 responses are visible to the user.
- Deliberate route render exceptions show `Bir hata oluştu` and `Sayfayı Yenile`, with no `Unexpected Application Error!` developer boundary text.
- `npm --prefix frontend test`, `npm --prefix frontend run lint:invariants`, and `npm --prefix frontend run build` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-storefront/10-10-SUMMARY.md` describing the DTO mappings, resolver version installed, password regex, route error fallback wiring, tests added, and UAT retest status.
</output>
