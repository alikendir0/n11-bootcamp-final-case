---
phase: 11-frontend-chat-assistant-devops-deploy
plan: 06
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - docs/devops-pipeline-comparison.md
autonomous: true
requirements: [DEV-04]
must_haves:
  truths:
    - "Jenkins-vs-GitHub-Actions comparison doc exists and explains equivalent pipeline stages."
    - "The doc maps backend build/test, frontend build/test, infra-tests, Slack notifications, and release-tag GHCR Jib publish to a Jenkinsfile sketch."
    - "Credential handling is explicit: secrets stay in GitHub Actions secrets, Jenkins Credentials, or local `.env`, never source."
  artifacts:
    - path: "docs/devops-pipeline-comparison.md"
      provides: "Jenkins comparison requirement"
  key_links:
    - from: "docs/devops-pipeline-comparison.md"
      to: ".github/workflows/ci.yml"
      via: "GitHub Actions build/test and Slack notification stage mapping"
      pattern: "Build Frontend"
    - from: "docs/devops-pipeline-comparison.md"
      to: ".github/workflows/release.yml"
      via: "release-tag GHCR Jib publish stage mapping"
      pattern: "Publish Images on Tag"
---

<objective>
Create the Jenkins-versus-GitHub-Actions comparison document required by DEV-04.

Purpose: Keep documentation-only pipeline comparison separate from the compose/tunnel execution plan so Plan 11-05 stays within the 2-3 task target and this doc can be implemented independently after the CI/release workflows exist.
Output: `docs/devops-pipeline-comparison.md`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>task 1: document Jenkins versus GitHub Actions pipeline logic</name>
  <files>docs/devops-pipeline-comparison.md</files>
  <read_first>.github/workflows/ci.yml; .github/workflows/release.yml; .planning/REQUIREMENTS.md; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md</read_first>
  <action>Create `docs/devops-pipeline-comparison.md`. Include sections: `Pipeline stages`, `GitHub Actions implementation`, `Equivalent Jenkinsfile sketch`, `Secrets and credentials`, `Why GitHub Actions for this project`, and `Local-host deploy model`. The comparison must map: checkout, setup Java 21, setup Node 24, Gradle build/test, frontend build/test, infra-tests, Slack notification, release-tag GHCR Jib publish, and no AWS/OIDC. Include a fenced `Jenkinsfile` declarative sketch with stages `Build Backend`, `Build Frontend`, `Infra Tests`, `Publish Images on Tag`, and `Notify Slack`. State that Jenkins would store `SLACK_WEBHOOK_URL`, `GITHUB_TOKEN`/registry credentials, `CLOUDFLARE_TUNNEL_TOKEN`, and `NGROK_AUTHTOKEN` in Jenkins Credentials, while this repo stores them in GitHub Actions secrets or local `.env` only. Do not add a Jenkins runtime dependency or Jenkinsfile to the repo; DEV-04 asks for comparison documentation, not a second CI system.</action>
  <verify>
    <automated>test -f docs/devops-pipeline-comparison.md &amp;&amp; grep -q "Jenkinsfile" docs/devops-pipeline-comparison.md &amp;&amp; grep -q "GitHub Actions" docs/devops-pipeline-comparison.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Jenkinsfile" docs/devops-pipeline-comparison.md` returns at least one match.
    - `grep -R "GitHub Actions" docs/devops-pipeline-comparison.md` returns at least one match.
    - `grep -R "Publish Images on Tag" docs/devops-pipeline-comparison.md` returns at least one match.
    - `grep -R "SLACK_WEBHOOK_URL" docs/devops-pipeline-comparison.md` returns at least one match.
    - `grep -R "CLOUDFLARE_TUNNEL_TOKEN" docs/devops-pipeline-comparison.md` returns at least one match.
  </acceptance_criteria>
  <done>Jenkins comparison requirement DEV-04 is satisfied with concrete pipeline-stage mapping and credential-handling explanation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→operator | Documentation tells the candidate/interviewer where CI and tunnel secrets belong. |
| CI docs→credential stores | The comparison references GitHub Actions secrets, Jenkins Credentials, and local `.env` without exposing values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | I | `docs/devops-pipeline-comparison.md` | mitigate | Use placeholder secret names only; never include Slack, registry, Cloudflare, or ngrok values. |
| T-11-06-02 | T | Jenkinsfile sketch | mitigate | Label the Jenkinsfile as illustrative documentation only; do not create an executable second pipeline that can drift from GitHub Actions. |
</threat_model>

<verification>
Run `test -f docs/devops-pipeline-comparison.md &amp;&amp; grep -q "Jenkinsfile" docs/devops-pipeline-comparison.md &amp;&amp; grep -q "GitHub Actions" docs/devops-pipeline-comparison.md` and inspect the acceptance grep checks.
</verification>

<success_criteria>
DEV-04 is covered by a concrete Jenkins/GitHub Actions comparison doc that maps every relevant Phase 11 CI/CD stage and credential-handling rule.
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-chat-assistant-devops-deploy/11-06-SUMMARY.md`.
</output>
