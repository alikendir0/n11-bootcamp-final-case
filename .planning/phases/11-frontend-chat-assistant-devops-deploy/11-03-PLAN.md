---
phase: 11-frontend-chat-assistant-devops-deploy
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - frontend/e2e/chat-assistant.spec.ts
  - frontend/scripts/check-frontend-invariants.sh
  - frontend/src/components/chat/ChatAssistant.test.tsx
  - frontend/src/hooks/useChatAssistant.test.tsx
autonomous: true
requirements: [FE-12]
must_haves:
  truths:
    - "Automated checks prove the chat bubble is visible across core routes."
    - "Automated checks prove chat cart actions refresh the existing header badge path after backend confirmation."
    - "Frontend invariant gates prevent direct ai-service URLs and forbidden EventSource usage for POST SSE."
  artifacts:
    - path: "frontend/e2e/chat-assistant.spec.ts"
      provides: "Playwright smoke coverage for chat visibility and cart handoff"
    - path: "frontend/scripts/check-frontend-invariants.sh"
      provides: "Static no-direct-service/no-EventSource chat gate"
  key_links:
    - from: "frontend/e2e/chat-assistant.spec.ts"
      to: "frontend/src/components/layout/Layout.tsx"
      via: "visible bubble on routed pages"
      pattern: "Yapay Zeka Alışveriş Asistanını Aç"
    - from: "frontend/scripts/check-frontend-invariants.sh"
      to: "frontend/src/api/chatApi.ts"
      via: "grep gate forbidding EventSource and direct ai-service URL"
      pattern: "EventSource"
---

<objective>
Lock Phase 11 chat behavior with automated frontend verification and invariant gates.

Purpose: The demo-critical FE-12 behavior must remain stable after DevOps changes and future cleanup; this plan proves persistence across routes, POST SSE posture, and cart refresh behavior.
Output: Playwright/Vitest coverage and static frontend invariant checks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-UI-SPEC.md
@frontend/playwright.config.ts
@frontend/package.json
@frontend/scripts/check-frontend-invariants.sh
@frontend/src/components/chat/ChatAssistant.tsx
@frontend/src/api/chatApi.ts
@frontend/src/hooks/useCart.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add Playwright smoke for chat route persistence</name>
  <files>frontend/e2e/chat-assistant.spec.ts</files>
  <read_first>frontend/playwright.config.ts; frontend/package.json; frontend/src/router.tsx; frontend/src/components/layout/Layout.tsx; frontend/src/components/chat/ChatAssistant.tsx; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-UI-SPEC.md</read_first>
  <behavior>
    - On `/`, `/arama`, `/sepetim`, and `/hesabim`, the bubble with aria-label `Yapay Zeka Alışveriş Asistanını Aç` is visible.
    - Opening the drawer, navigating with React Router links, and returning keeps title `Yapay Zeka Alışveriş Asistanı` visible until the user closes it.
  </behavior>
  <action>Create `frontend/e2e/chat-assistant.spec.ts` using existing Playwright baseURL. Add tests that visit `/`, `/arama`, `/sepetim`, and `/hesabim`, assert the chat trigger by aria-label `Yapay Zeka Alışveriş Asistanını Aç`, click it, assert drawer title `Yapay Zeka Alışveriş Asistanı`, click an existing route link if present or use `page.goto('/arama')`, and assert the drawer title remains visible to prove D-04. Keep selectors copy-based and Turkish. Do not require a live Gemini response in this smoke; backend streaming is covered by hook/API tests and live demo runbook.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:e2e -- chat-assistant.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Yapay Zeka Alışveriş Asistanını Aç" frontend/e2e/chat-assistant.spec.ts` returns at least one match.
    - `grep -R "Yapay Zeka Alışveriş Asistanı" frontend/e2e/chat-assistant.spec.ts` returns at least one match.
    - `grep -R "\/sepetim" frontend/e2e/chat-assistant.spec.ts` returns at least one match.
  </acceptance_criteria>
  <done>Playwright smoke proves the assistant is globally visible and remains open across navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: harden unit tests for confirmed cart handoff and retry</name>
  <files>frontend/src/components/chat/ChatAssistant.test.tsx, frontend/src/hooks/useChatAssistant.test.tsx</files>
  <read_first>frontend/src/components/chat/ChatAssistant.test.tsx; frontend/src/hooks/useChatAssistant.test.tsx; frontend/src/hooks/useChatAssistant.ts; frontend/src/components/chat/ChatProductCard.tsx; frontend/src/hooks/useCart.ts; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md</read_first>
  <behavior>
    - Tests assert `cartQueryKey` invalidation happens after `tool_result.ok === true` for cart mutations and not after `tool_call` pending events.
    - Tests assert retry button text `Tekrar dene` resends the same last user message with unchanged conversation ID.
    - Tests assert login-required card links to `/giris-yap?redirectUrl=<encoded current path>`.
  </behavior>
  <action>Extend existing Plan 01/02 tests. Add a mocked stream sequence: `tool_call add_to_cart`, no invalidation yet; then `tool_result { callId, ok: true, summary }`, assert `queryClient.invalidateQueries({ queryKey: cartQueryKey })` and Turkish toast. Add a failure sequence that renders `Yanıt alınamadı. Lütfen tekrar deneyiniz.` and button `Tekrar dene`; clicking retry calls `streamChat` with the previous `message` and same `conversationId`. Add auth-required assertion for body `Sepete eklemek ve sipariş işlemleri için giriş yapmanız gerekiyor.` and CTA `Giriş Yap` route containing encoded current path.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/chat/ChatAssistant.test.tsx src/hooks/useChatAssistant.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "tool_result" frontend/src/hooks/useChatAssistant.test.tsx` returns at least one match.
    - `grep -R "Tekrar dene" frontend/src/components/chat/ChatAssistant.test.tsx frontend/src/hooks/useChatAssistant.test.tsx` returns at least one match.
    - `grep -R "giris-yap" frontend/src/components/chat/ChatAssistant.test.tsx frontend/src/hooks/useChatAssistant.test.tsx` returns at least one match.
  </acceptance_criteria>
  <done>Tests prove D-07, D-08, D-09, and D-12 without relying on manual inspection.</done>
</task>

<task type="auto">
  <name>task 3: add frontend invariant gates for chat networking</name>
  <files>frontend/scripts/check-frontend-invariants.sh</files>
  <read_first>frontend/scripts/check-frontend-invariants.sh; frontend/src/api/chatApi.ts; frontend/src/lib/apiClient.ts; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md</read_first>
  <action>Extend `frontend/scripts/check-frontend-invariants.sh` with self-nonmatching grep gates that ignore comments and this script's own lines where necessary. Add checks: no direct `localhost:8088`, `ai-service`, or `:8088` URL string appears in non-test frontend source; `frontend/src/api/chatApi.ts` contains `/api/v1/chat/stream`; `frontend/src/api/chatApi.ts` contains `Accept` and `text/event-stream`; and `frontend/src/api/chatApi.ts` does not instantiate `new EventSource`. Implement grep gates with `grep -R --include='*.ts' --include='*.tsx' ... frontend/src | grep -v 'check-frontend-invariants'` style filtering, not unfiltered `grep -c` on a file that includes its own invariant text.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint:invariants</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "chatApi.ts" frontend/scripts/check-frontend-invariants.sh` returns at least one match.
    - `grep -R "text/event-stream" frontend/scripts/check-frontend-invariants.sh` returns at least one match.
    - `grep -R "EventSource" frontend/scripts/check-frontend-invariants.sh` returns at least one match.
    - `cd frontend &amp;&amp; npm run lint:invariants` exits 0.
  </acceptance_criteria>
  <done>Invariant script catches direct ai-service URLs and EventSource regression for the POST SSE endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mocks→chat UI | Mocked stream data exercises UI behavior without live Gemini. |
| source tree→CI gate | Static invariant checks prevent unsafe network patterns from shipping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | S | frontend source networking | mitigate | Invariant forbids direct ai-service/8088 URLs; gateway remains sole public edge. |
| T-11-03-02 | T | cart badge state | mitigate | Tests require invalidation only after confirmed `tool_result.ok === true`, preventing optimistic spoofed cart count. |
| T-11-03-03 | D | streaming regressions | mitigate | Invariant forbids native EventSource on POST stream; API tests cover fetch streaming parser. |
</threat_model>

<verification>
Run `cd frontend &amp;&amp; npm test -- --run src/components/chat/ChatAssistant.test.tsx src/hooks/useChatAssistant.test.tsx`, `cd frontend &amp;&amp; npm run lint:invariants`, and `cd frontend &amp;&amp; npm run build`.
</verification>

<success_criteria>
Automated checks cover the demo-critical chat route visibility, retry/auth/cart handoff behavior, and gateway-only streaming posture.
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-chat-assistant-devops-deploy/11-03-SUMMARY.md`.
</output>
