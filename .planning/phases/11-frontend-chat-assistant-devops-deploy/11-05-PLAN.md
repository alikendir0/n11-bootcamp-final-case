---
phase: 11-frontend-chat-assistant-devops-deploy
plan: 05
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - docker-compose.yml
  - .env.example
  - frontend/.env.example
  - README.md
  - scripts/verify-demo-tunnel.sh
autonomous: false
requirements: [DEV-05, DEV-09]
user_setup:
  - service: cloudflare-tunnel
    why: "Primary public demo URL for gateway and Iyzico callback"
    env_vars:
      - name: CLOUDFLARE_TUNNEL_TOKEN
        source: "Cloudflare Zero Trust dashboard after creating a tunnel"
  - service: ngrok
    why: "Fallback public demo URL if Cloudflare Tunnel is unavailable"
    env_vars:
      - name: NGROK_AUTHTOKEN
        source: "ngrok dashboard -> Your Authtoken"
      - name: DEMO_TUNNEL_HOSTNAME
        source: "The HTTPS hostname shown by Cloudflare Tunnel or ngrok, without https://"
must_haves:
  truths:
    - "`docker compose --profile full up` has a clear path to run Postgres, RabbitMQ, all 13 services, and frontend demo surface on the candidate machine."
    - "Root README starts with a one-command demo path, env matrix, tunnel proof curl, Iyzico card numbers, MCP snippet, and troubleshooting."
    - "A demo rehearsal proves `https://$DEMO_TUNNEL_HOSTNAME/api/v1/products` returns HTTP 200 through the public tunnel."
  artifacts:
    - path: "docker-compose.yml"
      provides: "full profile and image-tag/env posture"
    - path: "README.md"
      provides: "candidate demo runbook"
    - path: "scripts/verify-demo-tunnel.sh"
      provides: "repeatable public tunnel reachability proof for DEV-05"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "docker compose --profile full up command"
      pattern: "docker compose --profile full up"
    - from: "README.md"
      to: "PUBLIC_BASE_URL"
      via: "external curl proof"
      pattern: "curl https://&lt;tunnel-hostname&gt;/api/v1/products"
---

<objective>
Complete the local deploy/demo posture: compose full profile, safe env examples, root README runbook, and live public-tunnel proof.

Purpose: DEV-05/09 are the operator-facing grading artifacts that prove the project can be run, exposed publicly through a tunnel, explained, and rehearsed quickly.
Output: compose/env updates and docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md
@.planning/phases/11-frontend-chat-assistant-devops-deploy/11-PATTERNS.md
@docker-compose.yml
@.env.example
@frontend/.env.example
@payment-service/README.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>task 1: harden compose full-profile and image-tag posture</name>
  <files>docker-compose.yml, .env.example, frontend/.env.example</files>
  <read_first>docker-compose.yml; .env.example; frontend/.env.example; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-PATTERNS.md</read_first>
  <action>Update `docker-compose.yml` additively. Add `profiles: ["full"]` to all 13 Spring Boot services (`eureka-server`, `config-server`, `api-gateway`, `identity-service`, `product-service`, `inventory-service`, `cart-service`, `order-service`, `payment-service`, `notification-service`, `search-service`, `ai-service`, `mcp-server`) and to a new `frontend` service. Keep `postgres` and `rabbitmq` profile-free so dependencies start automatically. For backend image names, preserve local default but allow GHCR pulls with env interpolation: `${IMAGE_REGISTRY:-n11}/service-name:${IMAGE_TAG:-dev}` for each service. Add `frontend` service using image `node:24-alpine`, `working_dir: /app`, bind mount `./frontend:/app`, command `sh -c "npm ci && npm run dev -- --host 0.0.0.0"`, env `VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:9090}`, port `5173:5173`, depends_on api-gateway healthy, network `n11-net`, and `restart: unless-stopped`. Do not expose internal Spring service ports except the existing gateway host port `9090:8080`, eureka/config dev ports, Postgres, and RabbitMQ. Update `.env.example` with blank/no-secret placeholders: `IMAGE_REGISTRY=n11`, `IMAGE_TAG=dev`, `VITE_API_BASE_URL=http://localhost:9090`, `CLOUDFLARE_TUNNEL_TOKEN=`, `NGROK_AUTHTOKEN=`, `DEMO_TUNNEL_HOSTNAME=`, `SLACK_WEBHOOK_URL=`. Update `frontend/.env.example` default to `VITE_API_BASE_URL=http://localhost:9090` to match compose gateway host port.</action>
  <verify>
    <automated>docker compose --profile full config >/tmp/n11-compose-full.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "profiles:" docker-compose.yml` returns at least 14 matches after implementation.
    - `grep -R "IMAGE_REGISTRY" docker-compose.yml .env.example` returns at least one match in each file.
    - `grep -R "node:24-alpine" docker-compose.yml` returns one match.
    - `grep -R "CLOUDFLARE_TUNNEL_TOKEN=" .env.example` returns one match.
    - `grep -R "NGROK_AUTHTOKEN=" .env.example` returns one match.
    - `docker compose --profile full config >/tmp/n11-compose-full.yml` exits 0.
  </acceptance_criteria>
  <done>Compose can render a full-profile stack with tag-selectable images, a frontend demo service, and secret-free tunnel/env placeholders.</done>
</task>

<task type="auto">
  <name>task 2: write root README demo runbook</name>
  <files>README.md, scripts/verify-demo-tunnel.sh</files>
  <read_first>.planning/PROJECT.md; .planning/ROADMAP.md; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-CONTEXT.md; .planning/phases/11-frontend-chat-assistant-devops-deploy/11-UI-SPEC.md; payment-service/README.md; docker-compose.yml; .env.example; .github/workflows/release.yml</read_first>
  <action>Create root `README.md` because none exists at project root. Start with project title and a top section `## 30-second demo path` containing exactly these command/proof lines: `cp .env.example .env`, `./gradlew :eureka-server:jibDockerBuild :config-server:jibDockerBuild :api-gateway:jibDockerBuild :identity-service:jibDockerBuild :product-service:jibDockerBuild :inventory-service:jibDockerBuild :cart-service:jibDockerBuild :order-service:jibDockerBuild :payment-service:jibDockerBuild :notification-service:jibDockerBuild :search-service:jibDockerBuild :ai-service:jibDockerBuild :mcp-server:jibDockerBuild`, `docker compose --profile full up`, and `DEMO_TUNNEL_HOSTNAME=&lt;tunnel-hostname&gt; scripts/verify-demo-tunnel.sh`. Create executable `scripts/verify-demo-tunnel.sh` that requires `DEMO_TUNNEL_HOSTNAME`, strips any leading `https://`, runs `curl -fsS -o /tmp/n11-products-tunnel.json -w '%{http_code}' "https://${DEMO_TUNNEL_HOSTNAME}/api/v1/products"`, exits nonzero unless the status is exactly `200`, and prints `DEV-05 tunnel proof passed: https://${DEMO_TUNNEL_HOSTNAME}/api/v1/products returned 200`. Add sections: `Environment matrix` with required secrets (`GEMINI_API_KEY`, `IYZICO_API_KEY`, `IYZICO_SECRET_KEY`, `JWT_PRIVATE_KEY`, `SLACK_WEBHOOK_URL`, `CLOUDFLARE_TUNNEL_TOKEN`/`NGROK_AUTHTOKEN`) and public config (`PUBLIC_BASE_URL`, `VITE_API_BASE_URL`, `IMAGE_REGISTRY`, `IMAGE_TAG`, `DEMO_TUNNEL_HOSTNAME`); `Cloudflare Tunnel (primary)`; `ngrok fallback`; `GHCR release images` explaining `IMAGE_REGISTRY=ghcr.io/&lt;owner&gt;/&lt;repo&gt;` and `IMAGE_TAG=&lt;vX.Y.Z&gt;` or `latest`; `Iyzico sandbox demo` with test card `5528 7900 0000 0008`; `AI assistant demo path` mentioning `Yapay Zeka Alışveriş Asistanı`, `MacBook ara`, and chat cart badge update; `MCP external-agent demo` referencing `/mcp/**` through the same gateway tunnel; `Slack notifications` with exact examples `✅ build green on &lt;ref&gt;` and `❌ build failed on &lt;ref&gt;`; `Troubleshooting` including tunnel/callback and stale Jib image notes. Use `&lt;set-in-env&gt;` placeholders only; do not include real secrets.</action>
  <verify>
    <automated>test -f README.md &amp;&amp; test -x scripts/verify-demo-tunnel.sh &amp;&amp; grep -q "docker compose --profile full up" README.md &amp;&amp; grep -q "DEMO_TUNNEL_HOSTNAME=&lt;tunnel-hostname&gt; scripts/verify-demo-tunnel.sh" README.md &amp;&amp; grep -q "/api/v1/products" scripts/verify-demo-tunnel.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "30-second demo path" README.md` returns one match.
    - `grep -R "docker compose --profile full up" README.md` returns at least one match.
    - `grep -R "DEMO_TUNNEL_HOSTNAME=&lt;tunnel-hostname&gt; scripts/verify-demo-tunnel.sh" README.md` returns one match.
    - `grep -R "https://\${DEMO_TUNNEL_HOSTNAME}/api/v1/products" scripts/verify-demo-tunnel.sh` returns one match.
    - `DEMO_TUNNEL_HOSTNAME=&lt;real-cloudflare-or-ngrok-hostname&gt; scripts/verify-demo-tunnel.sh` returns 0 and prints HTTP 200 proof during demo rehearsal.
    - `grep -R "5528 7900 0000 0008" README.md` returns one match.
    - `grep -R "Yapay Zeka Alışveriş Asistanı" README.md` returns at least one match.
    - `grep -R "✅ build green on &lt;ref&gt;" README.md` returns one match.
    - `grep -R "&lt;set-in-env&gt;" README.md` returns at least one match.
  </acceptance_criteria>
  <done>Root README gives the candidate a scan-friendly one-command demo path, env matrix, executable tunnel proof, AI/MCP demos, and troubleshooting per D-16/DEV-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: verify public tunnel reaches products API</name>
  <files>scripts/verify-demo-tunnel.sh, README.md</files>
  <what-built>OpenCode added the full compose demo posture, Cloudflare/ngrok runbook, `DEMO_TUNNEL_HOSTNAME` env placeholder, and `scripts/verify-demo-tunnel.sh` for DEV-05.</what-built>
  <action>Run the executable tunnel proof after the local full stack and Cloudflare/ngrok tunnel are active. This checkpoint exists because the external hostname and tunnel session are outside repository source and must be confirmed against the user's live demo environment.</action>
  <how-to-verify>
    1. Start the full local stack: `docker compose --profile full up`.
    2. Start Cloudflare Tunnel primary or ngrok fallback so the public hostname routes to the local API gateway host port.
    3. Export the hostname without scheme: `export DEMO_TUNNEL_HOSTNAME=&lt;your-cloudflare-or-ngrok-hostname&gt;`.
    4. Run: `scripts/verify-demo-tunnel.sh`.
    5. Expected result: the script prints `DEV-05 tunnel proof passed: https://$DEMO_TUNNEL_HOSTNAME/api/v1/products returned 200` and exits 0.
  </how-to-verify>
  <verify>
    <automated>DEMO_TUNNEL_HOSTNAME=&lt;real-cloudflare-or-ngrok-hostname&gt; scripts/verify-demo-tunnel.sh</automated>
  </verify>
  <done>DEV-05 is proven when the public tunnel returns HTTP 200 from `/api/v1/products` through `scripts/verify-demo-tunnel.sh`.</done>
  <resume-signal>Type "approved" after the external curl proof returns 200, or paste the script output/error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet→gateway tunnel | External demo traffic reaches local gateway through Cloudflare/ngrok. |
| local `.env`→compose services | Secrets enter containers at runtime. |
| GHCR images→local compose | Candidate may pull release images from public/private registry by tag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | I | `.env.example` / README | mitigate | Use blank or `&lt;set-in-env&gt;` placeholders only; no real webhook, tunnel, JWT, Gemini, or Iyzico values. |
| T-11-05-02 | S | tunnel exposure | mitigate | README exposes only gateway URL; internal services remain without host ports except existing dev infra ports. |
| T-11-05-03 | T | image selection | mitigate | Compose supports `IMAGE_REGISTRY` and `IMAGE_TAG`; README documents immutable v* pinning instead of only latest. |
| T-11-05-04 | D | demo startup | mitigate | `restart: unless-stopped`, healthchecks, and 30-second rehearsal instructions reduce reboot/interview outage risk. |
</threat_model>

<verification>
Run `docker compose --profile full config >/tmp/n11-compose-full.yml`, grep acceptance checks, read README for required strings, and complete the DEV-05 checkpoint by running `scripts/verify-demo-tunnel.sh` against a real Cloudflare/ngrok hostname until `/api/v1/products` returns HTTP 200.
</verification>

<success_criteria>
The candidate has a validated local full-stack compose path, safe env placeholders, public tunnel instructions with external `/api/v1/products` HTTP 200 proof, and root demo README.
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-chat-assistant-devops-deploy/11-05-SUMMARY.md`.
</output>
