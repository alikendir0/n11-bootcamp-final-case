---
phase: 09
plan: 05
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - config-server/src/main/resources/config/api-gateway.yml
  - docker-compose.yml
  - .env.example
  - .planning/api-contracts.md
autonomous: true
requirements: [AI-12]
requirements_addressed: [AI-12]

must_haves:
  truths:
    - "api-gateway exposes /mcp/** with response-timeout: -1 (Streamable HTTP holds long connections)"
    - "/mcp/** is NOT in the public allowlist — every request must carry a valid Bearer JWT"
    - "docker-compose has an mcp-server service block (depends on identity-service + eureka-server + config-server; NO postgres dep, NO ports mapping)"
    - ".env.example carries MCP_API_KEY= placeholder; .env is gitignored"
    - "api-contracts.md §2 routing table lists /mcp/** → mcp-server"
  artifacts:
    - path: "config-server/src/main/resources/config/api-gateway.yml"
      provides: "/mcp/** route with response-timeout: -1, StripPrefix=0, PreserveHostHeader=true"
      contains: "id: mcp-server"
    - path: "docker-compose.yml"
      provides: "mcp-server service block (port 8090, healthcheck, depends_on)"
      contains: "mcp-server:"
    - path: ".env.example"
      provides: "MCP_API_KEY placeholder"
      contains: "MCP_API_KEY"
  key_links:
    - from: "api-gateway"
      to: "mcp-server"
      via: "Spring Cloud Gateway lb://MCP-SERVER predicate Path=/mcp/**"
      pattern: "lb://MCP-SERVER"
    - from: "docker-compose mcp-server service"
      to: ".env MCP_API_KEY"
      via: "environment: MCP_API_KEY=${MCP_API_KEY}"
      pattern: 'MCP_API_KEY=\$\{MCP_API_KEY'
---

<objective>
Open the gateway-front-door for mcp-server: explicit `/mcp/**` route entry in `config-server/src/main/resources/config/api-gateway.yml` with `response-timeout: -1` (Streamable HTTP holds connections open the same way Phase 8's SSE route does), an `mcp-server` service block in `docker-compose.yml` that depends on identity-service + eureka-server + config-server (NO postgres, NO ports mapping — internal-only), and an `.env.example` placeholder for `MCP_API_KEY`. Also update `.planning/api-contracts.md` §2 routing table to list the activated route.

Purpose: Without this plan, mcp-server runs but is unreachable through the gateway. The Cloudflare Tunnel hostname (D-03) routes `/mcp/**` to the gateway; the gateway needs an explicit route to know mcp-server exists (discovery-locator alone routes under `/mcp-server/**`, not `/mcp/**`).

Output: Gateway routes `/mcp/**` → mcp-server, compose up brings mcp-server healthy alongside the rest of the stack, `MCP_API_KEY` placeholder visible in `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcp-server/09-CONTEXT.md
@.planning/phases/09-mcp-server/09-RESEARCH.md
@.planning/phases/09-mcp-server/09-PATTERNS.md
@.planning/api-contracts.md
@./CLAUDE.md
@config-server/src/main/resources/config/api-gateway.yml
@docker-compose.yml

<interfaces>
<!-- Existing api-gateway route shapes the executor mirrors -->

ai-service-chat-stream block (api-gateway.yml lines 108-117) — analog for response-timeout:-1 + PreserveHostHeader:
```yaml
- id: ai-service-chat-stream
  uri: lb://AI-SERVICE
  predicates:
    - Path=/api/v1/chat/stream/**
  metadata:
    response-timeout: -1
    connect-timeout: 5000
  filters:
    - StripPrefix=2
    - PreserveHostHeader=true
```

ai-service compose block (docker-compose.yml lines 465-492) — analog without postgres:
```yaml
ai-service:
  image: n11/ai-service:dev
  env_file: [.env]
  environment:
    SPRING_CONFIG_IMPORT: configserver:http://config-server:8888?fail-fast=true&...
    EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
    SPRING_PROFILES_ACTIVE: docker
  depends_on: { postgres: ..., eureka-server: ..., config-server: ..., identity-service: ... }
  healthcheck: ...
  restart: unless-stopped
  networks: [n11-net]
```

mcp-server-specific differences:
- StripPrefix=0 (the path `/mcp` is the full path; nothing to strip — Streamable HTTP endpoint IS `/mcp` per RESEARCH.md Pattern 6).
- NO postgres depends_on (mcp-server has no DB — Plan 09-01 scope).
- Healthcheck port = 8090.
- Add `MCP_API_KEY` and `MCP_TRANSPORT` env entries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api-gateway.yml /mcp/** route block + Springdoc decision</name>
  <files>config-server/src/main/resources/config/api-gateway.yml</files>
  <read_first>
    - config-server/src/main/resources/config/api-gateway.yml (read full file; the route insertion point is the `routes:` list inside `spring.cloud.gateway.server.webflux.routes`)
    - .planning/phases/09-mcp-server/09-RESEARCH.md (Pattern 6: api-gateway Route Addition — exact YAML block; verifies `response-timeout: -1` matches ai-service-chat-stream pattern)
    - .planning/phases/09-mcp-server/09-PATTERNS.md (Pattern: api-gateway route with response-timeout override; analog: ai-service-chat-stream block lines 108-117)
    - .planning/phases/09-mcp-server/09-CONTEXT.md (D-02 Streamable HTTP; D-03 share gateway tunnel; D-11 mcp-server forwards X-User-Id NOT raw JWT — this is enforced by the gateway's existing GatewayHeaderInjectionFilter from Phase 3)
  </read_first>
  <action>
1. Open `config-server/src/main/resources/config/api-gateway.yml`. Locate the existing `routes:` list (under `spring.cloud.gateway.server.webflux.routes:`). After the last route entry (`ai-service-chat` block ending at line ~126), insert a new route block:

```yaml
            # Phase 9 (Plan 09-05 / D-02 / D-03): mcp-server Streamable HTTP transport.
            # api-contracts.md §2 routing table: /mcp/** → mcp-server.
            #
            # response-timeout: -1 — Streamable HTTP holds the response stream
            # open while the LLM reads tool results (same pattern as Phase 8
            # ai-service-chat-stream above). Without this override, the
            # gateway's 60s default would close the stream mid-tool-call.
            #
            # StripPrefix=0 — the Streamable HTTP endpoint IS the literal `/mcp`
            # path on mcp-server (NOT a sub-path). RESEARCH.md Pattern 6 verified
            # against Spring AI's `streamable-http.mcp-endpoint: /mcp` default.
            #
            # No allowlist entry — /mcp/** falls through to anyExchange().authenticated().
            # External MCP clients MUST already carry a valid Bearer JWT obtained
            # via /agents/exchange (Plan 09-02 / D-05). The gateway validates the
            # JWT via the existing oauth2ResourceServer config (Phase 3) and
            # injects X-User-Id downstream — exactly the same path a logged-in
            # user takes (CLAUDE.md auth posture).
            - id: mcp-server
              uri: lb://MCP-SERVER
              predicates:
                - Path=/mcp/**
              metadata:
                response-timeout: -1
                connect-timeout: 5000
              filters:
                - StripPrefix=0
                - PreserveHostHeader=true
```

2. Do NOT add a Springdoc aggregator entry for mcp-server (per RESEARCH.md "Springdoc surface for mcp-server: SKIP — MCP is JSON-RPC, no REST surface to document"). Do NOT touch the `springdoc:` block.

3. Do NOT add a public allowlist entry for `/mcp/**`. Per CONTEXT.md `<canonical_refs>` and api-contracts.md §3, `/mcp/**` MUST require auth at the gateway. The gateway's existing `anyExchange().authenticated()` rule from Phase 3 covers this.

4. **Stale-Jib-image hazard reminder (Plan 01-06 lesson):** Editing `config-server/src/main/resources/config/*.yml` requires `./gradlew :config-server:jibDockerBuild` BEFORE `docker compose up -d` for the change to be served. Document this in the Plan 09-07 RUNBOOK; do not run gradle in this task — Plan 09-07's smoke step covers the rebuild.
  </action>
  <verify>
    <automated>grep -c "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml | grep -q '^1$' &amp;&amp; grep -A 8 "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml | grep -q "lb://MCP-SERVER" &amp;&amp; grep -A 10 "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml | grep -q "Path=/mcp/\*\*" &amp;&amp; grep -A 12 "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml | grep -q "response-timeout: -1" &amp;&amp; grep -A 14 "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml | grep -q "StripPrefix=0"</automated>
  </verify>
  <done>
    - api-gateway.yml has exactly one `id: mcp-server` route entry with `uri: lb://MCP-SERVER`, `Path=/mcp/**`, `response-timeout: -1`, `StripPrefix=0`, `PreserveHostHeader=true`.
    - No Springdoc entry for mcp-server.
    - No public allowlist entry for `/mcp/**`.
  </done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml mcp-server service block</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml lines 460-492 (the ai-service block — closest analog: Spring Boot service, healthcheck on /actuator/health, n11-net network, env_file .env, no postgres dep)
    - docker-compose.yml lines 494-525 (the search-service block — even closer match: stateless, no DB, no AMQP, no ports)
    - .planning/phases/09-mcp-server/09-PATTERNS.md (Pattern: docker-compose service entry — exact analog and adaptation notes)
    - .planning/phases/09-mcp-server/09-CONTEXT.md (D-08 — MCP_API_KEY via env; D-01 — MCP_TRANSPORT=http for compose; CLAUDE.md Rule #5 — no plaintext secret in compose; we use ${MCP_API_KEY:-} substitution)
    - .planning/phases/09-mcp-server/09-RESEARCH.md (Runtime State Inventory — env vars to add)
  </read_first>
  <action>
1. Open `docker-compose.yml`. Locate the `search-service:` block (lines ~498-525). After it ends (after the `networks: [n11-net]` line), append a new `mcp-server:` service block:

```yaml
  # ---------------------------------------------------------------------------
  # mcp-server -- Phase 9 (port 8090).
  # Re-exposes the agent-toolset over the MCP wire protocol via Spring AI 1.1.5.
  # Streamable HTTP is the docker-compose transport (D-01 / D-02); stdio is
  # available via SAME image when launched by Claude Desktop with `docker run -i`
  # (D-04). MCP_API_KEY is exchanged for an internal JWT against identity-service
  # at first tool call (D-07 / Plan 09-04 AgentJwtCache).
  # No postgres dep (stateless), no AMQP dep (no events).
  # Requires: ./gradlew :mcp-server:jibDockerBuild before docker compose up
  # ---------------------------------------------------------------------------
  mcp-server:
    image: n11/mcp-server:dev
    container_name: n11-mcp-server
    env_file:
      - .env
    environment:
      SPRING_CONFIG_IMPORT: configserver:http://config-server:8888?fail-fast=true&max-attempts=10&max-interval=1500&multiplier=1.2&initial-interval=1100
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka-server:8761/eureka/
      SPRING_PROFILES_ACTIVE: docker
      # D-01: compose runs HTTP transport. Claude Desktop overrides via `docker run -e`.
      MCP_TRANSPORT: http
      MCP_STDIO_ENABLED: "false"
      # D-08: API key injected from .env (gitignored). The placeholder default
      # `${MCP_API_KEY:-}` allows compose to start even when .env is missing,
      # but mcp-server's AgentJwtClient throws IllegalStateException if the key
      # is empty when the first tool call arrives (Plan 09-04 Task 1).
      MCP_API_KEY: ${MCP_API_KEY:-}
    depends_on:
      eureka-server:
        condition: service_healthy
      config-server:
        condition: service_healthy
      identity-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8090/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 5s
      timeout: 3s
      retries: 6
      start_period: 25s
    restart: unless-stopped
    networks:
      - n11-net
```

2. Do NOT add a `ports:` mapping — mcp-server is internal-only; the gateway is the public face (Pitfall #14, established convention).

3. Do NOT add `postgres: { condition: service_healthy }` — mcp-server has no DB.

4. Do NOT add `rabbitmq: { condition: service_healthy }` — mcp-server has no AMQP.

5. Verify additivity (Plan 01-05 lesson): `grep -c '^  mcp-server:' docker-compose.yml` returns exactly 1 after the edit.
  </action>
  <verify>
    <automated>grep -c '^  mcp-server:' docker-compose.yml | grep -q '^1$' &amp;&amp; grep -A 30 '^  mcp-server:' docker-compose.yml | grep -q 'image: n11/mcp-server:dev' &amp;&amp; grep -A 30 '^  mcp-server:' docker-compose.yml | grep -q 'MCP_TRANSPORT: http' &amp;&amp; grep -A 30 '^  mcp-server:' docker-compose.yml | grep -q 'MCP_API_KEY: \${MCP_API_KEY' &amp;&amp; grep -A 30 '^  mcp-server:' docker-compose.yml | grep -q 'localhost:8090/actuator/health' &amp;&amp; ! grep -A 30 '^  mcp-server:' docker-compose.yml | grep -q 'postgres:' &amp;&amp; docker compose config 2>&amp;1 | grep -q 'mcp-server'</automated>
  </verify>
  <done>
    - `docker-compose.yml` has exactly one `mcp-server:` service block.
    - Block declares `image: n11/mcp-server:dev`, `env_file: [.env]`, env vars `MCP_TRANSPORT=http`, `MCP_STDIO_ENABLED=false`, `MCP_API_KEY=${MCP_API_KEY:-}`, `SPRING_CONFIG_IMPORT=configserver:...`, `EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=...`.
    - depends_on is exactly { eureka-server, config-server, identity-service } — no postgres, no rabbitmq.
    - Healthcheck targets `localhost:8090/actuator/health`.
    - No `ports:` mapping.
    - `docker compose config` (validation parse) exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: .env.example MCP_API_KEY placeholder</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (read full file; place MCP_API_KEY in the appropriate section — likely alongside GEMINI_API_KEY or similar Phase 8 / 9 secrets)
    - .planning/phases/09-mcp-server/09-CONTEXT.md (D-08 — single-key-per-process; CLAUDE.md Rule #5 — secrets via env only)
  </read_first>
  <action>
1. Open `.env.example`. Add the MCP_API_KEY line in the Phase 8/9 section (or at the bottom under a Phase 9 banner):

```
# ─── Phase 9: MCP Server ──────────────────────────────────────────────
# Demo agent API key. Generated at first identity-service boot
# (AgentSeedRunner — Plan 09-02 Task 5) and logged ONCE at WARN level.
# Copy from `docker compose logs identity-service | grep "MCP_API_KEY="`
# into your local .env. The plaintext is NEVER stored anywhere except
# .env (gitignored). On every demo cycle, regenerate by truncating the
# agent_api_keys table in identity-service and restarting (the runner
# re-seeds + re-logs).
MCP_API_KEY=
```

2. Verify the file remains a clean shell-style env file (no JSON, no YAML, no quoted multi-line values). Check for committed secrets via:

```bash
! grep -E 'MCP_API_KEY=[a-zA-Z0-9_-]{20,}' .env.example
```

The placeholder must be empty (`MCP_API_KEY=`).
  </action>
  <verify>
    <automated>test -f .env.example &amp;&amp; grep -q "^MCP_API_KEY=$" .env.example &amp;&amp; ! grep -E 'MCP_API_KEY=[a-zA-Z0-9_-]{20,}' .env.example</automated>
  </verify>
  <done>
    - `.env.example` contains a `MCP_API_KEY=` (empty) placeholder with a Phase 9 banner comment block.
    - No real plaintext key committed.
    - File remains a clean dotenv (no JSON / YAML drift).
  </done>
</task>

<task type="auto">
  <name>Task 4: api-contracts.md §2 routing table — activate /mcp/** entry</name>
  <files>.planning/api-contracts.md</files>
  <read_first>
    - .planning/api-contracts.md (read full file; locate §2 routing table — CONTEXT.md says `/mcp/**` is already reserved there, just needs activation; locate §3 public allowlist — verify mcp-server adds NO entries)
    - .planning/phases/09-mcp-server/09-CONTEXT.md (canonical_refs §api-contracts.md — exact section numbers)
    - .planning/phases/09-mcp-server/09-RESEARCH.md (D-02 Streamable HTTP — supersedes any "HTTP+SSE" wording in current api-contracts)
  </read_first>
  <action>
1. Open `.planning/api-contracts.md`. Locate §2 (routing table). The `/mcp/**` row currently is either a placeholder or reserved-but-inactive. Update it to read exactly:

   - Pattern: `/mcp/**`
   - Service: `mcp-server`
   - StripPrefix: `0`
   - Auth: `JWT Bearer (exchanged via /agents/exchange — Plan 09-02; never raw API key)`
   - Notes: `Streamable HTTP (MCP spec 2025-06-18); response-timeout: -1; same tunnel hostname as the rest of the gateway (D-03).`

2. Locate §3 (public allowlist). Verify `/mcp/**` is NOT listed there. If it is listed (placeholder), REMOVE it — every MCP request must already carry a Bearer JWT.

3. Locate §4 (auth strip + X-User-Id injection). No edit needed — the existing rule applies to mcp-server too (gateway strips Authorization, injects X-User-Id from JWT.sub before forwarding). Confirm by reading and noting in the SUMMARY.

4. Locate §6 (SSE caveat). Add (or amend) a one-line note: `MCP Streamable HTTP follows the same response-timeout: -1 + no body filters posture as ai-service SSE — applied by Plan 09-05.`

5. Run `git diff .planning/api-contracts.md` and confirm only those two-to-three rows changed.
  </action>
  <verify>
    <automated>grep -q '/mcp/\*\*' .planning/api-contracts.md &amp;&amp; grep -q 'Streamable HTTP' .planning/api-contracts.md &amp;&amp; ! grep -E '/mcp/\*\*.*public|/mcp/\*\*.*allow' .planning/api-contracts.md</automated>
  </verify>
  <done>
    - `.planning/api-contracts.md` §2 has the `/mcp/** → mcp-server` row finalized with StripPrefix=0, JWT auth, Streamable HTTP note.
    - §3 does NOT list `/mcp/**` (removed if it was a placeholder).
    - §6 SSE caveat note mentions Streamable HTTP applies the same posture.
    - Doc edits are limited to the affected rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External MCP client → api-gateway /mcp/** | Untrusted; gateway validates Bearer JWT via existing oauth2ResourceServer (Phase 3) |
| api-gateway → mcp-server lb://MCP-SERVER | Internal Docker network; gateway has stripped Authorization and injected X-User-Id |
| docker host → .env file | Filesystem; .env is gitignored, contains plaintext MCP_API_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Spoofing | /mcp/** | mitigate | No public allowlist entry — gateway requires JWT for every request. Per Phase 3 SecurityConfig, anyExchange().authenticated() applies. |
| T-09-20 | Tampering | api-gateway.yml | accept | YAML is config-server-served at JAR build time; immutable post-Jib. |
| T-09-21 | Information Disclosure | docker-compose env | accept | MCP_API_KEY in env vars is the v1 demo posture. .env is gitignored. Production posture (out of scope) would use Spring Cloud Config encryption or Docker secrets. |
| T-09-22 | DoS | Streamable HTTP long-held connections | mitigate | response-timeout: -1 + no retry filters — same posture as Phase 8 SSE; gateway memory-budget for in-flight streams covered by Spring Cloud Gateway defaults. |
</threat_model>

<verification>
1. `grep -c "id: mcp-server" config-server/src/main/resources/config/api-gateway.yml` returns 1.
2. `grep -c '^  mcp-server:' docker-compose.yml` returns 1.
3. `grep -q "^MCP_API_KEY=$" .env.example` succeeds (placeholder empty).
4. `grep -q '/mcp/\*\*' .planning/api-contracts.md` succeeds; `! grep -E '/mcp/\*\*.*public' .planning/api-contracts.md` confirms no public allowlist entry.
5. `docker compose config` (parse-only validation) exits 0 and shows mcp-server in the service list.
6. Manual (Plan 09-07 covers): after `./gradlew :config-server:jibDockerBuild :mcp-server:jibDockerBuild` + `docker compose up -d`, `curl -s http://localhost:9090/mcp -X POST` returns 401 without JWT and 200 (or 4xx with body) with a valid Bearer JWT.
</verification>

<success_criteria>
- /mcp/** routed by gateway with `response-timeout: -1`.
- mcp-server boots via `docker compose up` and reaches healthy state.
- MCP_API_KEY placeholder visible in .env.example; .env unchanged in source.
- api-contracts.md §2 routing entry activated; §3 allowlist explicitly does NOT include /mcp/**.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mcp-server/09-05-SUMMARY.md` capturing:
- The exact insertion line numbers in api-gateway.yml (so Plan 09-07's smoke runbook can reference them).
- Whether `docker compose up -d mcp-server` reached `(healthy)` within the 25s start_period budget on the first try.
- Any deviation from the planned `StripPrefix=0` (if the actual mcp-server endpoint path differs from what RESEARCH.md documented).
- Confirmed that the existing gateway SecurityConfig (`anyExchange().authenticated()`) catches `/mcp/**` without a per-route allow rule.
</output>
