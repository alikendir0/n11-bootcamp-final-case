# Deferred Items — Phase 06 Payment Iyzico

| ID | Category | Item | Discovered | Status |
|----|----------|------|------------|--------|
| D-06-01 | Test infrastructure | `./gradlew :payment-service:test` starts AMQP infrastructure during `StockReservedConsumerIntegrationTest` and fails without a RabbitMQ test container/credentials (`AmqpAuthenticationException`). Plan-specific tests pass; this appears unrelated to 06-01 SDK/config/schema/contract changes and should be fixed when Phase 6 revisits the stock-reserved consumer flow. | 06-01 overall verification | OPEN |
| D-06-02 | Test regression | `infra-tests:test --tests '*SagaHappyPathE2ETest'` fails with `BeanInstantiationException: DefaultIyzicoCheckoutClient — No default constructor found`. Root cause: Plans 06-03/06-04 added the Iyzico SDK client to payment-service; the `PaymentServiceTestConfig` in infra-tests boots payment-service against a Testcontainers Postgres + RabbitMQ but does not provide `IyzicoProperties`/`Options`/initializer/retriever beans, so Spring tries to default-construct `DefaultIyzicoCheckoutClient` and fails. Out of scope for Plan 06-05 (timeout + compensation E2E). Fix in a future plan: either provide the missing IyzicoCheckoutClient @MockBean in `SagaHappyPathE2ETest`, or extract the saga seam so payment.completed publishing does not require the SDK client beans. | 06-05 verification regression sweep | OPEN |
