---
phase: 06-payment-iyzico
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - payment-service/src/main/java/com/n11/payment/iyzico/DefaultIyzicoCheckoutClient.java
  - payment-service/src/main/java/com/n11/payment/messaging/PaymentSagaService.java
  - payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java
  - payment-service/src/main/java/com/n11/payment/payment/Payment.java
  - payment-service/src/test/java/com/n11/payment/messaging/StockReservedConsumerIntegrationTest.java
  - payment-service/src/test/java/com/n11/payment/iyzico/IyzicoCheckoutRequestMapperTest.java
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]
must_haves:
  truths:
    - "PAY-02: consuming stock.reserved initializes one Iyzico Checkout Form session"
    - "D-05: checkout starts automatically after stock reservation, not from the client"
    - "D-09: repeated stock.reserved delivery reuses active checkout and does not create a second session"
  artifacts:
    - path: "payment-service/src/main/java/com/n11/payment/iyzico/DefaultIyzicoCheckoutClient.java"
      provides: "Concrete CheckoutFormInitialize SDK mapping"
      contains: "CheckoutFormInitialize.create"
    - path: "payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java"
      provides: "PENDING payment persistence and idempotent checkout reuse"
      contains: "paymentPageUrl"
  key_links:
    - from: "payment-service/src/main/java/com/n11/payment/messaging/StockReservedConsumer.java"
      to: "Iyzico Checkout Form"
      via: "PaymentSagaService -> IyzicoCheckoutClient.initialize"
      pattern: "initialize"
---

<objective>
Replace the Phase 5 mock payment-completed producer with real Checkout Form initialization that persists a pending payment link.

Purpose: This is the core PAY-01/PAY-02 behavior and the basis for future frontend/tooling `get_payment_link` flows.
Output: SDK mapper, pending payment persistence, and idempotency tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-payment-iyzico/06-RESEARCH.md
@payment-service/src/main/java/com/n11/payment/messaging/PaymentSagaService.java
@payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java
@payment-service/src/test/java/com/n11/payment/messaging/StockReservedConsumerIntegrationTest.java
@payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: implement Checkout Form initialize mapper</name>
  <files>payment-service/src/main/java/com/n11/payment/iyzico/DefaultIyzicoCheckoutClient.java, payment-service/src/test/java/com/n11/payment/iyzico/IyzicoCheckoutRequestMapperTest.java</files>
  <read_first>.planning/phases/06-payment-iyzico/06-RESEARCH.md, payment-service/src/main/java/com/n11/payment/iyzico/IyzicoCheckoutClient.java, payment-service/src/main/java/com/n11/payment/order/OrderPaymentContext.java</read_first>
  <behavior>
    - Test 1: generated Iyzico request uses `Locale.TR`, `Currency.TRY`, `PaymentGroup.PRODUCT`, callback URL from D-02.
    - Test 2: request includes enabled installments `2,3,6,9` and physical basket items.
    - Test 3: adapter verifies initialize response signature and rejects non-success responses.
  </behavior>
  <action>Implement `DefaultIyzicoCheckoutClient` using `CheckoutFormInitialize.create(request, options)` from the official Java sample. Map order context to Buyer, Address, and BasketItem. For demo buyer fields that do not exist in order snapshot, use deterministic config-backed values: identity number from `iyzico.demo-buyer-identity-number`, email as `{userId}@n11clone.local`, name/surname split from shipping `recipientName` with surname fallback `Müşteri`, country `Turkey`, city from `il`, registration address from `mahalle + streetLine + ilce + il`. Set `price` and `paidPrice` to total amount, `currency=TRY`, `locale=tr`, `paymentGroup=PRODUCT`, `callbackUrl=IyzicoProperties.callbackUrl()`, `debitCardAllowed=true`, and enabled installments `[2,3,6,9]`. Return only token and paymentPageUrl; do not expose checkoutFormContent per D-07.</action>
  <verify><automated>./gradlew :payment-service:test --tests '*IyzicoCheckoutRequestMapperTest'</automated></verify>
  <acceptance_criteria>`DefaultIyzicoCheckoutClient.java` contains `CheckoutFormInitialize.create`; test asserts `Locale.TR`, `Currency.TRY`, and `/api/v1/payments/iyzico/callback`.</acceptance_criteria>
  <done>Adapter can initialize hosted checkout from internal payment context.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: initialize checkout from stock.reserved and persist pending link</name>
  <files>payment-service/src/main/java/com/n11/payment/messaging/PaymentSagaService.java, payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java, payment-service/src/test/java/com/n11/payment/messaging/StockReservedConsumerIntegrationTest.java</files>
  <read_first>payment-service/src/main/java/com/n11/payment/messaging/PaymentSagaService.java, payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java, payment-service/src/test/java/com/n11/payment/messaging/StockReservedConsumerIntegrationTest.java, payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java</read_first>
  <behavior>
    - Test 1: first `stock.reserved` creates exactly one PENDING payment with token and paymentPageUrl.
    - Test 2: redelivering the same event does not call Iyzico initialize again and does not create another row.
    - Test 3: no `payment.completed` outbox is written during initialization; completion waits for callback.
  </behavior>
  <action>Change `PaymentSagaService.processStockReserved` to fetch order payment context, call `IyzicoCheckoutClient.initialize`, and delegate persistence to the transactional service. Replace the Phase 5 mock `payment.completed` write in `PaymentTransactionalService.persistAndPublish` with `persistPendingCheckout`: inside one transaction, check `processed_events`; if duplicate, return. If an active PENDING payment already exists for the order, record the processed event and reuse it without SDK initialization. Otherwise save a new `Payment` with `PENDING`, `iyzicoToken`, `paymentPageUrl`, `expiresAt=now+timeoutMinutes`, amount/currency/orderId, then save processed event. Do not publish `payment.completed` until callback in Plan 04.</action>
  <verify><automated>./gradlew :payment-service:test --tests '*StockReservedConsumerIntegrationTest'</automated></verify>
  <acceptance_criteria>Updated test no longer expects status `COMPLETED` or `mock-`; it expects status `PENDING`, non-null `iyzicoToken`, non-null `paymentPageUrl`, one processed event, and zero `payment.completed` outbox rows.</acceptance_criteria>
  <done>PAY-02 is implemented: stock.reserved starts one hosted checkout session and records a reusable pending link.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RabbitMQ stock.reserved → payment-service | At-least-once event delivery triggers payment initialization |
| payment-service → Iyzico | Merchant credential API call creates hosted checkout |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | Iyzico initialize response | mitigate | Verify SDK signature before persisting token/link. |
| T-06-07 | Denial of Service | `stock.reserved` redelivery | mitigate | Processed-events and active-payment reuse prevent duplicate Checkout Form sessions. |
| T-06-08 | Information Disclosure | checkoutFormContent | mitigate | Do not expose or persist `checkoutFormContent` outside adapter per D-07. |
</threat_model>

<verification>`./gradlew :payment-service:test --tests '*IyzicoCheckoutRequestMapperTest' --tests '*StockReservedConsumerIntegrationTest'` passes.</verification>
<success_criteria>One stock reservation yields one pending Iyzico payment page URL and no premature completion event.</success_criteria>
<output>After completion, create `.planning/phases/06-payment-iyzico/06-03-SUMMARY.md`</output>
