---
phase: 06-payment-iyzico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - order-service/src/main/java/com/n11/order/order/PaymentContextController.java
  - order-service/src/main/java/com/n11/order/order/PaymentContextService.java
  - order-service/src/main/java/com/n11/order/order/dto/PaymentContextDto.java
  - order-service/src/test/java/com/n11/order/order/PaymentContextControllerTest.java
  - payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java
  - payment-service/src/main/java/com/n11/payment/order/OrderPaymentContext.java
  - config-server/src/main/resources/config/payment-service.yml
autonomous: true
requirements: [PAY-01, PAY-02]
must_haves:
  truths:
    - "Payment-service can obtain buyer/address/item data needed by Iyzico without cross-schema database access"
    - "The payment context endpoint is internal-service oriented and not exposed as a gateway route"
    - "D-05: stock.reserved remains the trigger; payment-service enriches from order-service internally"
  artifacts:
    - path: "order-service/src/main/java/com/n11/order/order/PaymentContextController.java"
      provides: "Internal order snapshot for payment initialization"
      contains: "@GetMapping(\"/internal/orders/{orderId}/payment-context\")"
    - path: "payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java"
      provides: "RestClient wrapper for order payment context"
      contains: "RestClient"
  key_links:
    - from: "payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java"
      to: "order-service/src/main/java/com/n11/order/order/PaymentContextController.java"
      via: "GET /internal/orders/{orderId}/payment-context"
      pattern: "payment-context"
---

<objective>
Add the internal order snapshot contract required to construct an Iyzico Checkout Form request.

Purpose: Iyzico requires buyer, address, and basket items; current `stock.reserved` intentionally carries only saga data. This plan preserves service boundaries by using internal REST instead of cross-schema joins.
Output: order-service payment context endpoint and payment-service client/DTO.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-payment-iyzico/06-CONTEXT.md
@.planning/phases/06-payment-iyzico/06-RESEARCH.md
@order-service/src/main/java/com/n11/order/order/Order.java
@order-service/src/main/java/com/n11/order/order/OrderItem.java
@order-service/src/main/java/com/n11/order/order/OrderShippingAddress.java
@order-service/src/main/java/com/n11/order/order/OrderQueryService.java
@payment-service/src/main/java/com/n11/payment/messaging/StockReservedConsumer.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: expose internal order payment context</name>
  <files>order-service/src/main/java/com/n11/order/order/PaymentContextController.java, order-service/src/main/java/com/n11/order/order/PaymentContextService.java, order-service/src/main/java/com/n11/order/order/dto/PaymentContextDto.java, order-service/src/test/java/com/n11/order/order/PaymentContextControllerTest.java</files>
  <read_first>order-service/src/main/java/com/n11/order/order/Order.java, order-service/src/main/java/com/n11/order/order/OrderItem.java, order-service/src/main/java/com/n11/order/order/OrderShippingAddress.java, order-service/src/main/java/com/n11/order/order/OrderQueryService.java</read_first>
  <behavior>
    - Test 1: existing order returns orderId, userId, totalAmount, currency, shipping address, and item lines.
    - Test 2: unknown orderId returns 404.
    - Test 3: endpoint path is `/internal/orders/{orderId}/payment-context`, not `/orders/{id}` and not a gateway route.
  </behavior>
  <action>Create `PaymentContextDto` with `orderId`, `userId`, `totalAmount`, `currency`, `ShippingAddress(recipientName, phone, il, ilce, mahalle, streetLine, postalCode, title)`, and `List<Item(productId, nameSnapshot, qty, unitPrice)>`. Create `PaymentContextService` using `OrderRepository`, `OrderItemRepository`, and `OrderShippingAddressRepository` to read only the order-service schema. Create `PaymentContextController` under `@RequestMapping("/internal/orders")` with `@GetMapping("/{orderId}/payment-context")`. This internal endpoint is intentionally not listed in `api-gateway.yml`; it is called by payment-service over the Docker network. Use opaque 404 for missing rows. Do not require JWT here because it is not publicly routed; the trust boundary remains the Docker mesh plus gateway-only external exposure.</action>
  <verify><automated>./gradlew :order-service:test --tests '*PaymentContextControllerTest'</automated></verify>
  <acceptance_criteria>`PaymentContextController.java` contains `/internal/orders`; `PaymentContextDto.java` contains `ShippingAddress` and `Item`; no `api-gateway.yml` payment context route is added.</acceptance_criteria>
  <done>order-service provides all fields payment-service needs for Checkout Form initialization.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add payment-service order context client</name>
  <files>payment-service/src/main/java/com/n11/payment/order/OrderPaymentContextClient.java, payment-service/src/main/java/com/n11/payment/order/OrderPaymentContext.java, config-server/src/main/resources/config/payment-service.yml</files>
  <read_first>order-service/src/main/java/com/n11/order/order/dto/PaymentContextDto.java, config-server/src/main/resources/config/payment-service.yml</read_first>
  <behavior>
    - Test 1: client calls `{baseUrl}/internal/orders/{orderId}/payment-context`.
    - Test 2: 404 from order-service becomes a payment initialization failure, not a retry loop.
  </behavior>
  <action>Create payment-service DTO `OrderPaymentContext` matching `PaymentContextDto`. Create `OrderPaymentContextClient` using Spring `RestClient` with configurable `app.clients.order.base-url` default `http://order-service:8085`. Method `getPaymentContext(UUID orderId)` calls `/internal/orders/{orderId}/payment-context`; on 404 throw a domain exception such as `PaymentInitializationException` with sanitized message. Add `app.clients.order.base-url: ${ORDER_SERVICE_BASE_URL:http://order-service:8085}` to `payment-service.yml`.</action>
  <verify><automated>./gradlew :payment-service:compileJava</automated></verify>
  <acceptance_criteria>`OrderPaymentContextClient.java` contains `/internal/orders/{orderId}/payment-context`; `payment-service.yml` contains `ORDER_SERVICE_BASE_URL`.</acceptance_criteria>
  <done>payment-service can fetch order payment context for Plan 03 initialization.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| payment-service → order-service internal REST | Internal mesh call retrieves PII needed for payment |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | `PaymentContextController` | mitigate | Do not add a gateway route; return only payment-required snapshot fields; keep direct service ports unexposed in docker-compose. |
| T-06-05 | Tampering | Internal payment context | accept | Internal route is mesh-only; payment-service still verifies final provider callback before terminal saga mutation. |
</threat_model>

<verification>`./gradlew :order-service:test --tests '*PaymentContextControllerTest'` and `./gradlew :payment-service:compileJava` pass.</verification>
<success_criteria>Iyzico-required buyer/address/basket data is available to payment-service without cross-schema joins or saga PII expansion.</success_criteria>
<output>After completion, create `.planning/phases/06-payment-iyzico/06-02-SUMMARY.md`</output>
