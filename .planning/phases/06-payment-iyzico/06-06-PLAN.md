---
phase: 06-payment-iyzico
plan: 06
type: execute
wave: 5
depends_on: [06-04, 06-05]
files_modified:
  - payment-service/README.md
  - .planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md
  - .env.example
  - config-server/src/main/resources/config/payment-service.yml
autonomous: false
requirements: [PAY-05, PAY-07]
user_setup:
  - service: cloudflare-tunnel
    why: "Public HTTPS callback for Iyzico sandbox and demo gateway"
    env_vars:
      - name: PUBLIC_BASE_URL
        source: "Cloudflare Tunnel hostname or ngrok forwarding URL"
    dashboard_config:
      - task: "If using Cloudflare primary path, create a tunnel and DNS route to the gateway host"
        location: "Cloudflare Zero Trust / cloudflared CLI"
  - service: iyzico-sandbox
    why: "Manual sandbox payment with hosted form"
    env_vars:
      - name: IYZICO_API_KEY
        source: "Iyzico Sandbox Dashboard"
      - name: IYZICO_SECRET_KEY
        source: "Iyzico Sandbox Dashboard"
must_haves:
  truths:
    - "D-01: Cloudflare Tunnel is documented as the primary public exposure path for Phase 6 callbacks and demo gateway URL"
    - "D-03/D-04/PAY-05: payment-service README documents Cloudflare primary and ngrok fallback setup"
    - "PAY-07: operator can execute a full sandbox card flow with `5528 7900 0000 0008`"
    - "The runbook proves callback reachability, final order CONFIRMED, and compensation fallback"
  artifacts:
    - path: "payment-service/README.md"
      provides: "Payment service runbook and env var matrix"
      contains: "PUBLIC_BASE_URL"
    - path: ".planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md"
      provides: "Human sandbox verification script"
      contains: "5528 7900 0000 0008"
  key_links:
    - from: "Cloudflare/ngrok public URL"
      to: "/api/v1/payments/iyzico/callback"
      via: "PUBLIC_BASE_URL"
      pattern: "PUBLIC_BASE_URL"
---

<objective>
Document and verify the real sandbox payment path through a public tunnel.

Purpose: PAY-07 cannot be fully automated without live Iyzico credentials and browser-hosted payment, so this plan creates the operator runbook and a blocking human verification checkpoint.
Output: README, smoke runbook, env matrix, and approval checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-payment-iyzico/06-CONTEXT.md
@.planning/phases/06-payment-iyzico/06-RESEARCH.md
@payment-service/README.md
@.env.example
</context>

<tasks>
<task type="auto">
  <name>task 1: write payment-service tunnel and Iyzico runbook</name>
  <files>payment-service/README.md, .env.example, config-server/src/main/resources/config/payment-service.yml</files>
  <read_first>.planning/phases/06-payment-iyzico/06-CONTEXT.md, .planning/phases/06-payment-iyzico/06-RESEARCH.md, .env.example, config-server/src/main/resources/config/payment-service.yml</read_first>
  <action>Create/update `payment-service/README.md` with sections: `## Environment`, `## Cloudflare Tunnel (primary)`, `## ngrok fallback`, `## Iyzico sandbox checkout`, `## Callback troubleshooting`. The env matrix must list `IYZICO_API_KEY`, `IYZICO_SECRET_KEY`, `PUBLIC_BASE_URL`, `PAYMENT_TIMEOUT_MINUTES`, `PAYMENT_TIMEOUT_SCAN_DELAY_MS`, and `IYZICO_DEMO_BUYER_IDENTITY_NUMBER`. Cloudflare instructions must include `cloudflared tunnel login`, `cloudflared tunnel create n11-demo`, config routing to `http://localhost:8080`, `cloudflared tunnel route dns`, and `cloudflared tunnel run`. ngrok fallback must include `ngrok config add-authtoken $NGROK_AUTHTOKEN` and `ngrok http 8080`. Update `.env.example` with placeholders only; do not add real secrets. Keep docker-compose cloudflared sidecar explicitly deferred to Phase 11 per D-03.</action>
  <verify><automated>grep -v '^#' payment-service/README.md | grep -c 'PUBLIC_BASE_URL' && grep -v '^#' .env.example | grep -c 'IYZICO_API_KEY'</automated></verify>
  <acceptance_criteria>`payment-service/README.md` contains `Cloudflare Tunnel (primary)`, `ngrok fallback`, `PUBLIC_BASE_URL`, and `5528 7900 0000 0008`; `.env.example` contains Iyzico placeholders with empty/example values only.</acceptance_criteria>
  <done>PAY-05 documentation is complete with primary and fallback tunnel paths.</done>
</task>

<task type="auto">
  <name>task 2: create Phase 6 live smoke runbook</name>
  <files>.planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md</files>
  <read_first>.planning/phases/05-cart-order-skeleton/05-05-SUMMARY.md, payment-service/README.md, .planning/ROADMAP.md</read_first>
  <action>Create `06-06-SMOKE-RUNBOOK.md` with exact commands/steps: preflight `./gradlew :payment-service:test :order-service:test`; Jib rebuild `./gradlew :config-server:jibDockerBuild :api-gateway:jibDockerBuild :payment-service:jibDockerBuild :order-service:jibDockerBuild :inventory-service:jibDockerBuild`; `docker compose up -d`; set/verify `PUBLIC_BASE_URL`; register/login/address/cart/order through gateway; poll `GET /api/v1/payments/{orderId}` until `paymentPageUrl`; open hosted Iyzico page; use test card `5528 7900 0000 0008`; complete OTP/3DS as sandbox instructs; verify callback hits `POST /api/v1/payments/iyzico/callback`; verify `GET /api/v1/orders/{orderId}` reaches CONFIRMED; verify failure/timeout path by forcing fake failure if sandbox decline card is available or by setting `PAYMENT_TIMEOUT_MINUTES=1` and asserting order CANCELLED + inventory RELEASED. Include a sign-off checklist with PASS/FAIL rows.</action>
  <verify><automated>test -f .planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md && grep -v '^#' .planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md | grep -c '5528 7900 0000 0008'</automated></verify>
  <acceptance_criteria>Smoke runbook contains `paymentPageUrl`, `iyzico/callback`, `CONFIRMED`, `CANCELLED`, `RELEASED`, and the exact card number.</acceptance_criteria>
  <done>Human operator has a deterministic script for PAY-07 verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: verify live Iyzico sandbox callback flow</name>
  <files>.planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md, payment-service/README.md</files>
  <what-built>OpenCode automated Iyzico Checkout Form integration, public callback routing, timeout compensation, Cloudflare/ngrok runbooks, and a live smoke script.</what-built>
  <action>Pause for human verification because the final PAY-07 proof requires live Iyzico sandbox credentials, a browser-hosted payment page, and a publicly reachable Cloudflare Tunnel/ngrok URL.</action>
  <how-to-verify>
    1. Provide sandbox `IYZICO_API_KEY`, `IYZICO_SECRET_KEY`, and a public `PUBLIC_BASE_URL` via Cloudflare Tunnel or ngrok.
    2. Follow `.planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md`.
    3. Confirm `GET /api/v1/payments/{orderId}` returns a `paymentPageUrl`.
    4. Complete the Iyzico hosted payment with `5528 7900 0000 0008`.
    5. Confirm Iyzico callback reaches payment-service and order status becomes `CONFIRMED`.
    6. Confirm a failure or timeout emits `payment.failed`, inventory releases stock, and order becomes `CANCELLED`.
  </how-to-verify>
  <verify><automated>test -f .planning/phases/06-payment-iyzico/06-06-SMOKE-RUNBOOK.md && test -f payment-service/README.md</automated></verify>
  <done>Human reports `approved` after the runbook proves payment link, callback, CONFIRMED order, and failure/timeout compensation.</done>
  <resume-signal>Type "approved" when PAY-07 passes, or paste the failing smoke step/log excerpt.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public tunnel → gateway | External internet reaches local gateway during demo |
| operator env → local stack | Secrets injected via environment |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | `.env.example`/README | mitigate | Placeholders only; explicitly state real keys live in gitignored `.env`. |
| T-06-16 | Spoofing | Public tunnel | mitigate | Expose gateway only; payment callback still verified by server-side retrieve/signature. |
| T-06-17 | Availability | Cloudflare setup failure | mitigate | ngrok fallback documented per D-04. |
</threat_model>

<verification>Automated grep checks pass, then human checkpoint approves the live sandbox flow.</verification>
<success_criteria>PAY-05 is documented and PAY-07 is manually verified through a real public callback and sandbox hosted payment.</success_criteria>
<output>After completion, create `.planning/phases/06-payment-iyzico/06-06-SUMMARY.md`</output>
