---
phase: 06-payment-iyzico
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - payment-service/src/main/java/com/n11/payment/payment/PaymentTimeoutJob.java
  - payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java
  - payment-service/src/test/java/com/n11/payment/payment/PaymentTimeoutJobTest.java
  - infra-tests/build.gradle.kts
  - infra-tests/src/test/java/com/n11/infratests/saga/PaymentFailureCompensationE2ETest.java
  - infra-tests/src/test/resources/db/migration/payment/V3__iyzico_checkout_fields.sql
autonomous: true
requirements: [PAY-03, PAY-06, QUAL-05]
must_haves:
  truths:
    - "PAY-06: stuck pending payments time out and emit payment.failed"
    - "QUAL-05: payment.failed compensation releases inventory stock and cancels the order"
    - "Timeout and Iyzico decline use the same downstream compensation event shape"
  artifacts:
    - path: "payment-service/src/main/java/com/n11/payment/payment/PaymentTimeoutJob.java"
      provides: "Scheduled timeout compensation trigger"
      contains: "@Scheduled"
    - path: "infra-tests/src/test/java/com/n11/infratests/saga/PaymentFailureCompensationE2ETest.java"
      provides: "Cross-service compensation proof"
      contains: "payment.failed"
  key_links:
    - from: "PaymentTimeoutJob"
      to: "payments.tx/payment.failed"
      via: "PaymentTransactionalService timeout terminal transition"
      pattern: "PAYMENT_TIMEOUT"
---

<objective>
Add payment timeout compensation and prove payment.failed releases stock and cancels the order.

Purpose: A hosted payment can hang; the saga must not leave reserved stock indefinitely.
Output: timeout scheduler, unit/integration tests, and infra compensation E2E.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/saga-contracts.md
@.planning/phases/05-cart-order-skeleton/05-04-SUMMARY.md
@.planning/phases/06-payment-iyzico/06-RESEARCH.md
@payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java
@inventory-service/src/main/java/com/n11/inventory/messaging/PaymentFailedConsumer.java
@order-service/src/main/java/com/n11/order/messaging/OrderSagaService.java
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: implement scheduled payment timeout job</name>
  <files>payment-service/src/main/java/com/n11/payment/payment/PaymentTimeoutJob.java, payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java, payment-service/src/test/java/com/n11/payment/payment/PaymentTimeoutJobTest.java</files>
  <read_first>payment-service/src/main/java/com/n11/payment/messaging/PaymentTransactionalService.java, payment-service/src/main/java/com/n11/payment/payment/PaymentRepository.java, config-server/src/main/resources/config/payment-service.yml</read_first>
  <behavior>
    - Test 1: expired PENDING payment becomes TIMED_OUT.
    - Test 2: timeout writes one `payment.failed` outbox row with reason `PAYMENT_TIMEOUT`.
    - Test 3: already COMPLETED/FAILED/TIMED_OUT payments are ignored.
  </behavior>
  <action>Create `PaymentTimeoutJob` with `@Scheduled(fixedDelayString="${payment.timeout.scan-delay-ms:60000}")`. It scans repository for `status=PENDING AND expires_at < now()` and calls a transactional timeout method. Add config `payment.timeout.scan-delay-ms: ${PAYMENT_TIMEOUT_SCAN_DELAY_MS:60000}`. The transactional timeout method must update by status guard PENDING only, set status TIMED_OUT, set failedAt/failureReason, and write `payment.failed` outbox payload with reason `PAYMENT_TIMEOUT` and errorCode `TIMEOUT`. Keep this path identical in event shape to callback failure.</action>
  <verify><automated>./gradlew :payment-service:test --tests '*PaymentTimeoutJobTest'</automated></verify>
  <acceptance_criteria>`PaymentTimeoutJob.java` contains `@Scheduled`; timeout test asserts status `TIMED_OUT` and one `payment.failed` outbox event containing `PAYMENT_TIMEOUT`.</acceptance_criteria>
  <done>PAY-06 timeout path emits compensation reliably.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add payment failure compensation E2E test</name>
  <files>infra-tests/build.gradle.kts, infra-tests/src/test/java/com/n11/infratests/saga/PaymentFailureCompensationE2ETest.java, infra-tests/src/test/resources/db/migration/payment/V3__iyzico_checkout_fields.sql</files>
  <read_first>infra-tests/src/test/java/com/n11/infratests/saga/SagaHappyPathE2ETest.java, inventory-service/src/main/java/com/n11/inventory/messaging/PaymentFailedConsumer.java, order-service/src/main/java/com/n11/order/messaging/OrderSagaService.java, .planning/phases/05-cart-order-skeleton/05-04-SUMMARY.md</read_first>
  <behavior>
    - Test 1: publishing a `payment.failed` envelope causes inventory reservation status RELEASED.
    - Test 2: publishing the same `payment.failed` twice leaves one side effect.
    - Test 3: order-service consumes the same event and order moves to CANCELLED with payment reason.
  </behavior>
  <action>Create or extend infra-tests to include payment V3 migration and a `PaymentFailureCompensationE2ETest`. Follow the established `PaymentServiceTestConfig`, non-auto-delete sniffer queue, and Flyway subdirectory patterns from Phase 5. Use Testcontainers Postgres + RabbitMQ. Seed the minimal order/inventory reservation rows needed by existing repositories or publish prerequisite events if existing helpers are available. Publish a canonical `payment.failed` envelope to `payments.tx` with `messageId=eventId`, then Awaitility-poll until inventory release and order cancellation are observable. Redeliver the same event and assert counts/status remain stable. Avoid broad `processed_events.count()` assertions; filter by eventId per Phase 5 lesson.</action>
  <verify><automated>./gradlew :infra-tests:test --tests '*PaymentFailureCompensationE2ETest'</automated></verify>
  <acceptance_criteria>`PaymentFailureCompensationE2ETest.java` contains `payment.failed`, `Awaitility`, and filtered eventId assertions; test command passes.</acceptance_criteria>
  <done>QUAL-05 compensation path is proven against real service code and RabbitMQ semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| scheduler → saga event bus | Internal automated timeout emits compensating failure event |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Denial of Service | Pending payments | mitigate | Scheduled timeout releases stock by emitting payment.failed after configured duration. |
| T-06-13 | Tampering | Timeout race with callback | mitigate | Status guard PENDING only; first terminal transition wins. |
| T-06-14 | Repudiation | Timeout compensation | mitigate | Store failedAt/failureReason and outbox event with reason PAYMENT_TIMEOUT. |
</threat_model>

<verification>`./gradlew :payment-service:test --tests '*PaymentTimeoutJobTest'` and `./gradlew :infra-tests:test --tests '*PaymentFailureCompensationE2ETest'` pass.</verification>
<success_criteria>Payment timeout and payment failure both drive the same compensation path; duplicate failure events are idempotent.</success_criteria>
<output>After completion, create `.planning/phases/06-payment-iyzico/06-05-SUMMARY.md`</output>
